diff --git a/src/core/commands/builtInCommands.ts b/src/core/commands/builtInCommands.ts
index e98e0be..441cf6a 100644
--- a/src/core/commands/builtInCommands.ts
+++ b/src/core/commands/builtInCommands.ts
@@ -1,10 +1,11 @@
+import { t } from '@/i18n/i18n';
+
 /**
  * Claudian - Built-in slash commands
  *
  * System commands that perform actions (not prompt expansions).
  * These are handled separately from user-defined slash commands.
  */
-
 import { ProviderRegistry } from '../providers/ProviderRegistry';
 import type { ProviderCapabilities, ProviderId } from '../providers/types';
 
@@ -51,37 +52,37 @@ export const BUILT_IN_COMMANDS: BuiltInCommand[] = [
   {
     name: 'clear',
     aliases: ['new'],
-    description: 'Start a new conversation',
+    get description() { return t('ui.commandNew'); },
     action: 'clear',
   },
   {
     name: 'add-dir',
-    description: 'Add external context directory',
+    get description() { return t('ui.commandAddDirectory'); },
     action: 'add-dir',
     hasArgs: true,
     argumentHint: '[path/to/directory]',
   },
   {
     name: 'resume',
-    description: 'Resume a previous conversation',
+    get description() { return t('ui.commandResume'); },
     action: 'resume',
     requiredCapability: 'supportsNativeHistory',
   },
   {
     name: 'fork',
-    description: 'Fork entire conversation to new session',
+    get description() { return t('ui.commandFork'); },
     action: 'fork',
     requiredCapability: 'supportsFork',
   },
   {
     name: 'fast',
-    description: 'Toggle fast mode',
+    get description() { return t('ui.commandFast'); },
     action: 'fast',
     supportedProviderIds: ['codex'],
   },
   {
     name: 'instruction',
-    description: 'Save a reusable custom instruction',
+    get description() { return t('ui.commandInstruction'); },
     action: 'instruction',
     exact: true,
     requiredCapability: 'supportsInstructionMode',
diff --git a/src/core/security/approvalRules.ts b/src/core/security/approvalRules.ts
index ce16665..daf5741 100644
--- a/src/core/security/approvalRules.ts
+++ b/src/core/security/approvalRules.ts
@@ -1,5 +1,7 @@
 /** Permission utilities for tool action approval. */
 
+import { t } from '@/i18n/i18n';
+
 import {
   TOOL_BASH,
   TOOL_EDIT,
@@ -33,20 +35,20 @@ export function getActionPattern(toolName: string, input: Record<string, unknown
 }
 
 export function getActionDescription(toolName: string, input: Record<string, unknown>): string {
-  const pattern = getActionPattern(toolName, input) ?? '(unknown)';
+  const pattern = getActionPattern(toolName, input) ?? t('ui.actionUnknown');
   switch (toolName) {
     case TOOL_BASH:
-      return `Run command: ${pattern}`;
+      return t('ui.actionRun', { pattern });
     case TOOL_READ:
-      return `Read file: ${pattern}`;
+      return t('ui.actionRead', { pattern });
     case TOOL_WRITE:
-      return `Write to file: ${pattern}`;
+      return t('ui.actionWrite', { pattern });
     case TOOL_EDIT:
-      return `Edit file: ${pattern}`;
+      return t('ui.actionEdit', { pattern });
     case TOOL_GLOB:
-      return `Search files matching: ${pattern}`;
+      return t('ui.actionGlob', { pattern });
     case TOOL_GREP:
-      return `Search content matching: ${pattern}`;
+      return t('ui.actionGrep', { pattern });
     default:
       return `${toolName}: ${pattern}`;
   }
diff --git a/src/features/chat/ClaudianView.ts b/src/features/chat/ClaudianView.ts
index 543ff86..1c2f0f3 100644
--- a/src/features/chat/ClaudianView.ts
+++ b/src/features/chat/ClaudianView.ts
@@ -611,7 +611,7 @@ export class ClaudianView extends ItemView {
       cls: 'claudian-session-resizer',
     });
     this.sessionSidebarResizerEl.setAttribute('role', 'separator');
-    this.sessionSidebarResizerEl.setAttribute('aria-label', 'Resize conversation sessions');
+    this.sessionSidebarResizerEl.setAttribute('aria-label', t('ui.resizeConversationSessions'));
     this.sessionSidebarResizerEl.setAttribute('aria-orientation', 'vertical');
     this.sessionSidebarResizerEl.setAttribute('tabindex', '0');
     this.sessionSidebarResizerEl.addEventListener('pointerdown', (event) => {
@@ -692,7 +692,7 @@ export class ClaudianView extends ItemView {
       attr: { type: 'button' },
     });
     setIcon(this.newTabButtonEl, 'square-plus');
-    this.newTabButtonEl.setAttribute('aria-label', 'New tab');
+    this.newTabButtonEl.setAttribute('aria-label', t('chat.fork.targetNewTab'));
     this.newTabButtonEl.addEventListener('click', () => this.requestNewTab());
 
     const newBtn = navActionsEl.createEl('button', {
@@ -700,7 +700,7 @@ export class ClaudianView extends ItemView {
       attr: { type: 'button' },
     });
     setIcon(newBtn, 'square-pen');
-    newBtn.setAttribute('aria-label', 'New conversation');
+    newBtn.setAttribute('aria-label', t('ui.newConversation'));
     newBtn.addEventListener('click', () => this.requestNewConversation());
 
     // History dropdown
@@ -712,7 +712,7 @@ export class ClaudianView extends ItemView {
       attr: { type: 'button' },
     });
     setIcon(historyBtn, 'history');
-    historyBtn.setAttribute('aria-label', 'Chat history');
+    historyBtn.setAttribute('aria-label', t('ui.chatHistory'));
 
     this.historyDropdown = historyContainer.createDiv({
       cls: 'claudian-history-menu claudian-nav-dropup-menu',
@@ -761,19 +761,19 @@ export class ClaudianView extends ItemView {
   }
 
   private requestNewTab(): void {
-    void this.createNewTab().catch(() => new Notice('Failed to create tab'));
+    void this.createNewTab().catch(() => new Notice(t('ui.failedToCreateTab')));
   }
 
   private requestNewConversation(): void {
     void (async () => {
       await this.tabManager?.createNewConversation();
       this.updateHistoryDropdown();
-    })().catch(() => new Notice('Failed to create conversation'));
+    })().catch(() => new Notice(t('ui.failedToCreateConversation')));
   }
 
   private requestDualNew(): void {
     void this.activateOrCreateDraftTab()
-      .catch(() => new Notice('Failed to start a new conversation'));
+      .catch(() => new Notice(t('ui.failedToStartANewConversation')));
   }
 
   private async activateOrCreateDraftTab(): Promise<void> {
@@ -808,7 +808,7 @@ export class ClaudianView extends ItemView {
 
     const targetTab = await this.createNewTab();
     if (!targetTab) {
-      throw new Error('New conversation input is unavailable');
+      throw new Error(t('ui.newConversationInputIsUnavailable'));
     }
     if (!isSourceLive()) {
       if (
@@ -821,10 +821,10 @@ export class ClaudianView extends ItemView {
     }
     const inputController = targetTab.controllers.inputController;
     if (!inputController) {
-      throw new Error('New conversation input is unavailable');
+      throw new Error(t('ui.newConversationInputIsUnavailable'));
     }
     void inputController.sendMessage({ content: planContent }).catch(() => {
-      new Notice('Failed to start the approved plan in a new conversation');
+      new Notice(t('ui.failedToStartTheApprovedPlanInANewConversation'));
     });
     return true;
   }
@@ -931,7 +931,7 @@ export class ClaudianView extends ItemView {
   private handleTabClick(tabId: TabId): void {
     const switched = this.tabManager?.switchToTab(tabId);
     if (switched) {
-      void switched.catch(() => new Notice('Failed to switch tab'));
+      void switched.catch(() => new Notice(t('ui.failedToSwitchTab')));
     }
   }
 
@@ -943,7 +943,7 @@ export class ClaudianView extends ItemView {
       await this.tabManager?.closeTab(tabId, force);
       this.updateTabBarVisibility();
     } catch {
-      new Notice('Failed to close tab');
+      new Notice(t('ui.failedToCloseTab'));
     }
   }
 
@@ -1214,7 +1214,7 @@ export class ClaudianView extends ItemView {
       },
       sessionScope: isArchiveView ? 'archived' : 'active',
       sessionActionMode: isArchiveView ? 'archived' : 'active',
-      historyHeaderLabel: isArchiveView ? 'Archived' : 'Sessions',
+      historyHeaderLabel: isArchiveView ? t('ui.archived') : t('collab.sessionsSurfaceLabel'),
       allowConversationSelection: !isArchiveView,
       onSetConversationPinned: (id: string, isPinned: boolean) => (
         this.setConversationPinned(id, isPinned)
@@ -1296,10 +1296,10 @@ export class ClaudianView extends ItemView {
     const newControl = container.createDiv({ cls: 'claudian-session-new-control' });
     newControl.setAttribute('role', 'button');
     newControl.setAttribute('tabindex', '0');
-    newControl.setAttribute('aria-label', 'New');
+    newControl.setAttribute('aria-label', t('settings.newTabHotkey.name'));
     const newIcon = newControl.createSpan({ cls: 'claudian-session-new-icon' });
     setIcon(newIcon, 'square-pen');
-    newControl.createSpan({ cls: 'claudian-session-new-label', text: 'New' });
+    newControl.createSpan({ cls: 'claudian-session-new-label', text: t('settings.newTabHotkey.name') });
     newControl.addEventListener('click', () => this.requestSessionNew());
     newControl.addEventListener('keydown', (event) => {
       if (event.key !== 'Enter' && event.key !== ' ') return;
@@ -1319,11 +1319,11 @@ export class ClaudianView extends ItemView {
           type: 'search',
           autocomplete: 'off',
           placeholder: this.isArchiveSessionView
-            ? 'Search archived sessions'
-            : 'Search sessions',
+            ? t('ui.searchArchivedSessions')
+            : t('ui.searchSessions'),
           'aria-label': this.isArchiveSessionView
-            ? 'Search archived sessions'
-            : 'Search sessions',
+            ? t('ui.searchArchivedSessions')
+            : t('ui.searchSessions'),
         },
       });
       searchInput.value = this.sessionSearchQuery;
@@ -1368,10 +1368,10 @@ export class ClaudianView extends ItemView {
       const searchControl = container.createDiv({ cls: 'claudian-session-search-control' });
       searchControl.setAttribute('role', 'button');
       searchControl.setAttribute('tabindex', '0');
-      searchControl.setAttribute('aria-label', 'Search');
+      searchControl.setAttribute('aria-label', t('ui.search'));
       const searchIcon = searchControl.createSpan({ cls: 'claudian-session-nav-icon' });
       setIcon(searchIcon, 'search');
-      searchControl.createSpan({ cls: 'claudian-session-nav-label', text: 'Search' });
+      searchControl.createSpan({ cls: 'claudian-session-nav-label', text: t('ui.search') });
       const activateSearch = (): void => this.activateSessionSearch();
       searchControl.addEventListener('click', activateSearch);
       searchControl.addEventListener('keydown', (event) => {
@@ -1385,7 +1385,7 @@ export class ClaudianView extends ItemView {
     const archiveControl = container.createDiv({ cls: 'claudian-session-archive-control' });
     archiveControl.setAttribute('role', 'button');
     archiveControl.setAttribute('tabindex', '0');
-    const archiveLabel = this.isArchiveSessionView ? 'Sessions' : 'Archive';
+    const archiveLabel = this.isArchiveSessionView ? t('collab.sessionsSurfaceLabel') : t('ui.archive');
     archiveControl.setAttribute('aria-label', archiveLabel);
     const archiveIcon = archiveControl.createSpan({ cls: 'claudian-session-nav-icon' });
     setIcon(archiveIcon, this.isArchiveSessionView ? 'arrow-left' : 'archive');
@@ -1421,14 +1421,14 @@ export class ClaudianView extends ItemView {
           icon,
           allCollapsed ? 'expand' : 'collapse',
         ),
-        allCollapsed ? 'Expand all groups' : 'Collapse all groups',
+        allCollapsed ? t('ui.expandAllGroups') : t('ui.collapseAllGroups'),
         () => this.toggleAllSessionGroups(),
       );
     }
     const optionsButton = this.createSessionHeaderAction(
       actions,
       'ellipsis',
-      'Session options',
+      t('ui.sessionOptions'),
       (event) => this.showSessionOptionsMenu(optionsButton, event),
     );
 
@@ -1664,7 +1664,7 @@ export class ClaudianView extends ItemView {
 
   private async startLinkedContentConversation(contentPath: string): Promise<void> {
     if (!this.tabManager) {
-      throw new Error('Chat tabs are unavailable');
+      throw new Error(t('ui.chatTabsAreUnavailable'));
     }
     if (this.isArchiveSessionView) {
       this.setArchiveSessionView(false);
@@ -1672,7 +1672,7 @@ export class ClaudianView extends ItemView {
 
     await this.tabManager.waitForTabSwitchIdle();
     if (!this.contentExists(contentPath)) {
-      throw new Error('Linked content is no longer available');
+      throw new Error(t('ui.linkedContentIsNoLongerAvailable'));
     }
     const initialTabId = this.tabManager.getActiveTabId();
     const initialSwitchRevision = this.tabManager.getTabSwitchRequestRevision();
@@ -1684,12 +1684,12 @@ export class ClaudianView extends ItemView {
       lifecycleState: 'provisional',
     });
     if (!tab) {
-      throw new Error('Failed to create a provisional chat tab');
+      throw new Error(t('ui.failedToCreateAProvisionalChatTab'));
     }
 
     try {
       if (!this.contentExists(contentPath)) {
-        throw new Error('Linked content is no longer available');
+        throw new Error(t('ui.linkedContentIsNoLongerAvailable'));
       }
       tab.ui.linkedContentController.selectExplicit(contentPath);
       this.updateTabBarVisibility();
@@ -1902,7 +1902,7 @@ export class ClaudianView extends ItemView {
     const list = container.querySelector<HTMLElement>('.claudian-history-list');
     if (!list) return;
 
-    const label = this.isArchiveSessionView ? 'Sessions' : 'Archive';
+    const label = this.isArchiveSessionView ? t('collab.sessionsSurfaceLabel') : t('ui.archive');
     const control = list.createDiv({ cls: 'claudian-history-archive-control' });
     control.setAttribute('role', 'button');
     control.setAttribute('tabindex', '0');
@@ -1975,14 +1975,14 @@ export class ClaudianView extends ItemView {
 
     const openTabs = this.getOpenConversationTabs(conversationId);
     if (openTabs.some(({ tab }) => tab.state.isStreaming)) {
-      new Notice('Running sessions cannot be archived');
+      new Notice(t('ui.runningSessionsCannotBeArchived'));
       return;
     }
 
     for (const { manager, tab } of openTabs) {
       const didClose = await manager.closeTab(tab.id);
       if (!didClose) {
-        throw new Error('Failed to close the session before archiving');
+        throw new Error(t('ui.failedToCloseTheSessionBeforeArchiving'));
       }
     }
     await this.plugin.setConversationArchived(conversationId, true);
@@ -2092,7 +2092,7 @@ export class ClaudianView extends ItemView {
       && [...sessionGroupKeys].every(groupKey => collapsedGroupKeys.has(groupKey));
     button.setAttribute(
       'aria-label',
-      allCollapsed ? 'Expand all groups' : 'Collapse all groups',
+      allCollapsed ? t('ui.expandAllGroups') : t('ui.collapseAllGroups'),
     );
     const icon = button.querySelector<HTMLElement>('.claudian-session-header-icon');
     if (icon) {
@@ -2138,26 +2138,26 @@ export class ClaudianView extends ItemView {
     const sort = this.getSessionManagerSort();
 
     menu.addItem(item => item
-      .setTitle('Organize sessions')
+      .setTitle(t('ui.organizeSessions'))
       .setIsLabel(true));
     menu.addItem(item => item
-      .setTitle('In one list')
+      .setTitle(t('ui.inOneList'))
       .setChecked(organization === 'list')
       .onClick(() => this.setSessionManagerOrganization('list')));
     menu.addItem(item => item
-      .setTitle('By linked content')
+      .setTitle(t('ui.byLinkedContent'))
       .setChecked(organization === 'linked-content')
       .onClick(() => this.setSessionManagerOrganization('linked-content')));
     menu.addSeparator();
     menu.addItem(item => item
-      .setTitle('Sort sessions by')
+      .setTitle(t('ui.sortSessionsBy'))
       .setIsLabel(true));
     menu.addItem(item => item
-      .setTitle('Last activity')
+      .setTitle(t('ui.lastActivity'))
       .setChecked(sort === 'last-updated')
       .onClick(() => this.setSessionManagerSort('last-updated')));
     menu.addItem(item => item
-      .setTitle('Created')
+      .setTitle(t('ui.created'))
       .setChecked(sort === 'created')
       .onClick(() => this.setSessionManagerSort('created')));
 
@@ -2175,14 +2175,14 @@ export class ClaudianView extends ItemView {
     void this.plugin.mutateSettings((settings) => {
       settings.sessionManagerOrganization = organization;
     }).then(() => this.refreshSessionManagerPresentation())
-      .catch(() => new Notice('Failed to update session organization'));
+      .catch(() => new Notice(t('ui.failedToUpdateSessionOrganization')));
   }
 
   private setSessionManagerSort(sort: 'last-updated' | 'created'): void {
     void this.plugin.mutateSettings((settings) => {
       settings.sessionManagerSort = sort;
     }).then(() => this.refreshSessionManagerPresentation())
-      .catch(() => new Notice('Failed to update session sorting'));
+      .catch(() => new Notice(t('ui.failedToUpdateSessionSorting')));
   }
 
   private refreshSessionManagerPresentation(): void {
@@ -2375,7 +2375,7 @@ export class ClaudianView extends ItemView {
 
     const cleanup = (this.tabManager?.discardProvisionalTabs() ?? Promise.resolve())
       .catch(() => {
-        new Notice('Failed to close the provisional session preview');
+        new Notice(t('ui.failedToCloseTheProvisionalSessionPreview'));
       });
     this.pendingProvisionalTabCleanup = cleanup;
     void cleanup.finally(() => {
@@ -2556,7 +2556,7 @@ export class ClaudianView extends ItemView {
               }
             })
             .catch((error: unknown) => {
-              new Notice(error instanceof Error ? error.message : 'Failed to change permission mode.');
+              new Notice(error instanceof Error ? error.message : t('ui.failedToChangePermissionMode'));
             });
         } else {
           activeTab.state.prePlanPermissionMode = current;
@@ -2568,7 +2568,7 @@ export class ClaudianView extends ItemView {
             if (activeMode !== 'plan') {
               activeTab.state.prePlanPermissionMode = null;
             }
-            new Notice(error instanceof Error ? error.message : 'Failed to change permission mode.');
+            new Notice(error instanceof Error ? error.message : t('ui.failedToChangePermissionMode'));
           });
         }
       }
diff --git a/src/features/chat/constants.ts b/src/features/chat/constants.ts
index f258cb4..333633f 100644
--- a/src/features/chat/constants.ts
+++ b/src/features/chat/constants.ts
@@ -1,114 +1,114 @@
 /** Random flavor words shown when response completes (e.g., "Baked for 1:23"). */
 export const COMPLETION_FLAVOR_WORDS = [
-  'Baked',
-  'Cooked',
-  'Crunched',
-  'Brewed',
-  'Crafted',
-  'Forged',
-  'Conjured',
-  'Whipped up',
-  'Stirred',
-  'Simmered',
-  'Toasted',
-  'Sautéed',
-  'Finagled',
-  'Marinated',
-  'Distilled',
-  'Fermented',
-  'Percolated',
-  'Steeped',
-  'Roasted',
-  'Cured',
-  'Smoked',
-  'Cogitated',
+  'ui.completionFlavor0',
+  'ui.completionFlavor1',
+  'ui.completionFlavor2',
+  'ui.completionFlavor3',
+  'ui.completionFlavor4',
+  'ui.completionFlavor5',
+  'ui.completionFlavor6',
+  'ui.completionFlavor7',
+  'ui.completionFlavor8',
+  'ui.completionFlavor9',
+  'ui.completionFlavor10',
+  'ui.completionFlavor11',
+  'ui.completionFlavor12',
+  'ui.completionFlavor13',
+  'ui.completionFlavor14',
+  'ui.completionFlavor15',
+  'ui.completionFlavor16',
+  'ui.completionFlavor17',
+  'ui.completionFlavor18',
+  'ui.completionFlavor19',
+  'ui.completionFlavor20',
+  'ui.completionFlavor21',
 ] as const;
 
 /** Random flavor texts shown while Claude is thinking. */
 export const FLAVOR_TEXTS = [
   // Classic
-  'Thinking...',
-  'Pondering...',
-  'Processing...',
-  'Analyzing...',
-  'Considering...',
-  'Working on it...',
-  'Vibing...',
-  'One moment...',
-  'On it...',
+  'ui.thinkingFlavor0',
+  'ui.thinkingFlavor1',
+  'ui.thinkingFlavor2',
+  'ui.thinkingFlavor3',
+  'ui.thinkingFlavor4',
+  'ui.thinkingFlavor5',
+  'ui.thinkingFlavor6',
+  'ui.thinkingFlavor7',
+  'ui.thinkingFlavor8',
   // Thoughtful
-  'Ruminating...',
-  'Contemplating...',
-  'Reflecting...',
-  'Mulling it over...',
-  'Let me think...',
-  'Hmm...',
-  'Cogitating...',
-  'Deliberating...',
-  'Weighing options...',
-  'Gathering thoughts...',
+  'ui.thinkingFlavor9',
+  'ui.thinkingFlavor10',
+  'ui.thinkingFlavor11',
+  'ui.thinkingFlavor12',
+  'ui.thinkingFlavor13',
+  'ui.thinkingFlavor14',
+  'ui.thinkingFlavor15',
+  'ui.thinkingFlavor16',
+  'ui.thinkingFlavor17',
+  'ui.thinkingFlavor18',
   // Playful
-  'Brewing ideas...',
-  'Connecting dots...',
-  'Assembling thoughts...',
-  'Spinning up neurons...',
-  'Loading brilliance...',
-  'Consulting the oracle...',
-  'Summoning knowledge...',
-  'Crunching thoughts...',
-  'Dusting off neurons...',
-  'Wrangling ideas...',
-  'Herding thoughts...',
-  'Juggling concepts...',
-  'Untangling this...',
-  'Piecing it together...',
+  'ui.thinkingFlavor19',
+  'ui.thinkingFlavor20',
+  'ui.thinkingFlavor21',
+  'ui.thinkingFlavor22',
+  'ui.thinkingFlavor23',
+  'ui.thinkingFlavor24',
+  'ui.thinkingFlavor25',
+  'ui.thinkingFlavor26',
+  'ui.thinkingFlavor27',
+  'ui.thinkingFlavor28',
+  'ui.thinkingFlavor29',
+  'ui.thinkingFlavor30',
+  'ui.thinkingFlavor31',
+  'ui.thinkingFlavor32',
   // Cozy
-  'Sipping coffee...',
-  'Warming up...',
-  'Getting cozy with this...',
-  'Settling in...',
-  'Making tea...',
-  'Grabbing a snack...',
+  'ui.thinkingFlavor33',
+  'ui.thinkingFlavor34',
+  'ui.thinkingFlavor35',
+  'ui.thinkingFlavor36',
+  'ui.thinkingFlavor37',
+  'ui.thinkingFlavor38',
   // Technical
-  'Parsing...',
-  'Compiling thoughts...',
-  'Running inference...',
-  'Querying the void...',
-  'Defragmenting brain...',
-  'Allocating memory...',
-  'Optimizing...',
-  'Indexing...',
-  'Syncing neurons...',
+  'ui.thinkingFlavor39',
+  'ui.thinkingFlavor40',
+  'ui.thinkingFlavor41',
+  'ui.thinkingFlavor42',
+  'ui.thinkingFlavor43',
+  'ui.thinkingFlavor44',
+  'ui.thinkingFlavor45',
+  'ui.thinkingFlavor46',
+  'ui.thinkingFlavor47',
   // Zen
-  'Breathing...',
-  'Finding clarity...',
-  'Channeling focus...',
-  'Centering...',
-  'Aligning chakras...',
-  'Meditating on this...',
+  'ui.thinkingFlavor48',
+  'ui.thinkingFlavor49',
+  'ui.thinkingFlavor50',
+  'ui.thinkingFlavor51',
+  'ui.thinkingFlavor52',
+  'ui.thinkingFlavor53',
   // Whimsical
-  'Asking the stars...',
-  'Reading tea leaves...',
-  'Shaking the magic 8-ball...',
-  'Consulting ancient scrolls...',
-  'Decoding the matrix...',
-  'Communing with the ether...',
-  'Peering into the abyss...',
-  'Channeling the cosmos...',
+  'ui.thinkingFlavor54',
+  'ui.thinkingFlavor55',
+  'ui.thinkingFlavor56',
+  'ui.thinkingFlavor57',
+  'ui.thinkingFlavor58',
+  'ui.thinkingFlavor59',
+  'ui.thinkingFlavor60',
+  'ui.thinkingFlavor61',
   // Action
-  'Diving in...',
-  'Rolling up sleeves...',
-  'Getting to work...',
-  'Tackling this...',
-  'On the case...',
-  'Investigating...',
-  'Exploring...',
-  'Digging deeper...',
+  'ui.thinkingFlavor62',
+  'ui.thinkingFlavor63',
+  'ui.thinkingFlavor64',
+  'ui.thinkingFlavor65',
+  'ui.thinkingFlavor66',
+  'ui.thinkingFlavor67',
+  'ui.thinkingFlavor68',
+  'ui.thinkingFlavor69',
   // Casual
-  'Bear with me...',
-  'Hang tight...',
-  'Just a sec...',
-  'Working my magic...',
-  'Almost there...',
-  'Give me a moment...',
-];
+  'ui.thinkingFlavor70',
+  'ui.thinkingFlavor71',
+  'ui.thinkingFlavor72',
+  'ui.thinkingFlavor73',
+  'ui.thinkingFlavor74',
+  'ui.thinkingFlavor75',
+] as const;
diff --git a/src/features/chat/controllers/ConversationController.ts b/src/features/chat/controllers/ConversationController.ts
index a82f17a..d084b9d 100644
--- a/src/features/chat/controllers/ConversationController.ts
+++ b/src/features/chat/controllers/ConversationController.ts
@@ -14,7 +14,7 @@ import type {
   SessionManagerOrganization,
   SessionManagerSort,
 } from '../../../core/types';
-import { t } from '../../../i18n/i18n';
+import { getLocale, t } from '../../../i18n/i18n';
 import { createProviderIconSvg } from '../../../shared/icons';
 import { confirm } from '../../../shared/modals/ConfirmModal';
 import { extractUserDisplayContent } from '../../../utils/context';
@@ -417,7 +417,7 @@ export class ConversationController {
     const msgs = state.messages;
     const userIdx = msgs.findIndex(m => m.id === userMessageId);
     if (userIdx === -1) {
-      new Notice(t('chat.rewind.failed', { error: 'Message not found' }));
+      new Notice(t('chat.rewind.failed', { error: t('chat.fork.errorMessageNotFound') }));
       return;
     }
     const userMsg = msgs[userIdx];
@@ -454,24 +454,24 @@ export class ConversationController {
           initialized = await this.deps.ensureExecutionInitialized();
         } catch (e) {
           new Notice(t('chat.rewind.failed', {
-            error: e instanceof Error ? e.message : 'Failed to initialize agent service',
+            error: e instanceof Error ? e.message : t('ui.failedToInitializeAgentService'),
           }));
           return;
         }
         if (this.deps.isDisposed?.()) return;
         if (!initialized) {
-          new Notice(t('chat.rewind.failed', { error: 'Agent service not available' }));
+          new Notice(t('chat.rewind.failed', { error: t('ui.agentServiceNotAvailable') }));
           return;
         }
       }
 
       const coordinator = this.getExecutionCoordinator();
       if (!coordinator) {
-        new Notice(t('chat.rewind.failed', { error: 'Agent execution not available' }));
+        new Notice(t('chat.rewind.failed', { error: t('ui.agentExecutionNotAvailable') }));
         return;
       }
       if (!isTargetCurrent()) {
-        new Notice(t('chat.rewind.failed', { error: 'Conversation changed while rewinding.' }));
+        new Notice(t('chat.rewind.failed', { error: t('ui.conversationChangedWhileRewinding') }));
         return;
       }
 
@@ -488,12 +488,12 @@ export class ConversationController {
           );
         } catch (e) {
           new Notice(t('chat.rewind.failed', {
-            error: e instanceof Error ? e.message : 'Unknown error',
+            error: e instanceof Error ? e.message : t('ui.unknownError'),
           }));
           return;
         }
         if (!preview.canRewind) {
-          new Notice(t('chat.rewind.cannot', { error: preview.error ?? 'Unknown error' }));
+          new Notice(t('chat.rewind.cannot', { error: preview.error ?? t('ui.unknownError') }));
           return;
         }
         if (preview.conflicts && preview.conflicts.length > 0) {
@@ -504,7 +504,7 @@ export class ConversationController {
           return;
         }
         if (!isTargetCurrent() || this.getExecutionCoordinator() !== coordinator) {
-          new Notice(t('chat.rewind.failed', { error: 'Conversation changed while rewinding.' }));
+          new Notice(t('chat.rewind.failed', { error: t('ui.conversationChangedWhileRewinding') }));
           return;
         }
       }
@@ -521,7 +521,7 @@ export class ConversationController {
         return;
       }
       if (!isTargetCurrent() || this.getExecutionCoordinator() !== coordinator) {
-        new Notice(t('chat.rewind.failed', { error: 'Conversation changed while rewinding.' }));
+        new Notice(t('chat.rewind.failed', { error: t('ui.conversationChangedWhileRewinding') }));
         return;
       }
 
@@ -533,15 +533,15 @@ export class ConversationController {
           mode,
         );
       } catch (e) {
-        new Notice(t('chat.rewind.failed', { error: e instanceof Error ? e.message : 'Unknown error' }));
+        new Notice(t('chat.rewind.failed', { error: e instanceof Error ? e.message : t('ui.unknownError') }));
         return;
       }
       if (!result.canRewind) {
-        new Notice(t('chat.rewind.cannot', { error: result.error ?? 'Unknown error' }));
+        new Notice(t('chat.rewind.cannot', { error: result.error ?? t('ui.unknownError') }));
         return;
       }
       if (!isTargetCurrent() || this.getExecutionCoordinator() !== coordinator) {
-        new Notice(t('chat.rewind.failed', { error: 'Conversation changed while rewinding.' }));
+        new Notice(t('chat.rewind.failed', { error: t('ui.conversationChangedWhileRewinding') }));
         return;
       }
 
@@ -579,7 +579,7 @@ export class ConversationController {
             },
         );
       } catch (e) {
-        saveError = e instanceof Error ? e.message : 'Failed to save';
+        saveError = e instanceof Error ? e.message : t('ui.failedToSave');
       }
 
       if (saveError) {
@@ -872,7 +872,7 @@ export class ConversationController {
         });
         pinnedHeader.createSpan({
           cls: 'claudian-history-section-label',
-          text: 'Pinned',
+          text: t('ui.pinned'),
         });
         pinnedList = pinnedSection.createDiv({
           cls: 'claudian-history-section-items',
@@ -896,14 +896,14 @@ export class ConversationController {
       });
       sessionsHeader.createSpan({
         cls: 'claudian-history-section-label',
-        text: options.showArchivedSection ? 'Archived' : 'Sessions',
+        text: options.showArchivedSection ? t('ui.archived') : t('collab.sessionsSurfaceLabel'),
       });
       sessionList = sessionsSection.createDiv({
         cls: 'claudian-history-section-items claudian-session-list-items',
       });
     } else {
       const dropdownHeader = container.createDiv({ cls: 'claudian-history-header' });
-      dropdownHeader.createSpan({ text: options.historyHeaderLabel ?? 'Sessions' });
+      dropdownHeader.createSpan({ text: options.historyHeaderLabel ?? t('collab.sessionsSurfaceLabel') });
       list = container.createDiv({ cls: 'claudian-history-list' });
       sessionList = list;
     }
@@ -921,7 +921,7 @@ export class ConversationController {
       }
       sessionList.createDiv({
         cls: 'claudian-history-empty',
-        text: searchTerms.length > 0 ? 'No matching sessions' : 'No conversations',
+        text: searchTerms.length > 0 ? t('ui.noMatchingSessions') : t('ui.noConversations'),
       });
       options.onBeforeRestoreListState?.(container);
       if (pinnedList) pinnedList.scrollTop = previousPinnedScrollTop;
@@ -1029,7 +1029,7 @@ export class ConversationController {
     if (renderedConversationCount < visibleConversationTotal && !options.signal?.aborted) {
       const loadMoreButton = sessionList.createEl('button', {
         cls: 'claudian-history-load-more',
-        text: `Load more (${visibleConversationTotal - renderedConversationCount} remaining)`,
+        text: t('ui.loadMoreRemainingFormat', { p0: visibleConversationTotal - renderedConversationCount }),
       });
       loadMoreButton.addEventListener('click', () => {
         if (options.signal?.aborted) return;
@@ -1109,7 +1109,7 @@ export class ConversationController {
     if (section.kind === 'missing') {
       groupHeader.createSpan({
         cls: 'claudian-session-group-status',
-        text: 'Missing',
+        text: t('ui.missing'),
       });
     }
     const groupStatusIndicator = groupStatusKind === null
@@ -1135,16 +1135,16 @@ export class ConversationController {
       setIcon(newConversationButton, 'square-pen');
       newConversationButton.setAttribute(
         'aria-label',
-        `New chat for ${section.label ?? contentPath}`,
+        t('ui.newChatForFormat', { p0: section.label ?? contentPath }),
       );
       newConversationButton.setAttribute(
         'title',
-        `New chat for ${section.label ?? contentPath}`,
+        t('ui.newChatForFormat', { p0: section.label ?? contentPath }),
       );
       const startConversation = (): void => {
         runConversationAction(
           () => startLinkedContentConversation(contentPath),
-          'Failed to start a chat for this Linked content',
+          t('ui.failedToStartAChatForThisLinkedContent'),
         );
       };
       newConversationButton.addEventListener('click', (event) => {
@@ -1218,13 +1218,13 @@ export class ConversationController {
         const menu = new Menu().setUseNativeMenu(false);
         if (canToggleLinkedContentPin && onSetLinkedContentPinned) {
           menu.addItem(menuItem => menuItem
-            .setTitle(isPinnedLinkedContent ? 'Unpin Linked content' : 'Pin Linked content')
+            .setTitle(isPinnedLinkedContent ? t('ui.unpinLinkedContent') : t('ui.pinLinkedContent'))
             .onClick(() => {
               runConversationAction(
                 () => onSetLinkedContentPinned(contentPath, !isPinnedLinkedContent),
                 isPinnedLinkedContent
-                  ? 'Failed to unpin Linked content'
-                  : 'Failed to pin Linked content',
+                  ? t('ui.failedToUnpinLinkedContent')
+                  : t('ui.failedToPinLinkedContent'),
               );
             }));
         }
@@ -1237,13 +1237,13 @@ export class ConversationController {
           if (canToggleLinkedContentPin) menu.addSeparator();
           menu.addItem((menuItem) => {
             menuItem
-              .setTitle('Archive all sessions')
+              .setTitle(t('ui.archiveAllSessions'))
               .setDisabled(archivableConversationIds.length === 0);
             if (archivableConversationIds.length > 0) {
               menuItem.onClick(() => {
                 runConversationAction(
                   () => onSetConversationsArchived(archivableConversationIds),
-                  'Failed to archive Linked content sessions',
+                  t('ui.failedToArchiveLinkedContentSessions'),
                 );
               });
             }
@@ -1385,9 +1385,9 @@ export class ConversationController {
         runConversationAction(
           () => this.runHistoryAction(
             () => options.onSelectConversation(conversation.id),
-            'Failed to load conversation',
+            t('ui.failedToLoadConversation'),
           ),
-          'Failed to load conversation',
+          t('ui.failedToLoadConversation'),
         );
       };
       if (options.showMetadataPopover) {
@@ -1408,9 +1408,9 @@ export class ConversationController {
           runConversationAction(
             () => this.runHistoryAction(
               () => options.onOpenConversationInNewTab?.(conversation.id, true),
-              'Failed to load conversation',
+              t('ui.failedToLoadConversation'),
             ),
-            'Failed to load conversation',
+            t('ui.failedToLoadConversation'),
           );
           return;
         }
@@ -1426,9 +1426,9 @@ export class ConversationController {
           runConversationAction(
             () => this.runHistoryAction(
               () => options.onOpenConversationInNewTab?.(conversation.id, true),
-              'Failed to load conversation',
+              t('ui.failedToLoadConversation'),
             ),
-            'Failed to load conversation',
+            t('ui.failedToLoadConversation'),
           );
         });
       }
@@ -1452,16 +1452,16 @@ export class ConversationController {
         cls: 'claudian-action-btn claudian-action-loading',
       });
       setIcon(loadingEl, 'loader-2');
-      loadingEl.setAttribute('aria-label', 'Generating title...');
+      loadingEl.setAttribute('aria-label', t('ui.generatingTitle'));
     } else if (conversation.titleGenerationStatus === 'failed') {
       const regenerateBtn = actions.createEl('button', { cls: 'claudian-action-btn' });
       setIcon(regenerateBtn, 'refresh-cw');
-      regenerateBtn.setAttribute('aria-label', 'Regenerate title');
+      regenerateBtn.setAttribute('aria-label', t('ui.regenerateTitle'));
       regenerateBtn.addEventListener('click', (event) => {
         event.stopPropagation();
         runConversationAction(
           () => this.regenerateTitle(conversation.id),
-          'Failed to regenerate response',
+          t('ui.failedToRegenerateResponse'),
         );
       });
     }
@@ -1471,15 +1471,15 @@ export class ConversationController {
         cls: 'claudian-action-btn claudian-open-new-tab-btn',
       });
       setIcon(openInNewTabBtn, 'square-plus');
-      openInNewTabBtn.setAttribute('aria-label', 'Open in new tab');
+      openInNewTabBtn.setAttribute('aria-label', t('ui.openInNewTab'));
       openInNewTabBtn.addEventListener('click', (event) => {
         event.stopPropagation();
         runConversationAction(
           () => this.runHistoryAction(
             () => options.onOpenConversationInNewTab?.(conversation.id, true),
-            'Failed to load conversation',
+            t('ui.failedToLoadConversation'),
           ),
-          'Failed to load conversation',
+          t('ui.failedToLoadConversation'),
         );
       });
     }
@@ -1489,15 +1489,15 @@ export class ConversationController {
         cls: 'claudian-action-btn claudian-delete-btn',
       });
       setIcon(deleteBtn, 'trash-2');
-      deleteBtn.setAttribute('aria-label', 'Delete');
+      deleteBtn.setAttribute('aria-label', t('common.delete'));
       deleteBtn.addEventListener('click', (event) => {
         event.stopPropagation();
         runConversationAction(
           () => this.runHistoryAction(
             () => this.deleteHistoryConversation(conversation.id, options),
-            'Failed to delete conversation',
+            t('ui.failedToDeleteConversation'),
           ),
-          'Failed to delete conversation',
+          t('ui.failedToDeleteConversation'),
         );
       });
     };
@@ -1507,15 +1507,15 @@ export class ConversationController {
         cls: 'claudian-action-btn claudian-assign-device-btn',
       });
       setIcon(assignDeviceBtn, 'monitor-down');
-      assignDeviceBtn.setAttribute('aria-label', 'Assign to this device');
+      assignDeviceBtn.setAttribute('aria-label', t('ui.assignToThisDevice'));
       assignDeviceBtn.addEventListener('click', (event) => {
         event.stopPropagation();
         runConversationAction(
           () => this.runHistoryAction(
             () => options.onAssignConversationToDevice?.(conversation.id),
-            'Failed to assign session to this device',
+            t('ui.failedToAssignSessionToThisDevice'),
           ),
-          'Failed to assign session to this device',
+          t('ui.failedToAssignSessionToThisDevice'),
         );
       });
     }
@@ -1528,15 +1528,15 @@ export class ConversationController {
             cls: 'claudian-action-btn claudian-pin-btn',
           });
           setIcon(pinBtn, isPinned ? 'pin-off' : 'pin');
-          pinBtn.setAttribute('aria-label', isPinned ? 'Unpin' : 'Pin');
+          pinBtn.setAttribute('aria-label', isPinned ? t('ui.unpin') : t('ui.pin'));
           pinBtn.addEventListener('click', (event) => {
             event.stopPropagation();
             runConversationAction(
               () => this.runHistoryAction(
                 () => options.onSetConversationPinned?.(conversation.id, !isPinned),
-                isPinned ? 'Failed to unpin session' : 'Failed to pin session',
+                isPinned ? t('ui.failedToUnpinSession') : t('ui.failedToPinSession'),
               ),
-              isPinned ? 'Failed to unpin session' : 'Failed to pin session',
+              isPinned ? t('ui.failedToUnpinSession') : t('ui.failedToPinSession'),
             );
           });
         }
@@ -1547,7 +1547,7 @@ export class ConversationController {
         setIcon(archiveBtn, 'archive');
         archiveBtn.setAttribute(
           'aria-label',
-          isRunning ? 'Cannot archive a running session' : 'Archive',
+          isRunning ? t('ui.cannotArchiveARunningSession') : t('ui.archive'),
         );
         if (isRunning) {
           archiveBtn.setAttribute('disabled', '');
@@ -1557,9 +1557,9 @@ export class ConversationController {
             runConversationAction(
               () => this.runHistoryAction(
                 () => options.onSetConversationArchived?.(conversation.id, true),
-                'Failed to archive session',
+                t('ui.failedToArchiveSession'),
               ),
-              'Failed to archive session',
+              t('ui.failedToArchiveSession'),
             );
           });
         }
@@ -1569,22 +1569,22 @@ export class ConversationController {
         cls: 'claudian-action-btn claudian-restore-btn',
       });
       setIcon(restoreBtn, 'undo-2');
-      restoreBtn.setAttribute('aria-label', 'Restore');
+      restoreBtn.setAttribute('aria-label', t('ui.restore'));
       restoreBtn.addEventListener('click', (event) => {
         event.stopPropagation();
         runConversationAction(
           () => this.runHistoryAction(
             () => options.onSetConversationArchived?.(conversation.id, false),
-            'Failed to restore session',
+            t('ui.failedToRestoreSession'),
           ),
-          'Failed to restore session',
+          t('ui.failedToRestoreSession'),
         );
       });
       createDeleteButton();
     } else {
       const renameBtn = actions.createEl('button', { cls: 'claudian-action-btn' });
       setIcon(renameBtn, 'pencil');
-      renameBtn.setAttribute('aria-label', 'Rename');
+      renameBtn.setAttribute('aria-label', t('ui.rename'));
       renameBtn.addEventListener('click', (event) => {
         event.stopPropagation();
         this.showRenameEditor(item, conversation.id, conversation.title, options);
@@ -1664,10 +1664,10 @@ export class ConversationController {
         ? 'x-circle'
         : 'loader-2';
     const label = kind === 'action-required'
-      ? 'Needs your input'
+      ? t('ui.needsYourInput')
       : kind === 'error'
-        ? 'Stopped with an error'
-        : 'Running';
+        ? t('ui.stoppedWithAnError')
+        : t('collab.host.status.running');
     setIcon(indicator, icon);
     indicator.setAttribute('aria-label', label);
     return indicator;
@@ -1772,13 +1772,13 @@ export class ConversationController {
     this.renderSessionMetadataRow(
       hoverEl,
       'calendar-days',
-      'Created',
+      t('ui.created'),
       this.formatMetadataDate(conversation.createdAt),
     );
     this.renderSessionMetadataRow(
       hoverEl,
       'clock-3',
-      'Last active',
+      t('ui.lastActive'),
       this.formatMetadataDateTime(conversation.lastActivityAt),
     );
 
@@ -1967,21 +1967,21 @@ export class ConversationController {
     }
 
     if (openState !== 'closed' && location === 'other-view') {
-      return isRunning ? 'Running in another pane' : 'Open in another pane';
+      return isRunning ? t('ui.runningInAnotherPane') : t('ui.openInAnotherPane');
     }
 
     if (isRunning) {
-      if (openState === 'closed') return 'Running';
-      return `Running in ${this.getHistoryTabLabel(status)}`;
+      if (openState === 'closed') return t('collab.host.status.running');
+      return t('ui.runningInFormat', { p0: this.getHistoryTabLabel(status) });
     }
 
     switch (openState) {
       case 'current':
         return typeof status.tabIndex === 'number'
-          ? `Current tab ${status.tabIndex}`
-          : 'Current session';
+          ? t('ui.currentTabFormat', { p0: status.tabIndex })
+          : t('ui.currentSession');
       case 'open':
-        return `Open in ${this.getHistoryTabLabel(status)}`;
+        return t('ui.openInFormat', { p0: this.getHistoryTabLabel(status) });
       case 'closed':
         return this.formatDate(timestamp);
     }
@@ -1989,11 +1989,11 @@ export class ConversationController {
 
   private getHistoryTabLabel(status: HistoryConversationStatus): string {
     if (typeof status.tabIndex === 'number') {
-      return `tab ${status.tabIndex}`;
+      return t('ui.tabFormat', { p0: status.tabIndex });
     }
 
     if (status.openState === 'current') {
-      return 'current tab';
+      return t('ui.currentTab');
     }
 
     return 'tab';
@@ -2042,28 +2042,28 @@ export class ConversationController {
     if (options.showOpenStateActions !== false && openState !== 'current') {
       if (openState === 'closed' && options.onOpenConversationInNewTab) {
         menu.addItem((menuItem) => menuItem
-          .setTitle('Open in new tab')
+          .setTitle(t('ui.openInNewTab'))
           .onClick(() => {
             void this.runHistoryAction(
               () => options.onOpenConversationInNewTab?.(conversationId, true),
-              'Failed to load conversation',
+              t('ui.failedToLoadConversation'),
             );
           }));
         menu.addItem((menuItem) => menuItem
-          .setTitle('Open in background tab')
+          .setTitle(t('ui.openInBackgroundTab'))
           .onClick(() => {
             void this.runHistoryAction(
               () => options.onOpenConversationInNewTab?.(conversationId, false),
-              'Failed to load conversation',
+              t('ui.failedToLoadConversation'),
             );
           }));
       } else if (openState === 'open') {
         menu.addItem((menuItem) => menuItem
-          .setTitle('Switch to open session')
+          .setTitle(t('ui.switchToOpenSession'))
           .onClick(() => {
             void this.runHistoryAction(
               () => options.onSelectConversation(conversationId),
-              'Failed to load conversation',
+              t('ui.failedToLoadConversation'),
             );
           }));
       }
@@ -2071,19 +2071,19 @@ export class ConversationController {
 
     if (options.sessionActionMode === 'archived') {
       menu.addItem((menuItem) => menuItem
-        .setTitle('Restore')
+        .setTitle(t('ui.restore'))
         .onClick(() => {
           void this.runHistoryAction(
             () => options.onSetConversationArchived?.(conversationId, false),
-            'Failed to restore session',
+            t('ui.failedToRestoreSession'),
           );
         }));
       menu.addItem((menuItem) => menuItem
-        .setTitle('Delete')
+        .setTitle(t('common.delete'))
         .onClick(() => {
           void this.runHistoryAction(
             () => this.deleteHistoryConversation(conversationId, options),
-            'Failed to delete conversation',
+            t('ui.failedToDeleteConversation'),
           );
         }));
       menu.showAtMouseEvent(event);
@@ -2093,30 +2093,30 @@ export class ConversationController {
     if (options.onSetConversationPinned) {
       const isPinned = conversation.isPinned === true;
       menu.addItem((menuItem) => menuItem
-        .setTitle(isPinned ? 'Unpin' : 'Pin')
+        .setTitle(isPinned ? t('ui.unpin') : t('ui.pin'))
         .onClick(() => {
           void this.runHistoryAction(
             () => options.onSetConversationPinned?.(conversationId, !isPinned),
-            isPinned ? 'Failed to unpin session' : 'Failed to pin session',
+            isPinned ? t('ui.failedToUnpinSession') : t('ui.failedToPinSession'),
           );
         }));
     }
 
     if (options.sessionActionMode === 'active') {
       menu.addItem((menuItem) => menuItem
-        .setTitle('Rename')
+        .setTitle(t('ui.rename'))
         .onClick(() => {
           this.showRenameEditor(item, conversationId, title, options);
         }));
       menu.addItem((menuItem) => {
         menuItem
-          .setTitle('Archive')
+          .setTitle(t('ui.archive'))
           .setDisabled(isRunning);
         if (!isRunning) {
           menuItem.onClick(() => {
             void this.runHistoryAction(
               () => options.onSetConversationArchived?.(conversationId, true),
-              'Failed to archive session',
+              t('ui.failedToArchiveSession'),
             );
           });
         }
@@ -2126,16 +2126,16 @@ export class ConversationController {
     }
 
     menu.addItem((menuItem) => menuItem
-      .setTitle('Rename')
+      .setTitle(t('ui.rename'))
       .onClick(() => {
         this.showRenameEditor(item, conversationId, title, options);
       }));
     menu.addItem((menuItem) => menuItem
-      .setTitle('Delete')
+      .setTitle(t('common.delete'))
       .onClick(() => {
         void this.runHistoryAction(
           () => this.deleteHistoryConversation(conversationId, options),
-          'Failed to delete conversation',
+          t('ui.failedToDeleteConversation'),
         );
       }));
 
@@ -2216,7 +2216,7 @@ export class ConversationController {
         await this.deps.plugin.renameConversation(convId, newTitle);
         options.onRerender();
       } catch {
-        new Notice('Failed to rename conversation');
+        new Notice(t('ui.failedToRenameConversation'));
       } finally {
         isFinishing = false;
       }
@@ -2226,7 +2226,7 @@ export class ConversationController {
       if (this.activeInlineRename?.input === input) {
         this.activeInlineRename = null;
       }
-      runConversationAction(finishRename, 'Failed to rename conversation');
+      runConversationAction(finishRename, t('ui.failedToRenameConversation'));
     });
     input.addEventListener('keydown', (e) => {
       // Check !e.isComposing for IME support (Chinese, Japanese, Korean, etc.)
@@ -2257,37 +2257,37 @@ export class ConversationController {
 
     // Day-specific greetings (some personalized, some universal)
     const dayGreetings: Record<number, string[]> = {
-      0: [personalize('Happy Sunday'), 'Sunday session?', 'Welcome to the weekend'],
-      1: [personalize('Happy Monday'), personalize('Back at it', 'Back at it!')],
-      2: [personalize('Happy Tuesday')],
-      3: [personalize('Happy Wednesday')],
-      4: [personalize('Happy Thursday')],
-      5: [personalize('Happy Friday'), personalize('That Friday feeling')],
-      6: [personalize('Happy Saturday', 'Happy Saturday!'), personalize('Welcome to the weekend')],
+      0: [personalize(t('ui.happySunday')), t('ui.sundaySession'), t('ui.welcomeToTheWeekend')],
+      1: [personalize(t('ui.happyMonday')), personalize(t('ui.backAtIt'), t('ui.backAtIt2'))],
+      2: [personalize(t('ui.happyTuesday'))],
+      3: [personalize(t('ui.happyWednesday'))],
+      4: [personalize(t('ui.happyThursday'))],
+      5: [personalize(t('ui.happyFriday')), personalize(t('ui.thatFridayFeeling'))],
+      6: [personalize(t('ui.happySaturday'), t('ui.happySaturday2')), personalize(t('ui.welcomeToTheWeekend'))],
     };
 
     // Time-specific greetings
     const getTimeGreetings = (): string[] => {
       if (hour >= 5 && hour < 12) {
-        return [personalize('Good morning'), 'Coffee and Claudian time?'];
+        return [personalize(t('ui.goodMorning')), t('ui.coffeeAndClaudianTime')];
       } else if (hour >= 12 && hour < 18) {
-        return [personalize('Good afternoon'), personalize('Hey there'), personalize("How's it going") + '?'];
+        return [personalize(t('ui.goodAfternoon')), personalize(t('ui.heyThere')), personalize(t('ui.howsItGoing')) + '?'];
       } else if (hour >= 18 && hour < 22) {
-        return [personalize('Good evening'), personalize('Evening'), personalize('How was your day') + '?'];
+        return [personalize(t('ui.goodEvening')), personalize(t('ui.evening')), personalize(t('ui.howWasYourDay')) + '?'];
       } else {
-        return ['Hello, night owl', personalize('Evening')];
+        return [t('ui.helloNightOwl'), personalize(t('ui.evening'))];
       }
     };
 
     // General greetings
     const generalGreetings = [
-      personalize('Hey there'),
-      name ? `Hi ${name}, how are you?` : 'Hi, how are you?',
-      personalize("How's it going") + '?',
-      personalize('Welcome back') + '!',
-      personalize("What's new") + '?',
-      ...(name ? [`${name} returns!`] : []),
-      'You are absolutely right!',
+      personalize(t('ui.heyThere')),
+      name ? t('ui.helloName', { name }) : t('ui.hiHowAreYou'),
+      personalize(t('ui.howsItGoing')) + '?',
+      personalize(t('ui.welcomeBack')) + '!',
+      personalize(t('ui.whatsNew')) + '?',
+      ...(name ? [t('ui.nameReturns', { name })] : []),
+      t('ui.youAreAbsolutelyRight'),
     ];
 
     // Combine day + time + general greetings, pick randomly
@@ -2401,13 +2401,13 @@ export class ConversationController {
     const now = new Date();
 
     if (date.toDateString() === now.toDateString()) {
-      return date.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit', hour12: false });
+      return date.toLocaleTimeString(getLocale(), { hour: '2-digit', minute: '2-digit', hour12: false });
     }
-    return date.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
+    return date.toLocaleDateString(getLocale(), { month: 'short', day: 'numeric' });
   }
 
   formatMetadataDate(timestamp: number): string {
-    return new Date(timestamp).toLocaleDateString(undefined, {
+    return new Date(timestamp).toLocaleDateString(getLocale(), {
       day: 'numeric',
       month: 'short',
       year: 'numeric',
@@ -2415,7 +2415,7 @@ export class ConversationController {
   }
 
   formatMetadataDateTime(timestamp: number): string {
-    return new Date(timestamp).toLocaleString(undefined, {
+    return new Date(timestamp).toLocaleString(getLocale(), {
       day: 'numeric',
       hour: '2-digit',
       minute: '2-digit',
diff --git a/src/features/chat/controllers/InputController.ts b/src/features/chat/controllers/InputController.ts
index 2fa9cba..26507ba 100644
--- a/src/features/chat/controllers/InputController.ts
+++ b/src/features/chat/controllers/InputController.ts
@@ -479,7 +479,7 @@ export class InputController {
     this.awaitingProviderAssistantStart = true;
 
     streamController.showThinkingIndicator(
-      isCompact ? 'Compacting...' : undefined,
+      isCompact ? t('ui.compacting') : undefined,
       isCompact ? 'claudian-thinking--compact' : undefined,
     );
     state.responseStartTime = performance.now();
@@ -501,7 +501,7 @@ export class InputController {
     if (this.deps.ensureExecutionInitialized) {
       const ready = await this.deps.ensureExecutionInitialized();
       if (!ready) {
-        new Notice('Failed to initialize agent execution. Please try again.');
+        new Notice(t('ui.failedToInitializeAgentExecutionPleaseTryAgain'));
         this.restoreMessageToInput(this.createQueuedMessage(displayContent, admittedTurnRequest));
         this.rollbackFailedTurn(messagesBeforeTurn, hadPendingConversationSave);
         this.activeStreamingAssistantMessage = null;
@@ -513,7 +513,7 @@ export class InputController {
 
     const coordinator = this.getExecutionCoordinator();
     if (!coordinator) {
-      new Notice('Agent execution is not available. Please reload the plugin.');
+      new Notice(t('ui.agentExecutionIsNotAvailablePleaseReloadThePlugin'));
       this.restoreMessageToInput(this.createQueuedMessage(displayContent, admittedTurnRequest));
       this.rollbackFailedTurn(messagesBeforeTurn, hadPendingConversationSave);
       this.activeStreamingAssistantMessage = null;
@@ -571,12 +571,12 @@ export class InputController {
           this.finishAcceptedMissingSession(streamGeneration);
         }
         const notice = resolution === 'deleted'
-            ? 'The provider session no longer exists. Its Claudian record was removed; send again to start a new session.'
+            ? t('ui.theProviderSessionNoLongerExistsItsClaudianRecordWasRemovedSendAgainToStartANewSession')
             : resolution === 'reset'
-              ? 'The provider session no longer exists. Claudian preserved the recoverable history; send again to rebuild the session.'
+              ? t('ui.theProviderSessionNoLongerExistsClaudianPreservedTheRecoverableHistorySendAgainToRebuildTheSession')
               : resolution === 'preserved'
-                ? 'The provider session no longer exists. Claudian preserved its record because the remaining history could not be verified.'
-                : 'The provider session no longer exists. Send again to start a new session.';
+                ? t('ui.theProviderSessionNoLongerExistsClaudianPreservedItsRecordBecauseTheRemainingHistoryCouldNotBeVerified')
+                : t('ui.theProviderSessionNoLongerExistsSendAgainToStartANewSession');
         new Notice(notice);
         wasInvalidated = true;
       } else if (result.status === 'error' && result.error) {
@@ -591,12 +591,12 @@ export class InputController {
         );
         this.rollbackFailedTurn(messagesBeforeTurn, hadPendingConversationSave);
         didRollbackUnsentTurn = true;
-        new Notice('Message was not sent. Please try again.');
+        new Notice(t('ui.messageWasNotSentPleaseTryAgain'));
         this.reportDeferredReviewableSettlement();
       } else {
         hadExecutionError = true;
         shouldReportReviewableSettlement = true;
-        const errorMsg = error instanceof Error ? error.message : 'Unknown error';
+        const errorMsg = error instanceof Error ? error.message : t('ui.unknownError');
         await streamController.appendText(`\n\n**Error:** ${errorMsg}`);
         currentReviewableSettlementReporter =
           this.deps.captureReviewableSettlement?.('error') ?? null;
@@ -633,14 +633,14 @@ export class InputController {
               : 0;
             if (durationSeconds > 0) {
               const flavorWord =
-                COMPLETION_FLAVOR_WORDS[Math.floor(Math.random() * COMPLETION_FLAVOR_WORDS.length)];
+                t(COMPLETION_FLAVOR_WORDS[Math.floor(Math.random() * COMPLETION_FLAVOR_WORDS.length)]);
               finalAssistantMsg.durationSeconds = durationSeconds;
               finalAssistantMsg.durationFlavorWord = flavorWord;
               // Add footer to live message in DOM
               if (state.currentContentEl) {
                 const footerEl = state.currentContentEl.createDiv({ cls: 'claudian-response-footer' });
                 footerEl.createSpan({
-                  text: `* ${flavorWord} for ${formatDurationMmSs(durationSeconds)}`,
+                  text: t('ui.completedIn', { word: flavorWord, duration: formatDurationMmSs(durationSeconds) }),
                   cls: 'claudian-baked-duration',
                 });
               }
@@ -796,7 +796,7 @@ export class InputController {
       const isPendingSteerOnly = !state.queuedMessage && !!visiblePendingSteer;
       indicatorEl.createSpan({
         cls: 'claudian-queue-indicator-text',
-        text: `${isPendingSteerOnly ? '⌙ Steering: ' : '⌙ Queued: '}${this.getQueuedMessageDisplay(visibleQueuedMessage)}`,
+        text: `${isPendingSteerOnly ? t('ui.steering') : t('ui.queued')}${this.getQueuedMessageDisplay(visibleQueuedMessage)}`,
       });
 
       if (state.queuedMessage) {
@@ -806,8 +806,8 @@ export class InputController {
           const steerButton = actionsEl.createEl('button', {
             cls: 'claudian-queue-indicator-action',
             text: pendingSteer?.providerDisposition === 'awaiting-result'
-              ? 'Steering...'
-              : 'Steer Now',
+              ? t('ui.steering2')
+              : t('ui.steerNow'),
           });
           steerButton.setAttribute('type', 'button');
           if (pendingSteer?.providerDisposition === 'awaiting-result') {
@@ -823,7 +823,7 @@ export class InputController {
         const editButton = this.createQueueIconButton(
           actionsEl,
           'pencil',
-          'Edit queued message',
+          t('ui.editQueuedMessage'),
         );
         editButton.addEventListener('click', (event) => {
           event.stopPropagation();
@@ -833,7 +833,7 @@ export class InputController {
         const discardButton = this.createQueueIconButton(
           actionsEl,
           'trash-2',
-          'Discard queued message',
+          t('ui.discardQueuedMessage'),
         );
         discardButton.addEventListener('click', (event) => {
           event.stopPropagation();
@@ -1429,7 +1429,7 @@ export class InputController {
       if (pending.providerDisposition === 'accepted-awaiting-correlation') return;
       if (error instanceof ChatExecutionPreHandoffError) {
         this.restoreDefinitelyUnsentSteer(pending);
-        new Notice('Failed to steer the queued message. It is still available.');
+        new Notice(t('ui.failedToSteerTheQueuedMessageItIsStillAvailable'));
         return;
       }
 
@@ -1439,7 +1439,7 @@ export class InputController {
         this.releasePendingSteer(pending);
       }
       new Notice(
-        'Steer delivery could not be confirmed. The message was not requeued to avoid sending it twice.',
+        t('ui.steerDeliveryCouldNotBeConfirmedTheMessageWasNotRequeuedToAvoidSendingItTwice'),
       );
     }
   }
@@ -1794,7 +1794,7 @@ export class InputController {
     if (this.deps.ensureExecutionInitialized) {
       const ready = await this.deps.ensureExecutionInitialized();
       if (!ready) {
-        new Notice('Failed to initialize instruction refinement. Please try again.');
+        new Notice(t('ui.failedToInitializeInstructionRefinementPleaseTryAgain'));
         return;
       }
     }
@@ -1821,7 +1821,7 @@ export class InputController {
                 );
               });
 
-              new Notice('Instruction added to custom system prompt');
+              new Notice(t('ui.instructionAddedToCustomSystemPrompt'));
               instructionModeManager?.clear();
             })();
           },
@@ -1842,8 +1842,8 @@ export class InputController {
               if (result.error === 'Cancelled') {
                 return;
               }
-              new Notice(result.error || 'Failed to process response');
-              modal?.showError(result.error || 'Failed to process response');
+              new Notice(result.error || t('ui.failedToProcessResponse'));
+              modal?.showError(result.error || t('ui.failedToProcessResponse'));
               return;
             }
 
@@ -1873,8 +1873,8 @@ export class InputController {
           instructionModeManager?.clear();
           return;
         }
-        new Notice(result.error || 'Failed to refine instruction');
-        modal.showError(result.error || 'Failed to refine instruction');
+        new Notice(result.error || t('ui.failedToRefineInstruction'));
+        modal.showError(result.error || t('ui.failedToRefineInstruction'));
         instructionModeManager?.clear();
         return;
       }
@@ -1884,12 +1884,12 @@ export class InputController {
       } else if (result.refinedInstruction) {
         modal.showConfirmation(result.refinedInstruction);
       } else {
-        new Notice('No instruction received');
-        modal.showError('No instruction received');
+        new Notice(t('ui.noInstructionReceived'));
+        modal.showError(t('ui.noInstructionReceived'));
         instructionModeManager?.clear();
       }
     } catch (error) {
-      const errorMsg = error instanceof Error ? error.message : 'Unknown error';
+      const errorMsg = error instanceof Error ? error.message : t('ui.unknownError');
       new Notice(`Error: ${errorMsg}`);
       modal?.showError(errorMsg);
       instructionModeManager?.clear();
@@ -1936,7 +1936,7 @@ export class InputController {
       text: description,
       cls: 'claudian-ask-approval-desc',
     });
-    descriptionEl.setAttribute('aria-label', `${toolName} approval details`);
+    descriptionEl.setAttribute('aria-label', t('ui.approvalDetailsFormat', { p0: toolName }));
     descriptionEl.setAttribute('role', 'region');
     descriptionEl.setAttribute('tabindex', '0');
     descriptionEl.addEventListener('keydown', (event) => {
@@ -1957,14 +1957,16 @@ export class InputController {
         optionDecisionMap.set(value, option.decision);
       }
       return {
-        label: option.label,
+        label: option.label === 'Deny' ? t('ui.deny')
+          : option.label === 'Allow once' ? t('ui.allowOnce')
+            : option.label === 'Always allow' ? t('ui.alwaysAllow') : option.label,
         description: option.description ?? '',
         value,
       };
     });
     const input = {
       questions: [{
-        question: 'Allow this action?',
+        question: t('ui.allowThisAction'),
         options: questionOptions,
         isOther: false,
         isSecret: false,
@@ -1977,14 +1979,14 @@ export class InputController {
       input,
       (inline) => { this.pendingApprovalInline = inline; },
       undefined,
-      { title: 'Permission required', headerEl, showCustomInput: false, immediateSelect: true },
+      { title: t('ui.permissionRequired'), headerEl, showCustomInput: false, immediateSelect: true },
     );
 
     if (!result) return 'cancel';
     const selected = Object.values(result)[0];
     const selectedValue = Array.isArray(selected) ? selected[0] : selected;
     if (typeof selectedValue !== 'string') {
-      new Notice(`Unexpected approval selection: "${String(selectedValue)}"`);
+      new Notice(t('ui.unexpectedApprovalSelectionFormat', { p0: String(selectedValue) }));
       return 'cancel';
     }
 
@@ -2212,7 +2214,7 @@ export class InputController {
     const capabilities = this.getActiveCapabilities();
 
     if (!isBuiltInCommandSupported(command, capabilities)) {
-      new Notice(`/${command.name} is not supported by this provider.`);
+      new Notice(t('ui.isNotSupportedByThisProviderFormat', { p0: command.name }));
       return;
     }
 
@@ -2233,12 +2235,12 @@ export class InputController {
       case 'add-dir': {
         const externalContextSelector = this.deps.getExternalContextSelector();
         if (!externalContextSelector) {
-          new Notice('External context selector not available.');
+          new Notice(t('ui.externalContextSelectorNotAvailable'));
           return;
         }
         const result = externalContextSelector.addExternalContext(args);
         if (result.success) {
-          new Notice(`Added external context: ${result.normalizedPath}`);
+          new Notice(t('ui.addedExternalContextFormat', { p0: result.normalizedPath }));
         } else {
           new Notice(result.error);
         }
@@ -2249,11 +2251,11 @@ export class InputController {
         break;
       case 'fork': {
         if (!this.getActiveCapabilities().supportsFork) {
-          new Notice('Fork is not supported by this provider.');
+          new Notice(t('ui.forkIsNotSupportedByThisProvider'));
           return;
         }
         if (!this.deps.onForkAll) {
-          new Notice('Fork not available.');
+          new Notice(t('ui.forkNotAvailable'));
           return;
         }
         await this.deps.onForkAll();
@@ -2263,17 +2265,17 @@ export class InputController {
         try {
           const toggled = await this.deps.toggleFastMode?.() ?? false;
           if (!toggled) {
-            new Notice('Fast mode is not available for this model.');
+            new Notice(t('ui.fastModeIsNotAvailableForThisModel'));
           }
         } catch {
-          new Notice('Failed to toggle fast mode.');
+          new Notice(t('ui.failedToToggleFastMode'));
         }
         break;
       }
       case 'instruction': {
         const manager = this.deps.getInstructionModeManager();
         if (!manager?.enter()) {
-          new Notice('Instruction mode is not available.');
+          new Notice(t('ui.instructionModeIsNotAvailable'));
         }
         break;
       }
@@ -2282,7 +2284,7 @@ export class InputController {
         const unknownAction = typeof (command as { action?: unknown }).action === 'string'
           ? (command as { action: string }).action
           : 'unknown';
-        new Notice(`Unknown command: ${unknownAction}`);
+        new Notice(t('ui.unknownCommandFormat', { p0: unknownAction }));
         break;
       }
     }
@@ -2316,7 +2318,7 @@ export class InputController {
 
     const conversations = plugin.getConversationList();
     if (conversations.length === 0) {
-      new Notice('No conversations to resume');
+      new Notice(t('ui.noConversationsToResume'));
       return;
     }
 
@@ -2333,7 +2335,7 @@ export class InputController {
           this.destroyResumeDropdown();
           openConversation(id).catch((err: unknown) => {
             const msg = err instanceof Error ? err.message : String(err);
-            new Notice(`Failed to open conversation: ${msg}`);
+            new Notice(t('ui.failedToOpenConversationFormat', { p0: msg }));
           });
         },
         onDismiss: () => {
diff --git a/src/features/chat/controllers/StreamController.ts b/src/features/chat/controllers/StreamController.ts
index b0aa07b..e4ab37d 100644
--- a/src/features/chat/controllers/StreamController.ts
+++ b/src/features/chat/controllers/StreamController.ts
@@ -1,5 +1,7 @@
 import { TFile } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import type {
   ProviderBackgroundOutputEvent,
   ProviderExecutionEvent,
@@ -271,7 +273,7 @@ export class StreamController {
 
       case 'notice':
         this.flushPendingTools();
-        await this.appendText(`\n\n⚠️ **${chunk.level === 'warning' ? 'Blocked' : 'Notice'}:** ${chunk.content}`);
+        await this.appendText(`\n\n⚠️ **${chunk.level === 'warning' ? t('ui.blocked') : t('ui.notice')}:** ${chunk.content}`);
         break;
 
       case 'error':
@@ -702,7 +704,7 @@ export class StreamController {
     if (subagentInfo.status === 'completed' || subagentInfo.status === 'error') {
       this.finalizeProviderSubagentState(
         spawnId,
-        subagentInfo.result || (subagentInfo.status === 'error' ? 'Error' : 'DONE'),
+        subagentInfo.result || (subagentInfo.status === 'error' ? 'Error' : t('ui.done')),
         subagentInfo.status === 'error',
       );
     }
@@ -817,7 +819,7 @@ export class StreamController {
       if (subagentInfo.status === 'completed' || subagentInfo.status === 'error') {
         this.finalizeProviderSubagentState(
           chunk.id,
-          subagentInfo.result || (subagentInfo.status === 'error' ? 'Error' : 'DONE'),
+          subagentInfo.result || (subagentInfo.status === 'error' ? 'Error' : t('ui.done')),
           subagentInfo.status === 'error',
         );
       }
@@ -840,7 +842,7 @@ export class StreamController {
         if (subagentInfo.status === 'completed' || subagentInfo.status === 'error') {
           this.finalizeProviderSubagentState(
             spawnId,
-            subagentInfo.result || (subagentInfo.status === 'error' ? 'Error' : 'DONE'),
+            subagentInfo.result || (subagentInfo.status === 'error' ? 'Error' : t('ui.done')),
             subagentInfo.status === 'error',
           );
         }
@@ -859,7 +861,7 @@ export class StreamController {
         this.updateProviderSubagentState(spawnId, subagentInfo);
         this.finalizeProviderSubagentState(
           spawnId,
-          subagentInfo.result || 'Task cancelled',
+          subagentInfo.result || t('ui.taskCancelled'),
           true,
         );
       }
@@ -1761,7 +1763,7 @@ export class StreamController {
         ? `claudian-thinking ${overrideCls}`
         : 'claudian-thinking';
       state.thinkingEl = state.currentContentEl.createDiv({ cls });
-      const text = overrideText || FLAVOR_TEXTS[Math.floor(Math.random() * FLAVOR_TEXTS.length)];
+      const text = overrideText || t(FLAVOR_TEXTS[Math.floor(Math.random() * FLAVOR_TEXTS.length)]);
       state.thinkingEl.createSpan({ text });
 
       // Create timer span with initial value
@@ -1776,7 +1778,7 @@ export class StreamController {
           return;
         }
         const elapsedSeconds = Math.floor((performance.now() - state.responseStartTime) / 1000);
-        timerSpan.setText(` (esc to interrupt · ${formatDurationMmSs(elapsedSeconds)})`);
+        timerSpan.setText(t('ui.escToInterruptFormat', { p0: formatDurationMmSs(elapsedSeconds) }));
       };
       updateTimer(); // Initial update
 
@@ -1818,7 +1820,7 @@ export class StreamController {
     if (!state.currentContentEl) return;
     this.hideThinkingIndicator();
     const el = state.currentContentEl.createDiv({ cls: 'claudian-compact-boundary' });
-    el.createSpan({ cls: 'claudian-compact-boundary-label', text: 'Conversation compacted' });
+    el.createSpan({ cls: 'claudian-compact-boundary-label', text: t('ui.conversationCompacted') });
   }
 
   // ============================================
diff --git a/src/features/chat/execution/ChatExecutionCoordinator.ts b/src/features/chat/execution/ChatExecutionCoordinator.ts
index 26ca1e0..d9da44f 100644
--- a/src/features/chat/execution/ChatExecutionCoordinator.ts
+++ b/src/features/chat/execution/ChatExecutionCoordinator.ts
@@ -33,6 +33,7 @@ import type {
   ImageAttachment,
   ProviderId,
 } from '@/core/types';
+import { t } from '@/i18n/i18n';
 import { toError } from '@/utils/error';
 
 import {
@@ -573,7 +574,7 @@ export class ChatExecutionCoordinator {
     await this.prepare();
     const session = this.requireCurrentSessionBinding().session;
     if (!isRewindableExecutionSession(session)) {
-      return { canRewind: false, error: 'Rewind is not supported by this provider.' };
+      return { canRewind: false, error: t('ui.rewindIsNotSupportedByThisProvider') };
     }
     return session.previewRewind(userMessageId, assistantMessageId, mode);
   }
@@ -612,7 +613,7 @@ export class ChatExecutionCoordinator {
     await this.prepare();
     const binding = this.requireCurrentSessionBinding();
     if (!isRewindableExecutionSession(binding.session)) {
-      return { canRewind: false, error: 'Rewind is not supported by this provider.' };
+      return { canRewind: false, error: t('ui.rewindIsNotSupportedByThisProvider') };
     }
     const result = await binding.session.rewind(
       userMessageId,
diff --git a/src/features/chat/execution/WarmExecutionPool.ts b/src/features/chat/execution/WarmExecutionPool.ts
index d652f11..ab57f1b 100644
--- a/src/features/chat/execution/WarmExecutionPool.ts
+++ b/src/features/chat/execution/WarmExecutionPool.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 export const DEFAULT_MAX_WARM_AGENT_PROCESSES = 5;
 export const MIN_WARM_AGENT_PROCESSES = 5;
 export const MAX_WARM_AGENT_PROCESSES = 10;
@@ -26,8 +28,8 @@ interface WarmExecutionEntry {
 export class WarmExecutionCapacityError extends Error {
   constructor(readonly limit: number) {
     super(
-      `The concurrent running session limit (${limit}) is currently in use. `
-      + 'Finish or stop a session before starting another.',
+      t('ui.theConcurrentRunningSessionLimitIsCurrentlyInUseFormat', { p0: limit })
+      + t('ui.finishOrStopASessionBeforeStartingAnother'),
     );
     this.name = 'WarmExecutionCapacityError';
   }
diff --git a/src/features/chat/fileMenu.ts b/src/features/chat/fileMenu.ts
index 50b6603..bf41a91 100644
--- a/src/features/chat/fileMenu.ts
+++ b/src/features/chat/fileMenu.ts
@@ -1,6 +1,8 @@
 import type { App, EventRef } from 'obsidian';
 import { Notice, TFile } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import { formatVaultFileMention } from '../../shared/mention/formatMention';
 
 interface FileMenuViewHost {
@@ -19,11 +21,11 @@ export async function addFileToClaudian(host: FileMenuHost, file: TFile): Promis
     await host.activateView();
     const appended = host.getView()?.appendToActiveInput(formatVaultFileMention(file.path)) ?? false;
     if (!appended) {
-      new Notice('Claudian chat is not ready.');
+      new Notice(t('ui.claudianChatIsNotReady'));
     }
     return appended;
   } catch {
-    new Notice('Failed to add file to Claudian.');
+    new Notice(t('ui.failedToAddFileToClaudian'));
     return false;
   }
 }
@@ -34,7 +36,7 @@ export function registerFileMenu(host: FileMenuHost): void {
       if (!(file instanceof TFile)) return;
 
       menu.addItem((item) => item
-        .setTitle('Add to Claudian')
+        .setTitle(t('ui.addToClaudian'))
         .setIcon('message-square-plus')
         .onClick(() => addFileToClaudian(host, file)));
     }),
diff --git a/src/features/chat/linked-content/LinkedContentChip.ts b/src/features/chat/linked-content/LinkedContentChip.ts
index 1afa69b..7867e7e 100644
--- a/src/features/chat/linked-content/LinkedContentChip.ts
+++ b/src/features/chat/linked-content/LinkedContentChip.ts
@@ -1,4 +1,5 @@
 import type { ComposerContextTray } from '@/features/chat/ui/ComposerContextTray';
+import { t } from '@/i18n/i18n';
 
 import type { LinkedContentPresentation } from './LinkedContentPresentation';
 
@@ -14,15 +15,15 @@ export class LinkedContentChip {
       this.contextTray.clearItems('linked-content');
       return;
     }
-    const missingLabel = content.missing ? `${content.label} · Missing content` : content.label;
+    const missingLabel = content.missing ? t('ui.missingContentFormat', { p0: content.label }) : content.label;
     this.contextTray.setItems('linked-content', [{
       id: content.path,
       kind: 'content',
       label: missingLabel,
       icon: content.icon,
       ariaLabel: content.missing
-        ? `Linked content: ${content.path}. Missing content`
-        : `Linked content: ${content.path}`,
+        ? t('ui.linkedContentMissingContentFormat', { p0: content.path })
+        : t('ui.linkedContentFormat', { p0: content.path }),
       ...(content.missing ? { status: 'missing' as const } : {}),
       onActivate: this.onActivate,
       ...(removable ? { onRemove: this.onRemove } : {}),
diff --git a/src/features/chat/linked-content/LinkedContentController.ts b/src/features/chat/linked-content/LinkedContentController.ts
index e30e58e..3bb2163 100644
--- a/src/features/chat/linked-content/LinkedContentController.ts
+++ b/src/features/chat/linked-content/LinkedContentController.ts
@@ -6,6 +6,7 @@ import {
   normalizeLinkedContentPath,
 } from '@/core/path/LinkedContentPath';
 import type { ComposerContextTray } from '@/features/chat/ui/ComposerContextTray';
+import { t } from '@/i18n/i18n';
 import { revealWorkspaceLeaf } from '@/utils/obsidianCompat';
 
 import { LinkedContentChip } from './LinkedContentChip';
@@ -268,7 +269,7 @@ export class LinkedContentController {
     if (this.destroyed || !this.path) return;
     const content = deriveLinkedContentPresentation(this.app, this.path);
     if (content.missing || !content.target) {
-      new Notice(`Linked content is missing: ${this.path}`);
+      new Notice(t('ui.linkedContentIsMissingFormat', { p0: this.path }));
       return;
     }
     if (content.target instanceof TFile) {
@@ -276,7 +277,7 @@ export class LinkedContentController {
         await this.revealFile(content.target);
       } catch (error) {
         new Notice(
-          `Failed to open Linked content: ${error instanceof Error ? error.message : String(error)}`,
+          t('ui.failedToOpenLinkedContentFormat', { p0: error instanceof Error ? error.message : String(error) }),
         );
       }
       return;
@@ -373,7 +374,7 @@ export class LinkedContentController {
       mode: snapshot.mode,
       path: snapshot.path,
       label: presentation?.missing
-        ? `${presentation.label} · Missing content`
+        ? t('ui.missingContentFormat', { p0: presentation.label })
         : presentation?.label ?? null,
     });
   }
diff --git a/src/features/chat/linked-content/LinkedContentPickerSource.ts b/src/features/chat/linked-content/LinkedContentPickerSource.ts
index 1a58b16..915b4de 100644
--- a/src/features/chat/linked-content/LinkedContentPickerSource.ts
+++ b/src/features/chat/linked-content/LinkedContentPickerSource.ts
@@ -1,6 +1,7 @@
 import type { TFile } from 'obsidian';
 
 import { normalizeLinkedContentPath } from '@/core/path/LinkedContentPath';
+import { t } from '@/i18n/i18n';
 
 export type LinkedContentPickerItemKind = 'none' | 'file' | 'folder';
 
@@ -35,8 +36,8 @@ export class LinkedContentPickerSource {
         id: 'none',
         kind: 'none',
         path: null,
-        label: 'None',
-        detail: 'No Linked content',
+        label: t('ui.none'),
+        detail: t('ui.noLinkedContent'),
         icon: 'circle-slash',
       },
     ];
diff --git a/src/features/chat/linked-content/LinkedContentSelector.ts b/src/features/chat/linked-content/LinkedContentSelector.ts
index 825ce01..c479ef8 100644
--- a/src/features/chat/linked-content/LinkedContentSelector.ts
+++ b/src/features/chat/linked-content/LinkedContentSelector.ts
@@ -1,5 +1,7 @@
 import { setIcon } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import type { LinkedContentPickerItem } from './LinkedContentPickerSource';
 
 export interface LinkedContentSelectorState {
@@ -58,7 +60,7 @@ export class LinkedContentSelector {
     setIcon(iconEl, 'link');
     const prefixEl = this.selectorRow.createSpan({
       cls: 'claudian-linked-content-selector-prefix',
-      text: 'Linked content:',
+      text: t('ui.linkedContent'),
     });
     prefixEl.setAttribute('id', this.prefixId);
     this.selectorButton = this.selectorRow.createEl('button', {
@@ -74,7 +76,7 @@ export class LinkedContentSelector {
 
   private updateSelectorButton(): void {
     if (!this.selectorButton || !this.state) return;
-    const value = this.state.label ?? 'None';
+    const value = this.state.label ?? t('ui.none');
     this.selectorButton.setText(value);
   }
 
@@ -186,12 +188,12 @@ export class LinkedContentSelector {
     this.showPickerResults();
     const listEl = this.pickerEl.createDiv({ cls: 'claudian-linked-content-picker-list' });
     listEl.setAttribute('role', 'listbox');
-    listEl.setAttribute('aria-label', 'Linked content choices');
+    listEl.setAttribute('aria-label', t('ui.linkedContentChoices'));
 
     if (this.filteredItems.length === 0) {
       listEl.createDiv({
         cls: 'claudian-composer-dropdown-item is-empty claudian-linked-content-picker-empty',
-        text: 'No matching files or folders',
+        text: t('ui.noMatchingFilesOrFolders'),
       });
       return;
     }
@@ -251,7 +253,7 @@ export class LinkedContentSelector {
 
   private sizeSearchInputToSelector(selectorRect: DOMRect): void {
     if (!this.searchInput) return;
-    const fallbackWidth = Math.max(1, Array.from(this.state?.label || 'None').length);
+    const fallbackWidth = Math.max(1, Array.from(this.state?.label || t('ui.none')).length);
     this.searchInput.style.width = selectorRect.width > 0
       ? `${selectorRect.width}px`
       : `${fallbackWidth}ch`;
diff --git a/src/features/chat/rendering/CitationRenderer.ts b/src/features/chat/rendering/CitationRenderer.ts
index f34da22..55277f1 100644
--- a/src/features/chat/rendering/CitationRenderer.ts
+++ b/src/features/chat/rendering/CitationRenderer.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 import type { CitationGroup } from '../../../core/types';
 import { setupCollapsible } from './collapsible';
 
@@ -20,10 +22,10 @@ export function renderCitationGroup(
   headerEl.setAttribute('role', 'button');
 
   headerEl.createSpan({ cls: 'claudian-citations-chevron', text: '\u203a' });
-  headerEl.createSpan({ cls: 'claudian-citations-label', text: 'Memory used' });
+  headerEl.createSpan({ cls: 'claudian-citations-label', text: t('ui.memoryUsed') });
   headerEl.createSpan({
     cls: 'claudian-citations-count',
-    text: `${citations.entries.length} ${citations.entries.length === 1 ? 'source' : 'sources'}`,
+    text: `${citations.entries.length} ${citations.entries.length === 1 ? t('ui.source') : t('ui.sources')}`,
   });
 
   const contentEl = wrapperEl.createDiv({ cls: 'claudian-citations-content' });
@@ -39,7 +41,7 @@ export function renderCitationGroup(
   }
 
   setupCollapsible(wrapperEl, headerEl, contentEl, { isExpanded: false }, {
-    baseAriaLabel: `Memory used, ${citations.entries.length} ${citations.entries.length === 1 ? 'source' : 'sources'}`,
+    baseAriaLabel: t('ui.memoryUsedFormat', { p0: citations.entries.length, p1: citations.entries.length === 1 ? t('ui.source') : t('ui.sources') }),
   });
 
   return wrapperEl;
diff --git a/src/features/chat/rendering/DiffRenderer.ts b/src/features/chat/rendering/DiffRenderer.ts
index 83faeb8..6436e0c 100644
--- a/src/features/chat/rendering/DiffRenderer.ts
+++ b/src/features/chat/rendering/DiffRenderer.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 import type { DiffLine, DiffStats } from '../../../core/types/diff';
 
 export interface DiffHunk {
@@ -95,7 +97,7 @@ export function renderDiffContent(
     }
     const remaining = diffLines.length - NEW_FILE_DISPLAY_CAP;
     const separator = containerEl.createDiv({ cls: 'claudian-diff-separator' });
-    separator.setText(`... ${remaining} more lines`);
+    separator.setText(t('ui.moreLinesFormat', { p0: remaining }));
     return;
   }
 
@@ -104,7 +106,7 @@ export function renderDiffContent(
   if (hunks.length === 0) {
     // No changes
     const noChanges = containerEl.createDiv({ cls: 'claudian-diff-no-changes' });
-    noChanges.setText('No changes');
+    noChanges.setText(t('ui.noChanges'));
     return;
   }
 
diff --git a/src/features/chat/rendering/InlineAskUserQuestion.ts b/src/features/chat/rendering/InlineAskUserQuestion.ts
index d615601..6439dc3 100644
--- a/src/features/chat/rendering/InlineAskUserQuestion.ts
+++ b/src/features/chat/rendering/InlineAskUserQuestion.ts
@@ -1,7 +1,9 @@
+import { t } from '@/i18n/i18n';
+
 import type { AskUserQuestionItem, AskUserQuestionOption } from '../../../core/types/tools';
 
-const HINTS_TEXT = 'Enter to select \u00B7 Tab/Arrow keys to navigate \u00B7 Esc to cancel';
-const HINTS_TEXT_IMMEDIATE = 'Enter to select \u00B7 Arrow keys to navigate \u00B7 Esc to cancel';
+const HINTS_TEXT = () => t('ui.enterToSelectTabArrowKeysToNavigateEscToCancel');
+const HINTS_TEXT_IMMEDIATE = () => t('ui.enterToSelectArrowKeysToNavigateEscToCancel');
 
 export interface InlineAskQuestionConfig {
   title?: string;
@@ -46,7 +48,7 @@ export class InlineAskUserQuestion {
     this.resolveCallback = resolve;
     this.signal = signal;
     this.config = {
-      title: config?.title ?? 'Question',
+      title: config?.title ?? t('ui.question'),
       headerEl: config?.headerEl,
       showCustomInput: config?.showCustomInput ?? true,
       immediateSelect: config?.immediateSelect ?? false,
@@ -165,7 +167,7 @@ export class InlineAskUserQuestion {
     if (typeof obj.value === 'string') return obj.value;
     if (typeof obj.text === 'string') return obj.text;
     if (typeof obj.name === 'string') return obj.name;
-    return 'Option';
+    return t('ui.option');
   }
 
   private stringifyOptionValue(value: unknown): string {
@@ -173,7 +175,7 @@ export class InlineAskUserQuestion {
     if (typeof value === 'number' || typeof value === 'boolean' || typeof value === 'bigint') {
       return `${value}`;
     }
-    return 'Option';
+    return t('ui.option');
   }
 
   private extractValue(obj: Record<string, unknown>, fallback: string): string {
@@ -202,7 +204,7 @@ export class InlineAskUserQuestion {
     const allAnswered = this.questions.every((_, i) => this.isQuestionAnswered(i));
     const submitTab = this.tabBar.createSpan({ cls: 'claudian-ask-tab' });
     submitTab.createSpan({ text: allAnswered ? '\u2713 ' : '', cls: 'claudian-ask-tab-submit-check' });
-    submitTab.createSpan({ text: 'Submit', cls: 'claudian-ask-tab-label' });
+    submitTab.createSpan({ text: t('ui.submit'), cls: 'claudian-ask-tab-label' });
     if (this.activeTabIndex === this.questions.length) submitTab.addClass('is-active');
     submitTab.addEventListener('click', () => this.switchTab(this.questions.length));
     this.tabElements.push(submitTab);
@@ -307,7 +309,7 @@ export class InlineAskUserQuestion {
         value: customText,
       });
       inputEl.setAttribute('type', q.isSecret ? 'password' : 'text');
-      inputEl.setAttribute('placeholder', q.isSecret ? 'Enter secret.' : 'Type something.');
+      inputEl.setAttribute('placeholder', q.isSecret ? t('ui.enterSecret') : t('ui.typeSomething'));
 
       inputEl.addEventListener('input', () => {
         this.customInputs.set(idx, inputEl.value);
@@ -334,14 +336,14 @@ export class InlineAskUserQuestion {
     }
 
     this.contentArea.createDiv({
-      text: this.config.immediateSelect ? HINTS_TEXT_IMMEDIATE : HINTS_TEXT,
+      text: this.config.immediateSelect ? HINTS_TEXT_IMMEDIATE() : HINTS_TEXT(),
       cls: 'claudian-ask-hints',
     });
   }
 
   private renderSubmitTab(): void {
     this.contentArea.createDiv({
-      text: 'Review your answers',
+      text: t('ui.reviewYourAnswers'),
       cls: 'claudian-ask-review-title',
     });
 
@@ -356,14 +358,14 @@ export class InlineAskUserQuestion {
       const bodyEl = pairEl.createDiv({ cls: 'claudian-ask-review-body' });
       bodyEl.createDiv({ text: q.question, cls: 'claudian-ask-review-q-text' });
       bodyEl.createDiv({
-        text: answerText || 'Not answered',
+        text: answerText || t('ui.notAnswered'),
         cls: answerText ? 'claudian-ask-review-a-text' : 'claudian-ask-review-empty',
       });
       pairEl.addEventListener('click', () => this.switchTab(idx));
     }
 
     this.contentArea.createDiv({
-      text: 'Ready to submit your answers?',
+      text: t('ui.readyToSubmitYourAnswers'),
       cls: 'claudian-ask-review-prompt',
     });
 
@@ -375,7 +377,7 @@ export class InlineAskUserQuestion {
     if (!allAnswered) submitRow.addClass('is-disabled');
     submitRow.createSpan({ text: this.focusedItemIndex === 0 ? '\u203A' : '\u00A0', cls: 'claudian-ask-cursor' });
     submitRow.createSpan({ text: '1. ', cls: 'claudian-ask-item-num' });
-    submitRow.createSpan({ text: 'Submit answers', cls: 'claudian-ask-item-label' });
+    submitRow.createSpan({ text: t('ui.submitAnswers'), cls: 'claudian-ask-item-label' });
     submitRow.addEventListener('click', () => {
       this.focusedItemIndex = 0;
       this.updateFocusIndicator();
@@ -387,7 +389,7 @@ export class InlineAskUserQuestion {
     if (this.focusedItemIndex === 1) cancelRow.addClass('is-focused');
     cancelRow.createSpan({ text: this.focusedItemIndex === 1 ? '\u203A' : '\u00A0', cls: 'claudian-ask-cursor' });
     cancelRow.createSpan({ text: '2. ', cls: 'claudian-ask-item-num' });
-    cancelRow.createSpan({ text: 'Cancel', cls: 'claudian-ask-item-label' });
+    cancelRow.createSpan({ text: t('common.cancel'), cls: 'claudian-ask-item-label' });
     cancelRow.addEventListener('click', () => {
       this.focusedItemIndex = 1;
       this.handleResolve(null);
@@ -395,7 +397,7 @@ export class InlineAskUserQuestion {
     this.currentItems.push(cancelRow);
 
     this.contentArea.createDiv({
-      text: HINTS_TEXT,
+      text: HINTS_TEXT(),
       cls: 'claudian-ask-hints',
     });
   }
diff --git a/src/features/chat/rendering/InlineExitPlanMode.ts b/src/features/chat/rendering/InlineExitPlanMode.ts
index 8d47714..f8c534a 100644
--- a/src/features/chat/rendering/InlineExitPlanMode.ts
+++ b/src/features/chat/rendering/InlineExitPlanMode.ts
@@ -1,13 +1,15 @@
 import * as fs from 'fs';
 import * as nodePath from 'path';
 
+import { t } from '@/i18n/i18n';
+
 import type {
   ExitPlanModeDecision,
   ExitPlanModePresentationOptions,
 } from '../../../core/types/tools';
 import type { RenderContentFn } from './MessageRenderer';
 
-const HINTS_TEXT = 'Arrow keys to navigate \u00B7 Enter to select \u00B7 Esc to cancel';
+const HINTS_TEXT = () => t('ui.arrowKeysToNavigateEnterToSelectEscToCancel');
 type ExitPlanModeAction = 'abandon' | 'approve' | 'approve-new-session' | 'feedback';
 
 export class InlineExitPlanMode {
@@ -54,7 +56,7 @@ export class InlineExitPlanMode {
     this.rootEl = this.containerEl.createDiv({ cls: 'claudian-plan-approval-inline' });
 
     const titleEl = this.rootEl.createDiv({ cls: 'claudian-plan-inline-title' });
-    titleEl.setText('Plan complete');
+    titleEl.setText(t('ui.planComplete'));
 
     this.planContent = this.readPlanContent();
     if (this.planContent) {
@@ -67,14 +69,14 @@ export class InlineExitPlanMode {
     } else if (this.planReadError) {
       this.rootEl.createDiv({
         cls: 'claudian-plan-content-preview claudian-plan-read-error',
-        text: `Could not read plan file: ${this.planReadError}. "Approve (new session)" will not include plan details.`,
+        text: t('ui.couldNotReadPlanFileApproveNewSessionWillNotIncludePlanDetailsFormat', { p0: this.planReadError }),
       });
     }
 
     const allowedPrompts = this.input.allowedPrompts as Array<{ tool: string; prompt: string }> | undefined;
     if (allowedPrompts && Array.isArray(allowedPrompts) && allowedPrompts.length > 0) {
       const permEl = this.rootEl.createDiv({ cls: 'claudian-plan-permissions' });
-      permEl.createDiv({ text: 'Requested permissions:', cls: 'claudian-plan-permissions-label' });
+      permEl.createDiv({ text: t('ui.requestedPermissions'), cls: 'claudian-plan-permissions-label' });
       const listEl = permEl.createEl('ul', { cls: 'claudian-plan-permissions-list' });
       for (const perm of allowedPrompts) {
         listEl.createEl('li', { text: perm.prompt });
@@ -87,7 +89,7 @@ export class InlineExitPlanMode {
       const newSessionRow = actionsEl.createDiv({ cls: 'claudian-ask-item' });
       newSessionRow.createSpan({ text: '\u00A0', cls: 'claudian-ask-cursor' });
       newSessionRow.createSpan({ text: `${this.items.length + 1}. `, cls: 'claudian-ask-item-num' });
-      newSessionRow.createSpan({ text: 'Approve (new session)', cls: 'claudian-ask-item-label' });
+      newSessionRow.createSpan({ text: t('ui.approveNewSession'), cls: 'claudian-ask-item-label' });
       const itemIndex = this.items.length;
       newSessionRow.addEventListener('click', () => {
         this.focusedIndex = itemIndex;
@@ -105,7 +107,7 @@ export class InlineExitPlanMode {
     approveRow.createSpan({ text: '\u00A0', cls: 'claudian-ask-cursor' });
     approveRow.createSpan({ text: `${this.items.length + 1}. `, cls: 'claudian-ask-item-num' });
     approveRow.createSpan({
-      text: this.presentation.approveLabel ?? 'Approve (current session)',
+      text: this.presentation.approveLabel ?? t('ui.approveCurrentSession'),
       cls: 'claudian-ask-item-label',
     });
     const approveIndex = this.items.length;
@@ -129,7 +131,7 @@ export class InlineExitPlanMode {
     this.feedbackInput = feedbackRow.createEl('input', {
       type: 'text',
       cls: 'claudian-ask-custom-text',
-      placeholder: 'Enter feedback to continue planning...',
+      placeholder: t('ui.enterFeedbackToContinuePlanning'),
     });
     this.feedbackInput.addEventListener('focus', () => { this.isInputFocused = true; });
     this.feedbackInput.addEventListener('blur', () => { this.isInputFocused = false; });
@@ -145,7 +147,7 @@ export class InlineExitPlanMode {
       const abandonRow = actionsEl.createDiv({ cls: 'claudian-ask-item' });
       abandonRow.createSpan({ text: '\u00A0', cls: 'claudian-ask-cursor' });
       abandonRow.createSpan({ text: `${this.items.length + 1}. `, cls: 'claudian-ask-item-num' });
-      abandonRow.createSpan({ text: 'Abandon', cls: 'claudian-ask-item-label' });
+      abandonRow.createSpan({ text: t('ui.abandon'), cls: 'claudian-ask-item-label' });
       const abandonIndex = this.items.length;
       abandonRow.addEventListener('click', () => {
         this.focusedIndex = abandonIndex;
@@ -160,8 +162,8 @@ export class InlineExitPlanMode {
 
     this.rootEl.createDiv({
       text: this.presentation.dismissOnEscape === false
-        ? 'Arrow keys to navigate \u00B7 Enter to select \u00B7 Shift+Tab to abandon'
-        : HINTS_TEXT,
+        ? t('ui.arrowKeysToNavigateEnterToSelectShiftTabToAbandon')
+        : HINTS_TEXT(),
       cls: 'claudian-ask-hints',
     });
 
@@ -200,7 +202,7 @@ export class InlineExitPlanMode {
       const content = fs.readFileSync(planFilePath, 'utf-8');
       return content.trim() || null;
     } catch (err) {
-      this.planReadError = err instanceof Error ? err.message : 'unknown error';
+      this.planReadError = err instanceof Error ? err.message : t('ui.unknownError2');
       return null;
     }
   }
diff --git a/src/features/chat/rendering/InlinePlanApproval.ts b/src/features/chat/rendering/InlinePlanApproval.ts
index 1c184f3..8b4b8d7 100644
--- a/src/features/chat/rendering/InlinePlanApproval.ts
+++ b/src/features/chat/rendering/InlinePlanApproval.ts
@@ -1,9 +1,11 @@
+import { t } from '@/i18n/i18n';
+
 export type PlanApprovalDecision =
   | { type: 'implement' }
   | { type: 'revise'; text: string }
   | { type: 'cancel' };
 
-const HINTS_TEXT = 'Arrow keys to navigate \u00B7 Enter to select \u00B7 Esc to cancel';
+const HINTS_TEXT = () => t('ui.arrowKeysToNavigateEnterToSelectEscToCancel');
 
 export class InlinePlanApproval {
   private containerEl: HTMLElement;
@@ -29,7 +31,7 @@ export class InlinePlanApproval {
   render(): void {
     this.rootEl = this.containerEl.createDiv({ cls: 'claudian-plan-approval-inline' });
 
-    this.rootEl.createDiv({ cls: 'claudian-plan-inline-title', text: 'Plan complete' });
+    this.rootEl.createDiv({ cls: 'claudian-plan-inline-title', text: t('ui.planComplete') });
 
     const actionsEl = this.rootEl.createDiv({ cls: 'claudian-ask-list' });
 
@@ -38,7 +40,7 @@ export class InlinePlanApproval {
     implementRow.addClass('is-focused');
     implementRow.createSpan({ text: '\u203A', cls: 'claudian-ask-cursor' });
     implementRow.createSpan({ text: '1. ', cls: 'claudian-ask-item-num' });
-    implementRow.createSpan({ text: 'Implement', cls: 'claudian-ask-item-label' });
+    implementRow.createSpan({ text: t('ui.implement'), cls: 'claudian-ask-item-label' });
     implementRow.addEventListener('click', () => {
       this.focusedIndex = 0;
       this.updateFocus();
@@ -53,7 +55,7 @@ export class InlinePlanApproval {
     this.feedbackInput = reviseRow.createEl('input', {
       type: 'text',
       cls: 'claudian-ask-custom-text',
-      placeholder: 'Enter feedback to revise plan...',
+      placeholder: t('ui.enterFeedbackToRevisePlan'),
     });
     this.feedbackInput.addEventListener('focus', () => { this.isInputFocused = true; });
     this.feedbackInput.addEventListener('blur', () => { this.isInputFocused = false; });
@@ -67,7 +69,7 @@ export class InlinePlanApproval {
     const cancelRow = actionsEl.createDiv({ cls: 'claudian-ask-item' });
     cancelRow.createSpan({ text: '\u00A0', cls: 'claudian-ask-cursor' });
     cancelRow.createSpan({ text: '3. ', cls: 'claudian-ask-item-num' });
-    cancelRow.createSpan({ text: 'Cancel', cls: 'claudian-ask-item-label' });
+    cancelRow.createSpan({ text: t('common.cancel'), cls: 'claudian-ask-item-label' });
     cancelRow.addEventListener('click', () => {
       this.focusedIndex = 2;
       this.updateFocus();
@@ -75,7 +77,7 @@ export class InlinePlanApproval {
     });
     this.items.push(cancelRow);
 
-    this.rootEl.createDiv({ text: HINTS_TEXT, cls: 'claudian-ask-hints' });
+    this.rootEl.createDiv({ text: HINTS_TEXT(), cls: 'claudian-ask-hints' });
 
     this.rootEl.setAttribute('tabindex', '0');
     this.rootEl.addEventListener('keydown', this.boundKeyDown);
diff --git a/src/features/chat/rendering/MessageRenderer.ts b/src/features/chat/rendering/MessageRenderer.ts
index b18bda4..8db7411 100644
--- a/src/features/chat/rendering/MessageRenderer.ts
+++ b/src/features/chat/rendering/MessageRenderer.ts
@@ -381,11 +381,11 @@ export class MessageRenderer {
 
   appendInterruptIndicator(contentEl: HTMLElement): void {
     const textEl = contentEl.createDiv({ cls: 'claudian-text-block' });
-    textEl.createSpan({ cls: 'claudian-interrupted', text: 'Interrupted' });
+    textEl.createSpan({ cls: 'claudian-interrupted', text: t('ui.interrupted') });
     textEl.appendText(' ');
     textEl.createSpan({
       cls: 'claudian-interrupted-hint',
-      text: '\u00B7 What should Claudian do instead?',
+      text: t('ui.whatShouldClaudianDoInstead'),
     });
   }
 
@@ -425,7 +425,7 @@ export class MessageRenderer {
           }
         } else if (block.type === 'context_compacted') {
           const boundaryEl = contentEl.createDiv({ cls: 'claudian-compact-boundary' });
-          boundaryEl.createSpan({ cls: 'claudian-compact-boundary-label', text: 'Conversation compacted' });
+          boundaryEl.createSpan({ cls: 'claudian-compact-boundary-label', text: t('ui.conversationCompacted') });
         } else if (block.type === 'subagent') {
           const taskToolCall = msg.toolCalls?.find((toolCall) => {
             if (toolCall.id !== block.subagentId) return false;
@@ -472,7 +472,7 @@ export class MessageRenderer {
       const flavorWord = msg.durationFlavorWord || 'Baked';
       const footerEl = contentEl.createDiv({ cls: 'claudian-response-footer' });
       footerEl.createSpan({
-        text: `* ${flavorWord} for ${formatDurationMmSs(msg.durationSeconds)}`,
+        text: t('ui.forFormat', { p0: flavorWord, p1: formatDurationMmSs(msg.durationSeconds) }),
         cls: 'claudian-baked-duration',
       });
     }
@@ -603,7 +603,7 @@ export class MessageRenderer {
       };
     }
 
-    const description = (toolCall.input?.description as string) || 'Subagent task';
+    const description = (toolCall.input?.description as string) || t('ui.subagentTask');
     const prompt = (toolCall.input?.prompt as string) || '';
     const mode = modeHint ?? (toolCall.input?.run_in_background === true ? 'async' : 'sync');
 
@@ -680,7 +680,7 @@ export class MessageRenderer {
       const imageWrapper = imagesEl.createEl('button', {
         cls: 'claudian-message-image',
         attr: {
-          'aria-label': `Preview ${image.name}`,
+          'aria-label': t('ui.previewFormat', { p0: image.name }),
           type: 'button',
         },
       });
@@ -763,7 +763,7 @@ export class MessageRenderer {
     } catch {
       el.createDiv({
         cls: 'claudian-render-error',
-        text: 'Failed to render message content.',
+        text: t('ui.failedToRenderMessageContent'),
       });
     }
   }
@@ -782,7 +782,7 @@ export class MessageRenderer {
     const copyBtn = textEl.createEl('button', {
       cls: 'claudian-text-copy-btn',
       attr: {
-        'aria-label': 'Copy message',
+        'aria-label': t('ui.copyMessage'),
         type: 'button',
       },
     });
@@ -808,7 +808,7 @@ export class MessageRenderer {
 
         // Show "copied!" feedback
         copyBtn.empty();
-        copyBtn.setText('Copied!');
+        copyBtn.setText(t('ui.copied'));
         copyBtn.classList.add('copied');
 
         feedbackTimeout = window.setTimeout(() => {
@@ -867,7 +867,7 @@ export class MessageRenderer {
       attr: { type: 'button' },
     });
     setIcon(copyBtn, 'copy');
-    copyBtn.setAttribute('aria-label', 'Copy message');
+    copyBtn.setAttribute('aria-label', t('ui.copyMessage'));
 
     let feedbackTimeout: number | null = null;
 
@@ -881,7 +881,7 @@ export class MessageRenderer {
         }
         if (feedbackTimeout) window.clearTimeout(feedbackTimeout);
         copyBtn.empty();
-        copyBtn.setText('Copied!');
+        copyBtn.setText(t('ui.copied'));
         copyBtn.classList.add('copied');
         feedbackTimeout = window.setTimeout(() => {
           copyBtn.empty();
@@ -939,7 +939,7 @@ export class MessageRenderer {
             try {
               await this.rewindCallback?.(messageId, mode);
             } catch (err) {
-              new Notice(t('chat.rewind.failed', { error: err instanceof Error ? err.message : 'Unknown error' }));
+              new Notice(t('chat.rewind.failed', { error: err instanceof Error ? err.message : t('ui.unknownError') }));
             }
           });
         });
@@ -962,7 +962,7 @@ export class MessageRenderer {
         try {
           await this.forkCallback?.(messageId);
         } catch (err) {
-          new Notice(t('chat.fork.failed', { error: err instanceof Error ? err.message : 'Unknown error' }));
+          new Notice(t('chat.fork.failed', { error: err instanceof Error ? err.message : t('ui.unknownError') }));
         }
       });
     });
diff --git a/src/features/chat/rendering/SubagentRenderer.ts b/src/features/chat/rendering/SubagentRenderer.ts
index 7b5042c..fdd4f16 100644
--- a/src/features/chat/rendering/SubagentRenderer.ts
+++ b/src/features/chat/rendering/SubagentRenderer.ts
@@ -1,5 +1,7 @@
 import { setIcon } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import { getToolIcon } from '../../../core/tools/toolIcons';
 import { TOOL_SUBAGENT } from '../../../core/tools/toolNames';
 import type { SubagentInfo, ToolCallInfo } from '../../../core/types';
@@ -47,7 +49,7 @@ const SUBAGENT_TOOL_STATUS_ICONS: Partial<Record<ToolCallInfo['status'], string>
 };
 
 function extractTaskDescription(input: Record<string, unknown>): string {
-  return (input.description as string) || 'Subagent task';
+  return (input.description as string) || t('ui.subagentTask');
 }
 
 function extractTaskPrompt(input: Record<string, unknown>): string {
@@ -83,13 +85,13 @@ function createSection(parentEl: HTMLElement, title: string, bodyClass?: string)
 function setPromptText(promptBodyEl: HTMLElement, prompt: string): void {
   promptBodyEl.empty();
   const textEl = promptBodyEl.createDiv({ cls: 'claudian-subagent-prompt-text' });
-  textEl.setText(prompt || 'No prompt provided');
+  textEl.setText(prompt || t('ui.noPromptProvided'));
 }
 
 function updateSyncHeaderAria(state: SubagentState): void {
   state.headerEl.setAttribute(
     'aria-label',
-    `Subagent task: ${truncateDescription(state.info.description)} - Status: ${state.info.status} - click to expand`
+    t('ui.subagentTaskStatusClickToExpandFormat', { p0: truncateDescription(state.info.description), p1: state.info.status })
   );
   state.statusEl.setAttribute('aria-label', `Status: ${state.info.status}`);
 }
@@ -99,7 +101,7 @@ function renderSubagentToolContent(contentEl: HTMLElement, toolCall: ToolCallInf
 
   if (!toolCall.result && toolCall.status === 'running') {
     const emptyEl = contentEl.createDiv({ cls: 'claudian-subagent-tool-empty' });
-    emptyEl.setText('Running...');
+    emptyEl.setText(t('chat.bangBash.running'));
     return;
   }
 
@@ -172,7 +174,7 @@ function ensureResultSection(state: SubagentState): SubagentSection {
     return { wrapperEl: state.resultSectionEl, bodyEl: state.resultBodyEl };
   }
 
-  const section = createSection(state.contentEl, 'Result', 'claudian-subagent-result-body');
+  const section = createSection(state.contentEl, t('ui.result'), 'claudian-subagent-result-body');
   section.wrapperEl.addClass('claudian-subagent-section-result');
   state.resultSectionEl = section.wrapperEl;
   state.resultBodyEl = section.bodyEl;
@@ -208,7 +210,7 @@ function hydrateSyncSubagentStateFromStored(state: SubagentState, subagent: Suba
   }
 
   if (subagent.status === 'completed' || subagent.status === 'error') {
-    const fallback = subagent.status === 'error' ? 'ERROR' : 'DONE';
+    const fallback = subagent.status === 'error' ? 'ERROR' : t('ui.done');
     finalizeSubagentBlock(state, subagent.result || fallback, subagent.status === 'error');
   } else {
     state.statusEl.className = 'claudian-subagent-status status-running';
@@ -249,11 +251,11 @@ export function createSubagentBlock(
   labelEl.setText(truncateDescription(description));
 
   const statusEl = headerEl.createDiv({ cls: 'claudian-subagent-status status-running' });
-  statusEl.setAttribute('aria-label', 'Status: running');
+  statusEl.setAttribute('aria-label', t('ui.statusRunning'));
 
   const contentEl = wrapperEl.createDiv({ cls: 'claudian-subagent-content' });
 
-  const promptSection = createSection(contentEl, 'Prompt', 'claudian-subagent-prompt-body');
+  const promptSection = createSection(contentEl, t('ui.prompt'), 'claudian-subagent-prompt-body');
   promptSection.wrapperEl.addClass('claudian-subagent-section-prompt');
   setPromptText(promptSection.bodyEl, prompt);
 
@@ -358,7 +360,7 @@ export function finalizeSubagentBlock(
     state.wrapperEl.addClass('error');
   }
 
-  const finalText = result?.trim() ? result : (isError ? 'ERROR' : 'DONE');
+  const finalText = result?.trim() ? result : (isError ? 'ERROR' : t('ui.done'));
   setResultText(state, finalText);
 
   updateSyncHeaderAria(state);
@@ -405,21 +407,21 @@ function getAsyncDisplayStatus(asyncStatus: string | undefined): 'running' | 'co
 
 function getAsyncStatusText(asyncStatus: string | undefined): string {
   switch (asyncStatus) {
-    case 'pending': return 'Initializing';
+    case 'pending': return t('ui.initializing');
     case 'completed': return ''; // Just show tick icon, no text
     case 'error': return 'Error';
-    case 'orphaned': return 'Orphaned';
-    default: return 'Running in background';
+    case 'orphaned': return t('ui.orphaned');
+    default: return t('ui.runningInBackground');
   }
 }
 
 function getAsyncStatusAriaLabel(asyncStatus: string | undefined): string {
   switch (asyncStatus) {
-    case 'pending': return 'Initializing';
-    case 'completed': return 'Completed';
+    case 'pending': return t('ui.initializing');
+    case 'completed': return t('ui.completed');
     case 'error': return 'Error';
-    case 'orphaned': return 'Orphaned';
-    default: return 'Running in background';
+    case 'orphaned': return t('ui.orphaned');
+    default: return t('ui.runningInBackground');
   }
 }
 
@@ -429,7 +431,7 @@ function updateAsyncLabel(state: AsyncSubagentState): void {
   const statusLabel = getAsyncStatusAriaLabel(state.info.asyncStatus);
   state.headerEl.setAttribute(
     'aria-label',
-    `Background task: ${truncateDescription(state.info.description)} - ${statusLabel} - click to expand`
+    t('ui.backgroundTaskClickToExpandFormat', { p0: truncateDescription(state.info.description), p1: statusLabel })
   );
 }
 
@@ -440,7 +442,7 @@ function renderAsyncContentLikeSync(
 ): void {
   contentEl.empty();
 
-  const promptSection = createSection(contentEl, 'Prompt', 'claudian-subagent-prompt-body');
+  const promptSection = createSection(contentEl, t('ui.prompt'), 'claudian-subagent-prompt-body');
   promptSection.wrapperEl.addClass('claudian-subagent-section-prompt');
   setPromptText(promptSection.bodyEl, subagent.prompt || '');
 
@@ -457,16 +459,16 @@ function renderAsyncContentLikeSync(
     return;
   }
 
-  const resultSection = createSection(contentEl, 'Result', 'claudian-subagent-result-body');
+  const resultSection = createSection(contentEl, t('ui.result'), 'claudian-subagent-result-body');
   resultSection.wrapperEl.addClass('claudian-subagent-section-result');
   const resultEl = resultSection.bodyEl.createDiv({ cls: 'claudian-subagent-result-output' });
 
   if (displayStatus === 'orphaned') {
-    resultEl.setText(subagent.result || 'Conversation ended before task completed');
+    resultEl.setText(subagent.result || t('ui.conversationEndedBeforeTaskCompleted'));
     return;
   }
 
-  const fallback = displayStatus === 'error' ? 'ERROR' : 'DONE';
+  const fallback = displayStatus === 'error' ? 'ERROR' : t('ui.done');
   const finalText = subagent.result?.trim() ? subagent.result : fallback;
   resultEl.setText(finalText);
 }
@@ -480,7 +482,7 @@ export function createAsyncSubagentBlock(
   taskToolId: string,
   taskInput: Record<string, unknown>
 ): AsyncSubagentState {
-  const description = (taskInput.description as string) || 'Background task';
+  const description = (taskInput.description as string) || t('ui.backgroundTask');
   const prompt = (taskInput.prompt as string) || '';
 
   const info: SubagentInfo = {
@@ -502,7 +504,7 @@ export function createAsyncSubagentBlock(
   headerEl.setAttribute('tabindex', '0');
   headerEl.setAttribute('role', 'button');
   headerEl.setAttribute('aria-expanded', 'false');
-  headerEl.setAttribute('aria-label', `Background task: ${description} - Initializing - click to expand`);
+  headerEl.setAttribute('aria-label', t('ui.backgroundTaskInitializingClickToExpandFormat', { p0: description }));
 
   const iconEl = headerEl.createDiv({ cls: 'claudian-subagent-icon' });
   iconEl.setAttribute('aria-hidden', 'true');
@@ -512,10 +514,10 @@ export function createAsyncSubagentBlock(
   labelEl.setText(truncateDescription(description));
 
   const statusTextEl = headerEl.createDiv({ cls: 'claudian-subagent-status-text' });
-  statusTextEl.setText('Initializing');
+  statusTextEl.setText(t('ui.initializing'));
 
   const statusEl = headerEl.createDiv({ cls: 'claudian-subagent-status status-running' });
-  statusEl.setAttribute('aria-label', 'Status: running');
+  statusEl.setAttribute('aria-label', t('ui.statusRunning'));
 
   const contentEl = wrapperEl.createDiv({ cls: 'claudian-subagent-content' });
   renderAsyncContentLikeSync(contentEl, info, 'running');
@@ -543,7 +545,7 @@ export function updateAsyncSubagentRunning(
   setAsyncWrapperStatus(state.wrapperEl, 'running');
   updateAsyncLabel(state);
 
-  state.statusTextEl.setText('Running in background');
+  state.statusTextEl.setText(t('ui.runningInBackground'));
 
   renderAsyncContentLikeSync(state.contentEl, state.info, 'running');
 }
@@ -583,12 +585,12 @@ export function finalizeAsyncSubagent(
 export function markAsyncSubagentOrphaned(state: AsyncSubagentState): void {
   state.info.asyncStatus = 'orphaned';
   state.info.status = 'error';
-  state.info.result = 'Conversation ended before task completed';
+  state.info.result = t('ui.conversationEndedBeforeTaskCompleted');
 
   setAsyncWrapperStatus(state.wrapperEl, 'orphaned');
   updateAsyncLabel(state);
 
-  state.statusTextEl.setText('Orphaned');
+  state.statusTextEl.setText(t('ui.orphaned'));
 
   state.statusEl.className = 'claudian-subagent-status status-error';
   state.statusEl.empty();
@@ -628,7 +630,7 @@ export function renderStoredAsyncSubagent(
   headerEl.setAttribute('aria-expanded', 'false');
   headerEl.setAttribute(
     'aria-label',
-    `Background task: ${subagent.description} - ${statusAriaLabel} - click to expand`
+    t('ui.backgroundTaskClickToExpandFormat', { p0: subagent.description, p1: statusAriaLabel })
   );
 
   const iconEl = headerEl.createDiv({ cls: 'claudian-subagent-icon' });
diff --git a/src/features/chat/rendering/ThinkingBlockRenderer.ts b/src/features/chat/rendering/ThinkingBlockRenderer.ts
index 13292ee..550be0e 100644
--- a/src/features/chat/rendering/ThinkingBlockRenderer.ts
+++ b/src/features/chat/rendering/ThinkingBlockRenderer.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 import { collapseElement, setupCollapsible } from './collapsible';
 
 export type RenderContentFn = (el: HTMLElement, markdown: string) => Promise<void>;
@@ -27,17 +29,17 @@ export function createThinkingBlock(
   header.setAttribute('tabindex', '0');
   header.setAttribute('role', 'button');
   header.setAttribute('aria-expanded', 'false');
-  header.setAttribute('aria-label', 'Extended thinking - click to expand');
+  header.setAttribute('aria-label', t('ui.extendedThinkingClickToExpand'));
 
   // Label with timer
   const labelEl = header.createSpan({ cls: 'claudian-thinking-label' });
   const startTime = Date.now();
-  labelEl.setText('Thinking 0s...');
+  labelEl.setText(t('ui.thinking0s'));
 
   // Start timer interval to update label every second
   const timerInterval = window.setInterval(() => {
     const elapsed = Math.floor((Date.now() - startTime) / 1000);
-    labelEl.setText(`Thinking ${elapsed}s...`);
+    labelEl.setText(t('ui.thinkingSeconds', { seconds: elapsed }));
   }, 1000);
 
   // Collapsible content (collapsed by default)
@@ -81,7 +83,7 @@ export function finalizeThinkingBlock(state: ThinkingBlockState): number {
   const durationSeconds = Math.floor((Date.now() - state.startTime) / 1000);
 
   // Update label to show final duration (without "...")
-  state.labelEl.setText(`Thought for ${durationSeconds}s`);
+  state.labelEl.setText(t('ui.thoughtSeconds', { seconds: durationSeconds }));
 
   // Collapse when done and sync state
   const header = state.wrapperEl.querySelector('.claudian-thinking-header');
@@ -110,11 +112,11 @@ export function renderStoredThinkingBlock(
   const header = wrapperEl.createDiv({ cls: 'claudian-thinking-header' });
   header.setAttribute('tabindex', '0');
   header.setAttribute('role', 'button');
-  header.setAttribute('aria-label', 'Extended thinking - click to expand');
+  header.setAttribute('aria-label', t('ui.extendedThinkingClickToExpand'));
 
   // Label with duration
   const labelEl = header.createSpan({ cls: 'claudian-thinking-label' });
-  const labelText = durationSeconds !== undefined ? `Thought for ${durationSeconds}s` : 'Thought';
+  const labelText = durationSeconds !== undefined ? t('ui.thoughtSeconds', { seconds: durationSeconds }) : t('ui.thought');
   labelEl.setText(labelText);
 
   // Collapsible content
diff --git a/src/features/chat/rendering/ToolCallRenderer.ts b/src/features/chat/rendering/ToolCallRenderer.ts
index f9bab0f..844038c 100644
--- a/src/features/chat/rendering/ToolCallRenderer.ts
+++ b/src/features/chat/rendering/ToolCallRenderer.ts
@@ -1,5 +1,7 @@
 import { setIcon } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import type { TodoItem } from '../../../core/tools/todo';
 import { getToolIcon, MCP_ICON_MARKER } from '../../../core/tools/toolIcons';
 import { extractResolvedAnswersFromResultText } from '../../../core/tools/toolInput';
@@ -62,14 +64,14 @@ export function getToolName(name: string, input: Record<string, unknown>): strin
       const todos = input.todos as Array<{ status: string }> | undefined;
       if (todos && Array.isArray(todos) && todos.length > 0) {
         const completed = todos.filter(t => t.status === 'completed').length;
-        return `Tasks ${completed}/${todos.length}`;
+        return t('ui.tasksFormat', { p0: completed, p1: todos.length });
       }
-      return 'Tasks';
+      return t('ui.tasks');
     }
     case TOOL_ENTER_PLAN_MODE:
-      return 'Entering plan mode';
+      return t('ui.enteringPlanMode');
     case TOOL_EXIT_PLAN_MODE:
-      return 'Plan complete';
+      return t('ui.planComplete');
     default:
       return name;
   }
@@ -144,9 +146,9 @@ export function getToolLabel(name: string, input: Record<string, unknown>): stri
       const todos = input.todos as Array<{ status: string }> | undefined;
       if (todos && Array.isArray(todos)) {
         const completed = todos.filter(t => t.status === 'completed').length;
-        return `Tasks (${completed}/${todos.length})`;
+        return t('ui.tasksFormat2', { p0: completed, p1: todos.length });
       }
-      return 'Tasks';
+      return t('ui.tasks');
     }
     case TOOL_SKILL: {
       const skillName = getInputText(input, 'skill', 'skill');
@@ -157,9 +159,9 @@ export function getToolLabel(name: string, input: Record<string, unknown>): stri
       return `ToolSearch: ${tools || 'tools'}`;
     }
     case TOOL_ENTER_PLAN_MODE:
-      return 'Entering plan mode';
+      return t('ui.enteringPlanMode');
     case TOOL_EXIT_PLAN_MODE:
-      return 'Plan complete';
+      return t('ui.planComplete');
     case TOOL_APPLY_PATCH: {
       const summary = getApplyPatchSummary(input);
       return summary ? `apply_patch: ${summary}` : 'apply_patch';
@@ -299,10 +301,10 @@ function getWebSearchSummary(input: Record<string, unknown>, maxLength: number):
 
   switch (data.actionType) {
     case 'open_page':
-      return truncateText(`Open ${data.url || 'page'}`, maxLength);
+      return truncateText(t('ui.openFormat', { p0: data.url || 'page' }), maxLength);
     case 'find_in_page': {
-      const target = data.pattern ? `Find "${data.pattern}"` : 'Find in page';
-      const suffix = data.url ? ` in ${data.url}` : '';
+      const target = data.pattern ? t('ui.findFormat', { p0: data.pattern }) : t('ui.findInPage');
+      const suffix = data.url ? t('ui.inFormat', { p0: data.url }) : '';
       return truncateText(target + suffix, maxLength);
     }
     case 'search':
@@ -362,20 +364,20 @@ function renderWebSearchActionExpanded(container: HTMLElement, input: Record<str
 
   switch (data.actionType) {
     case 'open_page':
-      linesEl.createDiv({ cls: 'claudian-tool-line', text: 'Open page' });
+      linesEl.createDiv({ cls: 'claudian-tool-line', text: t('ui.openPage') });
       if (data.url) {
         appendToolLink(linesEl, data.url, data.url);
       } else {
-        linesEl.createDiv({ cls: 'claudian-tool-line', text: 'URL unavailable' });
+        linesEl.createDiv({ cls: 'claudian-tool-line', text: t('ui.urlUnavailable') });
       }
       return true;
 
     case 'find_in_page':
-      linesEl.createDiv({ cls: 'claudian-tool-line', text: 'Find in page' });
+      linesEl.createDiv({ cls: 'claudian-tool-line', text: t('ui.findInPage') });
       if (data.url) {
         appendToolLink(linesEl, data.url, data.url);
       } else {
-        linesEl.createDiv({ cls: 'claudian-tool-line', text: 'URL unavailable' });
+        linesEl.createDiv({ cls: 'claudian-tool-line', text: t('ui.urlUnavailable') });
       }
       if (data.pattern) {
         linesEl.createDiv({ cls: 'claudian-tool-line', text: `Pattern: ${data.pattern}` });
@@ -387,17 +389,17 @@ function renderWebSearchActionExpanded(container: HTMLElement, input: Record<str
       const primaryQuery = data.query || data.queries[0];
       linesEl.createDiv({
         cls: 'claudian-tool-line',
-        text: primaryQuery ? `Query: ${primaryQuery}` : 'Search web',
+        text: primaryQuery ? `Query: ${primaryQuery}` : t('ui.searchWeb'),
       });
 
       const alternateQueries = data.queries.filter(query => query !== primaryQuery);
       for (const query of alternateQueries.slice(0, 4)) {
-        linesEl.createDiv({ cls: 'claudian-tool-line', text: `Alt query: ${query}` });
+        linesEl.createDiv({ cls: 'claudian-tool-line', text: t('ui.altQueryFormat', { p0: query }) });
       }
       if (alternateQueries.length > 4) {
         linesEl.createDiv({
           cls: 'claudian-tool-truncated',
-          text: `... ${alternateQueries.length - 4} more queries`,
+          text: t('ui.moreQueriesFormat', { p0: alternateQueries.length - 4 }),
         });
       }
       return true;
@@ -447,13 +449,13 @@ function renderWebSearchExpanded(
     return;
   }
 
-  container.createDiv({ cls: 'claudian-tool-empty', text: 'No result' });
+  container.createDiv({ cls: 'claudian-tool-empty', text: t('ui.noResult') });
 }
 
 function renderFileSearchExpanded(container: HTMLElement, result: string): void {
   const lines = result.split(/\r?\n/).filter(line => line.trim());
   if (lines.length === 0) {
-    container.createDiv({ cls: 'claudian-tool-empty', text: 'No matches found' });
+    container.createDiv({ cls: 'claudian-tool-empty', text: t('ui.noMatchesFound') });
     return;
   }
   renderLinesExpanded(container, result, 15, true);
@@ -480,7 +482,7 @@ function renderLinesExpanded(
   if (truncated) {
     linesEl.createDiv({
       cls: 'claudian-tool-truncated',
-      text: `... ${lines.length - maxLines} more lines`,
+      text: t('ui.moreLinesFormat', { p0: lines.length - maxLines }),
     });
   }
 }
@@ -520,7 +522,7 @@ function renderWebFetchExpanded(container: HTMLElement, result: string): void {
     lineEl.setText(result.slice(0, maxChars));
     linesEl.createDiv({
       cls: 'claudian-tool-truncated',
-      text: `... ${result.length - maxChars} more characters`,
+      text: t('ui.moreCharactersFormat', { p0: result.length - maxChars }),
     });
   } else {
     lineEl.setText(result);
@@ -581,7 +583,7 @@ function renderApplyPatchExpanded(
     return;
   }
 
-  container.createDiv({ cls: 'claudian-tool-empty', text: 'No result' });
+  container.createDiv({ cls: 'claudian-tool-empty', text: t('ui.noResult') });
 }
 
 function renderApplyPatchDiffSections(
@@ -592,12 +594,12 @@ function renderApplyPatchDiffSections(
     const sectionEl = container.createDiv({ cls: 'claudian-tool-patch-section' });
 
     if (fileDiff.operation === 'delete' && fileDiff.diffLines.length === 0) {
-      sectionEl.createDiv({ cls: 'claudian-tool-empty', text: 'File deleted' });
+      sectionEl.createDiv({ cls: 'claudian-tool-empty', text: t('ui.fileDeleted') });
       continue;
     }
 
     if (fileDiff.diffLines.length === 0) {
-      sectionEl.createDiv({ cls: 'claudian-tool-empty', text: 'No textual diff available' });
+      sectionEl.createDiv({ cls: 'claudian-tool-empty', text: t('ui.noTextualDiffAvailable') });
       continue;
     }
 
@@ -676,7 +678,7 @@ export function renderExpandedContent(
   input: Record<string, unknown> = {},
 ): void {
   if (!result && toolName !== TOOL_WEB_SEARCH && toolName !== TOOL_BASH && toolName !== TOOL_APPLY_PATCH) {
-    container.createDiv({ cls: 'claudian-tool-empty', text: 'No result' });
+    container.createDiv({ cls: 'claudian-tool-empty', text: t('ui.noResult') });
     return;
   }
 
@@ -754,13 +756,13 @@ const STATUS_ICONS: Record<string, string> = {
 function setTodoWriteStatus(statusEl: HTMLElement, input: Record<string, unknown>): void {
   const isComplete = areAllTodosCompleted(input);
   const status = isComplete ? 'completed' : 'running';
-  const ariaLabel = isComplete ? 'Status: completed' : 'Status: in progress';
+  const ariaLabel = isComplete ? t('ui.statusCompleted') : t('ui.statusInProgress');
   resetStatusElement(statusEl, `status-${status}`, ariaLabel);
   if (isComplete) setIcon(statusEl, 'check');
 }
 
 function setToolStatus(statusEl: HTMLElement, status: ToolCallInfo['status']): void {
-  resetStatusElement(statusEl, `status-${status}`, `Status: ${status}`);
+  resetStatusElement(statusEl, `status-${status}`, t(({ running: 'ui.statusRunning', completed: 'ui.statusCompleted', error: 'ui.statusError', blocked: 'ui.statusBlocked' } as const)[status]));
   const icon = STATUS_ICONS[status];
   if (icon) setIcon(statusEl, icon);
 }
@@ -799,7 +801,7 @@ export function renderTodoWriteResult(
   const todos = input.todos as TodoItem[] | undefined;
   if (!todos || !Array.isArray(todos)) {
     const item = container.createSpan({ cls: 'claudian-tool-result-item' });
-    item.setText('Tasks updated');
+    item.setText(t('ui.tasksUpdated'));
     return;
   }
 
@@ -890,7 +892,7 @@ function renderAskUserQuestionResult(container: HTMLElement, toolCall: ToolCallI
     const bodyEl = pairEl.createDiv({ cls: 'claudian-ask-review-body' });
     bodyEl.createDiv({ text: q.question, cls: 'claudian-ask-review-q-text' });
     bodyEl.createDiv({
-      text: answer || 'Not answered',
+      text: answer || t('ui.notAnswered'),
       cls: answer ? 'claudian-ask-review-a-text' : 'claudian-ask-review-empty',
     });
   }
@@ -906,14 +908,14 @@ function renderAskUserQuestionFallback(container: HTMLElement, toolCall: ToolCal
     : [];
 
   if (questions.length === 0) {
-    contentFallback(container, initialText || toolCall.result || 'Waiting for answer...');
+    contentFallback(container, initialText || toolCall.result || t('ui.waitingForAnswer'));
     return;
   }
 
   if (initialText || toolCall.result) {
     container.createDiv({
       cls: 'claudian-ask-review-prompt',
-      text: initialText || toolCall.result || 'Waiting for answer...',
+      text: initialText || toolCall.result || t('ui.waitingForAnswer'),
     });
   }
 
@@ -926,7 +928,7 @@ function renderAskUserQuestionFallback(container: HTMLElement, toolCall: ToolCal
     bodyEl.createDiv({ text: question.question, cls: 'claudian-ask-review-q-text' });
 
     if (!Array.isArray(question.options) || question.options.length === 0) {
-      bodyEl.createDiv({ cls: 'claudian-ask-review-empty', text: 'No options recorded' });
+      bodyEl.createDiv({ cls: 'claudian-ask-review-empty', text: t('ui.noOptionsRecorded') });
       continue;
     }
 
@@ -982,7 +984,7 @@ function renderBashContent(
   } else if (result) {
     renderLinesExpanded(container, result, 20);
   } else {
-    container.createDiv({ cls: 'claudian-tool-empty', text: 'No result' });
+    container.createDiv({ cls: 'claudian-tool-empty', text: t('ui.noResult') });
   }
 }
 
@@ -1025,7 +1027,7 @@ function renderToolContent(
   } else if (toolCall.name === TOOL_ASK_USER_QUESTION) {
     content.addClass('claudian-tool-content-ask');
     if (initialText) {
-      renderAskUserQuestionFallback(content, toolCall, 'Waiting for answer...');
+      renderAskUserQuestionFallback(content, toolCall, t('ui.waitingForAnswer'));
     } else if (!renderAskUserQuestionResult(content, toolCall)) {
       renderAskUserQuestionFallback(content, toolCall);
     }
@@ -1052,7 +1054,7 @@ export function renderToolCall(
 
   setGenericToolHeaderRight(statusEl, toolCall);
 
-  renderToolContent(content, toolCall, 'Running...');
+  renderToolContent(content, toolCall, t('chat.bangBash.running'));
 
   const initiallyExpanded = options.initiallyExpanded ?? false;
   const state = { isExpanded: initiallyExpanded };
diff --git a/src/features/chat/rendering/WriteEditRenderer.ts b/src/features/chat/rendering/WriteEditRenderer.ts
index f7d66c3..81f8f49 100644
--- a/src/features/chat/rendering/WriteEditRenderer.ts
+++ b/src/features/chat/rendering/WriteEditRenderer.ts
@@ -1,5 +1,7 @@
 import { setIcon } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import { getToolIcon } from '../../../core/tools/toolIcons';
 import type { ToolCallInfo, ToolDiffData } from '../../../core/types';
 import type { DiffLine } from '../../../core/types/diff';
@@ -78,7 +80,7 @@ export function createWriteEditBlock(
   const statsEl = headerEl.createDiv({ cls: 'claudian-write-edit-stats' });
 
   const statusEl = headerEl.createDiv({ cls: 'claudian-write-edit-status status-running' });
-  statusEl.setAttribute('aria-label', 'Status: running');
+  statusEl.setAttribute('aria-label', t('ui.statusRunning'));
 
   // Content area (collapsed by default)
   const contentEl = wrapperEl.createDiv({ cls: 'claudian-write-edit-content' });
@@ -86,7 +88,7 @@ export function createWriteEditBlock(
   // Initial loading state
   const loadingRow = contentEl.createDiv({ cls: 'claudian-write-edit-diff-row' });
   const loadingEl = loadingRow.createDiv({ cls: 'claudian-write-edit-loading' });
-  loadingEl.setText('Writing...');
+  loadingEl.setText(t('ui.writing'));
 
   // Create state object
   const state: WriteEditState = {
@@ -134,7 +136,7 @@ export function finalizeWriteEditBlock(state: WriteEditState, isError: boolean):
   if (isError) {
     state.statusEl.addClass('status-error');
     setIcon(state.statusEl, 'x');
-    state.statusEl.setAttribute('aria-label', 'Status: error');
+    state.statusEl.setAttribute('aria-label', t('ui.statusError'));
 
     // Show error in content if no diff was shown
     if (!state.diffLines) {
@@ -148,7 +150,7 @@ export function finalizeWriteEditBlock(state: WriteEditState, isError: boolean):
     state.contentEl.empty();
     const row = state.contentEl.createDiv({ cls: 'claudian-write-edit-diff-row' });
     const doneEl = row.createDiv({ cls: 'claudian-write-edit-done-text' });
-    doneEl.setText('DONE');
+    doneEl.setText(t('ui.done'));
   }
 
   // Update wrapper class
@@ -218,7 +220,7 @@ export function renderStoredWriteEdit(
     errorEl.setText(toolCall.result);
   } else {
     const doneEl = row.createDiv({ cls: 'claudian-write-edit-done-text' });
-    doneEl.setText(isError ? 'ERROR' : 'DONE');
+    doneEl.setText(isError ? 'ERROR' : t('ui.done'));
   }
 
   // Setup collapsible behavior (handles click, keyboard, ARIA, CSS)
diff --git a/src/features/chat/rendering/collapsible.ts b/src/features/chat/rendering/collapsible.ts
index a38ce42..80e9e03 100644
--- a/src/features/chat/rendering/collapsible.ts
+++ b/src/features/chat/rendering/collapsible.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 export interface CollapsibleState {
   isExpanded: boolean;
 }
@@ -39,7 +41,7 @@ export function setupCollapsible(
   // Helper to update aria-label based on expanded state
   const updateAriaLabel = (isExpanded: boolean) => {
     if (baseAriaLabel) {
-      const action = isExpanded ? 'click to collapse' : 'click to expand';
+      const action = isExpanded ? t('ui.clickToCollapse') : t('ui.clickToExpand');
       headerEl.setAttribute('aria-label', `${baseAriaLabel} - ${action}`);
     }
   };
diff --git a/src/features/chat/services/BangBashService.ts b/src/features/chat/services/BangBashService.ts
index e518309..d2c52bc 100644
--- a/src/features/chat/services/BangBashService.ts
+++ b/src/features/chat/services/BangBashService.ts
@@ -1,5 +1,7 @@
 import { exec } from 'child_process';
 
+import { t } from '@/i18n/i18n';
+
 export interface BangBashResult {
   command: string;
   stdout: string;
@@ -38,8 +40,8 @@ export class BangBashService {
             stderr: stderr ?? '',
             exitCode: 124,
             error: isMaxBuffer
-              ? 'Output exceeded maximum buffer size (1MB)'
-              : `Command timed out after ${TIMEOUT_MS / 1000}s`,
+              ? t('ui.outputExceededMaximumBufferSize1MB')
+              : t('ui.commandTimedOutAfterSFormat', { p0: TIMEOUT_MS / 1000 }),
           });
           return;
         }
diff --git a/src/features/chat/services/SubagentManager.ts b/src/features/chat/services/SubagentManager.ts
index a493415..8bce6b7 100644
--- a/src/features/chat/services/SubagentManager.ts
+++ b/src/features/chat/services/SubagentManager.ts
@@ -2,6 +2,8 @@ import { existsSync, readFileSync, realpathSync } from 'fs';
 import { tmpdir } from 'os';
 import { isAbsolute, sep } from 'path';
 
+import { t } from '@/i18n/i18n';
+
 import { ProviderRegistry } from '../../../core/providers/ProviderRegistry';
 import type { ProviderTaskResultInterpreter } from '../../../core/providers/types';
 import { TOOL_SUBAGENT } from '../../../core/tools/toolNames';
@@ -345,7 +347,7 @@ export class SubagentManager {
 
     if (isError) {
       if (!record.terminalSource) {
-        this.transitionToError(record, resultText || 'Task failed to start');
+        this.transitionToError(record, resultText || t('ui.taskFailedToStart'));
       }
       return;
     }
@@ -469,7 +471,7 @@ export class SubagentManager {
     if (record.nativeCompletion) return undefined;
 
     const result = completion.result?.trim()
-      || (completion.status === 'error' ? 'Background task failed.' : 'Background task completed.');
+      || (completion.status === 'error' ? t('ui.backgroundTaskFailed') : t('ui.backgroundTaskCompleted'));
     record.nativeCompletion = {
       taskId: completion.taskId,
       status: completion.status,
@@ -567,7 +569,7 @@ export class SubagentManager {
   private markOrphaned(record: AsyncSubagentRecord): void {
     record.info.asyncStatus = 'orphaned';
     record.info.status = 'error';
-    record.info.result = 'Conversation ended before task completed';
+    record.info.result = t('ui.conversationEndedBeforeTaskCompleted');
     record.info.completedAt = Date.now();
     record.terminalSource = 'local_error';
     this.publishAsyncState(record.info);
@@ -664,7 +666,7 @@ export class SubagentManager {
     taskInput: Record<string, unknown>,
     parentEl: HTMLElement
   ): HandleTaskResult {
-    const description = (taskInput.description as string) || 'Background task';
+    const description = (taskInput.description as string) || t('ui.backgroundTask');
     const prompt = (taskInput.prompt as string) || '';
 
     const info: SubagentInfo = {
diff --git a/src/features/chat/session-manager/SessionListOrganizer.ts b/src/features/chat/session-manager/SessionListOrganizer.ts
index d1046f9..7d6ff5a 100644
--- a/src/features/chat/session-manager/SessionListOrganizer.ts
+++ b/src/features/chat/session-manager/SessionListOrganizer.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 import type {
   ConversationMeta,
   SessionManagerOrganization,
@@ -141,7 +143,7 @@ export function organizeSessionList(
     sections.push({
       key: 'ungrouped',
       kind: 'ungrouped',
-      label: 'Unlinked',
+      label: t('ui.unlinked'),
       conversations: ungrouped,
     });
   }
diff --git a/src/features/chat/tabs/TabBar.ts b/src/features/chat/tabs/TabBar.ts
index 110dcc7..6c3f61a 100644
--- a/src/features/chat/tabs/TabBar.ts
+++ b/src/features/chat/tabs/TabBar.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 import { scheduleAnimationFrame } from '../../../utils/animationFrame';
 import type { TabBarItem, TabId } from './types';
 
@@ -184,15 +186,15 @@ export class TabBar {
   }
 
   private getBadgeStatusLabel(item: TabBarItem): string {
-    if (item.isActive) return 'active';
-    if (item.attention?.kind === 'action-required') return 'needs your input';
-    if (item.isWorking) return 'working';
+    if (item.isActive) return t('ui.tabActive');
+    if (item.attention?.kind === 'action-required') return t('ui.needsYourInput2');
+    if (item.isWorking) return t('ui.tabWorking');
     if (item.attention?.kind === 'review') {
       return item.attention.outcome === 'error'
-        ? 'stopped with an error, ready to review'
-        : 'finished, ready to review';
+        ? t('ui.stoppedWithAnErrorReadyToReview')
+        : t('ui.finishedReadyToReview');
     }
-    return 'idle';
+    return t('ui.tabIdle');
   }
 
   private truncateExpandedTitle(title: string): string {
diff --git a/src/features/chat/tabs/TabForking.ts b/src/features/chat/tabs/TabForking.ts
index 912bca9..117061d 100644
--- a/src/features/chat/tabs/TabForking.ts
+++ b/src/features/chat/tabs/TabForking.ts
@@ -95,7 +95,7 @@ export async function handleForkRequest(
   const sourceConversationId = tab.conversationId;
 
   if (!getTabCapabilities(tab, plugin).supportsFork) {
-    new Notice('Fork is not supported by this provider.');
+    new Notice(t('ui.forkIsNotSupportedByThisProvider'));
     return;
   }
 
@@ -157,7 +157,7 @@ export async function handleForkAll(
   const sourceConversationId = tab.conversationId;
 
   if (!getTabCapabilities(tab, plugin).supportsFork) {
-    new Notice('Fork is not supported by this provider.');
+    new Notice(t('ui.forkIsNotSupportedByThisProvider'));
     return;
   }
 
diff --git a/src/features/chat/tabs/TabLifecycle.ts b/src/features/chat/tabs/TabLifecycle.ts
index cafaf2e..168cb2b 100644
--- a/src/features/chat/tabs/TabLifecycle.ts
+++ b/src/features/chat/tabs/TabLifecycle.ts
@@ -1,5 +1,7 @@
 import { Notice } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import type { FeatureHost } from '../../FeatureHost';
 import type {
   AssembledTabRuntime,
@@ -156,7 +158,7 @@ async function destroyTabOnce(tab: AssembledTabRuntime): Promise<void> {
     try {
       await tab.controllers.conversationController.save(cancelledActiveTurn);
     } catch {
-      new Notice('Background task state could not be saved before closing the tab.');
+      new Notice(t('ui.backgroundTaskStateCouldNotBeSavedBeforeClosingTheTab'));
     }
   }
   await captureTeardownFailure(
@@ -186,5 +188,5 @@ export function getTabTitle(tab: AssembledTabRuntime, plugin: FeatureHost): stri
       return conversation.title;
     }
   }
-  return 'New Chat';
+  return t('ui.newChat');
 }
diff --git a/src/features/chat/tabs/TabManager.ts b/src/features/chat/tabs/TabManager.ts
index d1d2a79..b69230e 100644
--- a/src/features/chat/tabs/TabManager.ts
+++ b/src/features/chat/tabs/TabManager.ts
@@ -610,7 +610,7 @@ export class TabManager implements TabManagerInterface {
       if (rollbackErrors.length > 0) {
         throw new AggregateError(
           [error, ...rollbackErrors],
-          `Failed to switch to tab ${tabId} and restore the previous tab`,
+          t('ui.failedToSwitchToTabAndRestoreThePreviousTabFormat', { p0: tabId }),
           { cause: error },
         );
       }
@@ -955,7 +955,7 @@ export class TabManager implements TabManagerInterface {
         }
       }
 
-      throwCollectedErrors(closeErrors, `Failed to close tab ${tabId} cleanly`);
+      throwCollectedErrors(closeErrors, t('ui.failedToCloseTabCleanlyFormat', { p0: tabId }));
       return true;
     } finally {
       this.closingTabIds.delete(tabId);
@@ -1080,18 +1080,18 @@ export class TabManager implements TabManagerInterface {
     if (!error) {
       statusEl.createDiv({
         cls: 'claudian-tab-hydration-loading',
-        text: 'Loading conversation…',
+        text: t('ui.loadingConversation'),
       });
       return;
     }
 
     statusEl.createDiv({
       cls: 'claudian-tab-hydration-error',
-      text: error instanceof Error ? error.message : 'Failed to load conversation',
+      text: error instanceof Error ? error.message : t('ui.failedToLoadConversation'),
     });
     const retryButton = statusEl.createEl('button', {
       cls: 'mod-cta claudian-tab-hydration-retry',
-      text: 'Retry',
+      text: t('collab.panel.retry'),
     });
     retryButton.addEventListener('click', () => {
       if (!this.isTabAlive(tab)) return;
@@ -1768,7 +1768,7 @@ export class TabManager implements TabManagerInterface {
   private buildForkTitle(sourceTitle: string, forkAtUserMessage?: number): string {
     const MAX_TITLE_LENGTH = 50;
     const forkSuffix = forkAtUserMessage ? ` (#${forkAtUserMessage})` : '';
-    const forkPrefix = 'Fork: ';
+    const forkPrefix = t('ui.fork');
     const maxSourceLength = MAX_TITLE_LENGTH - forkPrefix.length - forkSuffix.length;
     const truncatedSource = sourceTitle.length > maxSourceLength
       ? sourceTitle.slice(0, maxSourceLength - 1) + '…'
diff --git a/src/features/chat/tabs/runtime/TabRuntimeControllers.ts b/src/features/chat/tabs/runtime/TabRuntimeControllers.ts
index a28f6b8..afc66e5 100644
--- a/src/features/chat/tabs/runtime/TabRuntimeControllers.ts
+++ b/src/features/chat/tabs/runtime/TabRuntimeControllers.ts
@@ -1,6 +1,8 @@
 import type { Component } from 'obsidian';
 import { Notice } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import { resolveNewConversationModel } from '../../../../core/providers/conversationModel';
 import { getEnabledProviderForModel } from '../../../../core/providers/modelRouting';
 import { ProviderRegistry } from '../../../../core/providers/ProviderRegistry';
@@ -92,7 +94,7 @@ export function buildTabRuntimeControllers(
       applyProviderUIGating(tab, plugin);
       return true;
     } catch (error) {
-      new Notice(error instanceof Error ? error.message : 'Failed to initialize chat execution');
+      new Notice(error instanceof Error ? error.message : t('ui.failedToInitializeChatExecution'));
       return false;
     }
   };
diff --git a/src/features/chat/tabs/runtime/TabRuntimeShell.ts b/src/features/chat/tabs/runtime/TabRuntimeShell.ts
index 19dbe21..76c2fa6 100644
--- a/src/features/chat/tabs/runtime/TabRuntimeShell.ts
+++ b/src/features/chat/tabs/runtime/TabRuntimeShell.ts
@@ -1,5 +1,7 @@
 import { Notice } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import type { ProviderInteractionPort } from '../../../../core/execution';
 import { resolveNewConversationModel } from '../../../../core/providers/conversationModel';
 import { getEnabledProviderForModel } from '../../../../core/providers/modelRouting';
@@ -169,7 +171,7 @@ function buildTabDOM(contentEl: HTMLElement): TabDOMElements {
   const inputEl = inputWrapper.createEl('textarea', {
     cls: 'claudian-input',
     attr: {
-      placeholder: 'Ask to make changes, @mention files, run /commands',
+      placeholder: t('ui.askToMakeChangesMentionFilesRunCommands'),
       rows: '3',
       dir: 'auto',
     },
@@ -302,7 +304,7 @@ function createTabExecutionCoordinator(
     resolveMissingProviderSession: (conversationId, missingProviderSessionId) =>
       plugin.handleMissingProviderSession(conversationId, missingProviderSessionId),
     onError: error => {
-      new Notice(error instanceof Error ? error.message : 'Provider execution failed.');
+      new Notice(error instanceof Error ? error.message : t('ui.providerExecutionFailed'));
     },
     warmExecution: {
       ownerId: id,
diff --git a/src/features/chat/tabs/runtime/TabRuntimeUI.ts b/src/features/chat/tabs/runtime/TabRuntimeUI.ts
index 7cdedfc..4d208d9 100644
--- a/src/features/chat/tabs/runtime/TabRuntimeUI.ts
+++ b/src/features/chat/tabs/runtime/TabRuntimeUI.ts
@@ -1,5 +1,7 @@
 import { Notice } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import {
   getProviderSettingsSnapshotWithModel,
   normalizeProviderModelSelection,
@@ -329,7 +331,7 @@ function buildInputToolbar(
       const boundProvider = tab.providerId;
       const modelProvider = getProviderForModel(model, plugin.settings);
       if (modelProvider !== boundProvider) {
-        new Notice('Cannot switch provider on a bound session. Start a new conversation instead.');
+        new Notice(t('ui.cannotSwitchProviderOnABoundSessionStartANewConversationInstead'));
         tab.ui.modelSelector.updateDisplay();
         return;
       }
diff --git a/src/features/chat/ui/BangBashModeManager.ts b/src/features/chat/ui/BangBashModeManager.ts
index 6f40b20..ead23e5 100644
--- a/src/features/chat/ui/BangBashModeManager.ts
+++ b/src/features/chat/ui/BangBashModeManager.ts
@@ -102,7 +102,7 @@ export class BangBashModeManager {
       this.clear();
       await this.callbacks.onSubmit(rawCommand);
     } catch (e) {
-      new Notice(`Command failed: ${e instanceof Error ? e.message : String(e)}`);
+      new Notice(t('ui.commandFailedFormat', { p0: e instanceof Error ? e.message : String(e) }));
     } finally {
       this.isSubmitting = false;
     }
diff --git a/src/features/chat/ui/ComposerContextTray.ts b/src/features/chat/ui/ComposerContextTray.ts
index dc5643e..3d27f9b 100644
--- a/src/features/chat/ui/ComposerContextTray.ts
+++ b/src/features/chat/ui/ComposerContextTray.ts
@@ -1,5 +1,7 @@
 import { setIcon } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import {
   cancelScheduledAnimationFrame,
   scheduleAnimationFrame,
@@ -113,7 +115,7 @@ export class ComposerContextTray {
     moreButton.removeClass('claudian-hidden');
     if (this.expanded) {
       this.containerEl.addClass('claudian-context-row--expanded');
-      moreButton.textContent = 'Show less';
+      moreButton.textContent = t('ui.showLess');
       moreButton.setAttribute('aria-expanded', 'true');
       return;
     }
@@ -136,7 +138,7 @@ export class ComposerContextTray {
 
     const hiddenCount = chips.length - visibleCount;
     moreButton.textContent = `+${hiddenCount} more`;
-    moreButton.setAttribute('aria-label', `Show ${hiddenCount} more context items`);
+    moreButton.setAttribute('aria-label', t('ui.showMoreContextItemsFormat', { p0: hiddenCount }));
   }
 
   destroy(): void {
@@ -222,7 +224,7 @@ export class ComposerContextTray {
         text: '\u00D7',
         attr: {
           type: 'button',
-          'aria-label': `Remove ${item.ariaLabel ?? item.label}`,
+          'aria-label': t('ui.removeFormat', { p0: item.ariaLabel ?? item.label }),
         },
       });
       removeButton.addEventListener('click', item.onRemove);
diff --git a/src/features/chat/ui/ImageContext.ts b/src/features/chat/ui/ImageContext.ts
index 9b14574..6c5e198 100644
--- a/src/features/chat/ui/ImageContext.ts
+++ b/src/features/chat/ui/ImageContext.ts
@@ -1,6 +1,8 @@
 import { Notice } from 'obsidian';
 import * as path from 'path';
 
+import { t } from '@/i18n/i18n';
+
 import type { ImageAttachment, ImageMediaType } from '../../../core/types';
 import { ComposerContextTray } from './ComposerContextTray';
 import { ImagePreviewModal } from './ImagePreviewModal';
@@ -143,7 +145,7 @@ export class ImageContextManager {
     line.setAttribute('y1', '3');
     line.setAttribute('x2', '12');
     line.setAttribute('y2', '15');
-    dropContent.createSpan({ text: 'Drop image here' });
+    dropContent.createSpan({ text: t('ui.dropImageHere') });
 
     inputWrapper.addEventListener('dragenter', this.dragEnterHandler);
     inputWrapper.addEventListener('dragover', this.dragOverHandler);
@@ -240,18 +242,18 @@ export class ImageContextManager {
   private async addImageFromFile(file: File, source: 'paste' | 'drop'): Promise<boolean> {
     if (this.destroyed) return false;
     if (!this.enabled) {
-      new Notice('Image attachments are not supported by this provider.');
+      new Notice(t('ui.imageAttachmentsAreNotSupportedByThisProvider'));
       return false;
     }
 
     if (file.size > MAX_IMAGE_SIZE) {
-      this.notifyImageError(`Image exceeds ${this.formatSize(MAX_IMAGE_SIZE)} limit.`);
+      this.notifyImageError(t('ui.imageExceedsLimitFormat', { p0: this.formatSize(MAX_IMAGE_SIZE) }));
       return false;
     }
 
     const mediaType = this.getMediaType(file.name) || (file.type as ImageMediaType);
     if (!mediaType) {
-      this.notifyImageError('Unsupported image type.');
+      this.notifyImageError(t('ui.unsupportedImageType'));
       return false;
     }
 
@@ -275,7 +277,7 @@ export class ImageContextManager {
       return true;
     } catch (error) {
       if (this.destroyed) return false;
-      this.notifyImageError('Failed to attach image.', error);
+      this.notifyImageError(t('ui.failedToAttachImage'), error);
       return false;
     }
   }
@@ -300,9 +302,9 @@ export class ImageContextManager {
     this.contextTray.setItems('images', images.map(([id, image], index) => ({
       id,
       kind: 'image' as const,
-      label: images.length === 1 ? 'Image' : `Image ${index + 1}`,
+      label: images.length === 1 ? t('ui.image') : t('ui.imageFormat', { p0: index + 1 }),
       title: `${image.name} · ${this.formatSize(image.size)}`,
-      ariaLabel: `Image attachment: ${image.name}`,
+      ariaLabel: t('ui.imageAttachmentFormat', { p0: image.name }),
       onActivate: () => this.showFullImage(image),
       onRemove: () => {
         this.attachedImages.delete(id);
@@ -334,9 +336,9 @@ export class ImageContextManager {
     let userMessage = message;
     if (error instanceof Error) {
       if (error.message.includes('ENOENT') || error.message.includes('no such file')) {
-        userMessage = `${message} (File not found)`;
+        userMessage = t('ui.fileNotFoundFormat', { p0: message });
       } else if (error.message.includes('EACCES') || error.message.includes('permission denied')) {
-        userMessage = `${message} (Permission denied)`;
+        userMessage = t('ui.permissionDeniedFormat', { p0: message });
       }
     }
     new Notice(userMessage);
diff --git a/src/features/chat/ui/ImagePreviewModal.ts b/src/features/chat/ui/ImagePreviewModal.ts
index b3f0fd2..ccad3f8 100644
--- a/src/features/chat/ui/ImagePreviewModal.ts
+++ b/src/features/chat/ui/ImagePreviewModal.ts
@@ -1,4 +1,5 @@
 import type { ImageAttachment } from '@/core/types';
+import { t } from '@/i18n/i18n';
 
 function getFocusableActiveElement(ownerDocument: Document): HTMLElement | null {
   const activeElement = ownerDocument.activeElement;
@@ -20,7 +21,7 @@ export class ImagePreviewModal {
     const modal = overlay.createDiv({ cls: 'claudian-image-modal' });
     modal.setAttribute('role', 'dialog');
     modal.setAttribute('aria-modal', 'true');
-    modal.setAttribute('aria-label', `Image preview: ${image.name}`);
+    modal.setAttribute('aria-label', t('ui.imagePreviewFormat', { p0: image.name }));
 
     modal.createEl('img', {
       attr: {
@@ -32,7 +33,7 @@ export class ImagePreviewModal {
     const closeButton = modal.createEl('button', {
       cls: 'claudian-image-modal-close',
       attr: {
-        'aria-label': 'Close image preview',
+        'aria-label': t('ui.closeImagePreview'),
         type: 'button',
       },
     });
diff --git a/src/features/chat/ui/InputToolbar.ts b/src/features/chat/ui/InputToolbar.ts
index e7ebf9b..2fd2e9c 100644
--- a/src/features/chat/ui/InputToolbar.ts
+++ b/src/features/chat/ui/InputToolbar.ts
@@ -2,6 +2,8 @@ import { Notice, setIcon } from 'obsidian';
 import * as os from 'os';
 import * as path from 'path';
 
+import { t } from '@/i18n/i18n';
+
 import type {
   ProviderCapabilities,
   ProviderChatUIConfig,
@@ -116,7 +118,7 @@ export class ModelSelector {
       });
     }
     const labelEl = this.buttonEl.createSpan({ cls: 'claudian-model-label' });
-    labelEl.setText(displayModel?.label || 'Unknown');
+    labelEl.setText(displayModel?.label || t('ui.unknown'));
   }
 
   renderOptions() {
@@ -160,7 +162,7 @@ export class ModelSelector {
           await this.callbacks.onModelChange(model.value);
           this.updateDisplay();
           this.renderOptions();
-        }, 'Failed to change model');
+        }, t('ui.failedToChangeModel'));
       });
     }
   }
@@ -189,7 +191,7 @@ export class ModeSelector {
     this.toggleEl = this.container.createDiv({ cls: 'claudian-toggle-switch' });
 
     this.toggleEl.addEventListener('click', () => {
-      runToolbarAction(() => this.toggle(), 'Failed to change mode');
+      runToolbarAction(() => this.toggle(), t('ui.failedToChangeMode'));
     });
 
     this.updateDisplay();
@@ -276,13 +278,13 @@ export class ThinkingBudgetSelector {
     // Effort selector (for adaptive thinking models)
     this.effortEl = this.container.createDiv({ cls: 'claudian-thinking-effort' });
     const effortLabel = this.effortEl.createSpan({ cls: 'claudian-thinking-label-text' });
-    effortLabel.setText('Effort:');
+    effortLabel.setText(t('ui.effort'));
     this.effortGearsEl = this.effortEl.createDiv({ cls: 'claudian-thinking-gears' });
 
     // Legacy budget selector (for custom models)
     this.budgetEl = this.container.createDiv({ cls: 'claudian-thinking-budget' });
     const budgetLabel = this.budgetEl.createSpan({ cls: 'claudian-thinking-label-text' });
-    budgetLabel.setText('Thinking:');
+    budgetLabel.setText(t('ui.thinking'));
     this.budgetGearsEl = this.budgetEl.createDiv({ cls: 'claudian-thinking-gears' });
 
     this.updateDisplay();
@@ -300,7 +302,7 @@ export class ThinkingBudgetSelector {
     const currentInfo = options.find(e => e.value === currentEffort);
 
     const currentEl = this.effortGearsEl.createDiv({ cls: 'claudian-thinking-current' });
-    currentEl.setText(currentInfo?.label || options[0]?.label || 'High');
+    currentEl.setText(currentInfo?.label || options[0]?.label || t('settings.codexSubagents.reasoningEffort.high'));
 
     const optionsEl = this.effortGearsEl.createDiv({ cls: 'claudian-thinking-options' });
 
@@ -320,7 +322,7 @@ export class ThinkingBudgetSelector {
         runToolbarAction(async () => {
           await this.callbacks.onEffortLevelChange(effort.value);
           this.updateDisplay();
-        }, 'Failed to change effort level');
+        }, t('ui.failedToChangeEffortLevel'));
       });
     }
   }
@@ -337,7 +339,7 @@ export class ThinkingBudgetSelector {
     const currentBudgetInfo = options.find(b => b.value === currentBudget);
 
     const currentEl = this.budgetGearsEl.createDiv({ cls: 'claudian-thinking-current' });
-    currentEl.setText(currentBudgetInfo?.label || options[0]?.label || 'Off');
+    currentEl.setText(currentBudgetInfo?.label || options[0]?.label || t('settings.codex.reasoningSummary.off'));
 
     const optionsEl = this.budgetGearsEl.createDiv({ cls: 'claudian-thinking-options' });
 
@@ -345,7 +347,7 @@ export class ThinkingBudgetSelector {
       const gearEl = optionsEl.createDiv({ cls: 'claudian-thinking-gear' });
       gearEl.setText(budget.label);
       const tokens = budget.tokens ?? 0;
-      gearEl.setAttribute('title', tokens > 0 ? `${tokens.toLocaleString()} tokens` : 'Disabled');
+      gearEl.setAttribute('title', tokens > 0 ? `${tokens.toLocaleString()} tokens` : t('common.disabled'));
 
       if (budget.value === currentBudget) {
         gearEl.addClass('selected');
@@ -356,7 +358,7 @@ export class ThinkingBudgetSelector {
         runToolbarAction(async () => {
           await this.callbacks.onThinkingBudgetChange(budget.value);
           this.updateDisplay();
-        }, 'Failed to change thinking budget');
+        }, t('ui.failedToChangeThinkingBudget'));
       });
     }
   }
@@ -427,7 +429,7 @@ export class PermissionToggle {
     this.updateDisplay();
 
     this.toggleEl.addEventListener('click', () => {
-      runToolbarAction(() => this.toggle(), 'Failed to change permission mode');
+      runToolbarAction(() => this.toggle(), t('ui.failedToChangePermissionMode2'));
     });
   }
 
@@ -449,7 +451,7 @@ export class PermissionToggle {
     this.container.removeClass('claudian-hidden');
     const mode = this.callbacks.getSettings().permissionMode;
     const planValue = toggleConfig.planValue;
-    const planLabel = toggleConfig.planLabel ?? 'PLAN';
+    const planLabel = toggleConfig.planLabel ?? t('ui.plan');
     const canShowPlan = Boolean(planValue) && capabilities.supportsPlanMode;
 
     if (canShowPlan && planValue && mode === planValue) {
@@ -506,7 +508,7 @@ export class ServiceTierToggle {
     this.buttonEl.addEventListener('click', () => {
       runToolbarAction(async () => {
         await this.toggle();
-      }, 'Failed to change service tier');
+      }, t('ui.failedToChangeServiceTier'));
     });
   }
 
@@ -534,7 +536,7 @@ export class ServiceTierToggle {
     const currentLabel = toggleConfig.isActive
       ? toggleConfig.activeLabel
       : toggleConfig.inactiveLabel;
-    this.container.setAttribute('title', `Fast mode: ${currentLabel}`);
+    this.container.setAttribute('title', t('ui.fastModeFormat', { p0: currentLabel }));
   }
 
   async toggle(): Promise<boolean> {
@@ -604,7 +606,7 @@ export class ExternalContextSelector {
     // If invalid paths were removed, notify user and save updated list
     if (invalidPaths.length > 0) {
       const pathNames = invalidPaths.map(p => this.shortenPath(p)).join(', ');
-      new Notice(`Removed ${invalidPaths.length} invalid external context path(s): ${pathNames}`, 5000);
+      new Notice(t('ui.removedInvalidExternalContextPathsFormat', { p0: invalidPaths.length, p1: pathNames }), 5000);
       this.onPersistenceChangeCallback?.([...this.persistentPaths]);
     }
   }
@@ -615,7 +617,7 @@ export class ExternalContextSelector {
     } else {
       // Validate path still exists before persisting
       if (!isValidDirectoryPath(path)) {
-        new Notice(`Cannot persist "${this.shortenPath(path)}" - directory no longer exists`, 4000);
+        new Notice(t('ui.cannotPersistDirectoryNoLongerExistsFormat', { p0: this.shortenPath(path) }), 4000);
         return;
       }
       this.persistentPaths.add(path);
@@ -668,7 +670,7 @@ export class ExternalContextSelector {
   addExternalContext(pathInput: string): AddExternalContextResult {
     const trimmed = pathInput?.trim();
     if (!trimmed) {
-      return { success: false, error: 'No path provided. Usage: /add-dir /absolute/path' };
+      return { success: false, error: t('ui.noPathProvidedUsageAdddirAbsolutepath') };
     }
 
     // Strip surrounding quotes if present (e.g., "/path/with spaces")
@@ -683,7 +685,7 @@ export class ExternalContextSelector {
     const normalizedPath = normalizePathForFilesystem(expandedPath);
 
     if (!path.isAbsolute(normalizedPath)) {
-      return { success: false, error: 'Path must be absolute. Usage: /add-dir /absolute/path' };
+      return { success: false, error: t('ui.pathMustBeAbsoluteUsageAdddirAbsolutepath') };
     }
 
     // Validate path exists and is a directory with specific error messages
@@ -694,7 +696,7 @@ export class ExternalContextSelector {
 
     // Check for duplicate (normalized comparison for cross-platform support)
     if (isDuplicatePath(normalizedPath, this.externalContextPaths)) {
-      return { success: false, error: 'This folder is already added as an external context.' };
+      return { success: false, error: t('ui.thisFolderIsAlreadyAddedAsAnExternalContext') };
     }
 
     // Check for nested/overlapping paths
@@ -761,7 +763,7 @@ export class ExternalContextSelector {
       }
       const result = await remote.dialog.showOpenDialog({
         properties: ['openDirectory'],
-        title: 'Select External Context',
+        title: t('ui.selectExternalContext'),
       });
 
       if (!result.canceled && result.filePaths.length > 0) {
@@ -769,7 +771,7 @@ export class ExternalContextSelector {
 
         // Check for duplicate (normalized comparison for cross-platform support)
         if (isDuplicatePath(selectedPath, this.externalContextPaths)) {
-          new Notice('This folder is already added as an external context.', 3000);
+          new Notice(t('ui.thisFolderIsAlreadyAddedAsAnExternalContext'), 3000);
           return;
         }
 
@@ -786,7 +788,7 @@ export class ExternalContextSelector {
         this.renderDropdown();
       }
     } catch {
-      new Notice('Unable to open folder picker.', 5000);
+      new Notice(t('ui.unableToOpenFolderPicker'), 5000);
     }
   }
 
@@ -795,8 +797,8 @@ export class ExternalContextSelector {
     const shortNew = this.shortenPath(newPath);
     const shortExisting = this.shortenPath(conflict.path);
     return conflict.type === 'parent'
-      ? `Cannot add "${shortNew}" - it's inside existing path "${shortExisting}"`
-      : `Cannot add "${shortNew}" - it contains existing path "${shortExisting}"`;
+      ? t('ui.cannotAddItsInsideExistingPathFormat', { p0: shortNew, p1: shortExisting })
+      : t('ui.cannotAddItContainsExistingPathFormat', { p0: shortNew, p1: shortExisting });
   }
 
   private renderDropdown() {
@@ -806,14 +808,14 @@ export class ExternalContextSelector {
 
     // Header
     const headerEl = this.dropdownEl.createDiv({ cls: 'claudian-external-context-header' });
-    headerEl.setText('External contexts');
+    headerEl.setText(t('ui.externalContexts'));
 
     // Path list
     const listEl = this.dropdownEl.createDiv({ cls: 'claudian-external-context-list' });
 
     if (this.externalContextPaths.length === 0) {
       const emptyEl = listEl.createDiv({ cls: 'claudian-external-context-empty' });
-      emptyEl.setText('Click folder icon to add');
+      emptyEl.setText(t('ui.clickFolderIconToAdd'));
     } else {
       for (const pathStr of this.externalContextPaths) {
         const itemEl = listEl.createDiv({ cls: 'claudian-external-context-item' });
@@ -831,7 +833,7 @@ export class ExternalContextSelector {
           lockBtn.addClass('locked');
         }
         setIcon(lockBtn, isPersistent ? 'lock' : 'unlock');
-        lockBtn.setAttribute('title', isPersistent ? 'Persistent (click to make session-only)' : 'Session-only (click to persist)');
+        lockBtn.setAttribute('title', isPersistent ? t('ui.persistentClickToMakeSessiononly') : t('ui.sessiononlyClickToPersist'));
         lockBtn.addEventListener('click', (e) => {
           e.stopPropagation();
           this.togglePersistence(pathStr);
@@ -839,7 +841,7 @@ export class ExternalContextSelector {
 
         const removeBtn = itemEl.createSpan({ cls: 'claudian-external-context-remove' });
         setIcon(removeBtn, 'x');
-        removeBtn.setAttribute('title', 'Remove path');
+        removeBtn.setAttribute('title', t('ui.removePath'));
         removeBtn.addEventListener('click', (e) => {
           e.stopPropagation();
           this.removePath(pathStr);
@@ -879,7 +881,7 @@ export class ExternalContextSelector {
 
     if (count > 0) {
       this.iconEl.addClass('active');
-      this.iconEl.setAttribute('title', `${count} external context${count > 1 ? 's' : ''} (click to add more)`);
+      this.iconEl.setAttribute('title', t('ui.externalContextClickToAddMoreFormat', { p0: count, p1: '' }));
 
       // Show badge only when more than 1 path
       if (count > 1) {
@@ -890,7 +892,7 @@ export class ExternalContextSelector {
       }
     } else {
       this.iconEl.removeClass('active');
-      this.iconEl.setAttribute('title', 'Add external contexts (click)');
+      this.iconEl.setAttribute('title', t('ui.addExternalContextsClick'));
       this.badgeEl.removeClass('visible');
     }
   }
@@ -905,7 +907,7 @@ export class ContextUsageMeter {
   constructor(parentEl: HTMLElement) {
     this.container = parentEl.createDiv({ cls: 'claudian-context-meter' });
     this.container.setAttribute('role', 'progressbar');
-    this.container.setAttribute('aria-label', 'Context usage');
+    this.container.setAttribute('aria-label', t('ui.contextUsage'));
     this.container.setAttribute('aria-valuemin', '0');
     this.container.setAttribute('aria-valuemax', '100');
     this.render();
@@ -994,7 +996,7 @@ export class ContextUsageMeter {
     // Set tooltip with detailed usage
     let tooltip = `${this.formatTokens(usage.contextTokens)} / ${this.formatTokens(usage.contextWindow)}`;
     if (usage.percentage > 80) {
-      tooltip += ' (Approaching limit, run `/compact` to continue)';
+      tooltip += t('ui.approachingLimitRunCompactToContinue');
     }
     this.container.setAttribute('data-tooltip', tooltip);
     this.container.setAttribute('aria-valuenow', String(usage.percentage));
diff --git a/src/features/chat/ui/InstructionModeManager.ts b/src/features/chat/ui/InstructionModeManager.ts
index 107c097..61b93cc 100644
--- a/src/features/chat/ui/InstructionModeManager.ts
+++ b/src/features/chat/ui/InstructionModeManager.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 export interface InstructionModeCallbacks {
   onSubmit: (rawInstruction: string) => Promise<void>;
   getInputWrapper: () => HTMLElement | null;
@@ -9,7 +11,7 @@ export interface InstructionModeState {
   rawInstruction: string;
 }
 
-const INSTRUCTION_MODE_PLACEHOLDER = 'Save in custom system prompt';
+const INSTRUCTION_MODE_PLACEHOLDER = () => t('ui.saveInCustomSystemPrompt');
 
 export class InstructionModeManager {
   private inputEl: HTMLTextAreaElement;
@@ -52,7 +54,7 @@ export class InstructionModeManager {
 
     wrapper.addClass('claudian-input-instruction-mode');
     this.state = { active: true, rawInstruction: '' };
-    this.inputEl.placeholder = INSTRUCTION_MODE_PLACEHOLDER;
+    this.inputEl.placeholder = INSTRUCTION_MODE_PLACEHOLDER();
     this.callbacks.onActiveChange?.(true);
     this.inputEl.focus?.();
     return true;
diff --git a/src/features/chat/ui/NavigationSidebar.ts b/src/features/chat/ui/NavigationSidebar.ts
index 6e676da..dc9341b 100644
--- a/src/features/chat/ui/NavigationSidebar.ts
+++ b/src/features/chat/ui/NavigationSidebar.ts
@@ -1,5 +1,7 @@
 import { setIcon } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import {
   cancelScheduledAnimationFrame,
   scheduleAnimationFrame,
@@ -36,11 +38,11 @@ export class NavigationSidebar {
 
     try {
       // Create buttons
-      this.topBtn = this.createButton('claudian-nav-btn-top', 'chevrons-up', 'Scroll to top');
-      this.prevBtn = this.createButton('claudian-nav-btn-prev', 'chevron-up', 'Previous message');
-      this.tocBtn = this.createButton('claudian-nav-btn-toc', 'list-tree', 'Conversation directory');
-      this.nextBtn = this.createButton('claudian-nav-btn-next', 'chevron-down', 'Next message');
-      this.bottomBtn = this.createButton('claudian-nav-btn-bottom', 'chevrons-down', 'Scroll to bottom');
+      this.topBtn = this.createButton('claudian-nav-btn-top', 'chevrons-up', t('ui.scrollToTop'));
+      this.prevBtn = this.createButton('claudian-nav-btn-prev', 'chevron-up', t('ui.previousMessage'));
+      this.tocBtn = this.createButton('claudian-nav-btn-toc', 'list-tree', t('ui.conversationDirectory'));
+      this.nextBtn = this.createButton('claudian-nav-btn-next', 'chevron-down', t('ui.nextMessage'));
+      this.bottomBtn = this.createButton('claudian-nav-btn-bottom', 'chevrons-down', t('ui.scrollToBottom'));
 
       this.setupEventListeners();
       this.applyVisibility();
@@ -198,13 +200,13 @@ export class NavigationSidebar {
     const entries = this.getDirectoryEntries();
     this.closeDirectory();
     this.tocPopover = this.parentEl.createDiv({ cls: 'claudian-nav-toc-popover' });
-    this.tocPopover.createDiv({ cls: 'claudian-nav-toc-title', text: 'Conversation directory' });
+    this.tocPopover.createDiv({ cls: 'claudian-nav-toc-title', text: t('ui.conversationDirectory') });
     const listEl = this.tocPopover.createDiv({ cls: 'claudian-nav-toc-list' });
 
     if (entries.length === 0) {
       listEl.createDiv({
         cls: 'claudian-nav-toc-empty',
-        text: 'No user prompts in this conversation',
+        text: t('ui.noUserPromptsInThisConversation'),
       });
       return;
     }
diff --git a/src/features/chat/ui/StatusPanel.ts b/src/features/chat/ui/StatusPanel.ts
index e7bdea2..037e3f4 100644
--- a/src/features/chat/ui/StatusPanel.ts
+++ b/src/features/chat/ui/StatusPanel.ts
@@ -229,7 +229,7 @@ export class StatusPanel {
     // Label
     this.todoHeaderEl.createSpan({
       cls: 'claudian-status-panel-label',
-      text: `Tasks (${completedCount}/${totalCount})`,
+      text: t('ui.tasksFormat2', { p0: completedCount, p1: totalCount }),
     });
 
     // Collapsed-only elements: status indicator and current task preview
@@ -293,10 +293,10 @@ export class StatusPanel {
   private updateTodoAriaLabel(completedCount: number, totalCount: number): void {
     if (!this.todoHeaderEl) return;
 
-    const action = this.isTodoExpanded ? 'Collapse' : 'Expand';
+    const action = this.isTodoExpanded ? t('ui.collapse') : t('ui.expand');
     this.todoHeaderEl.setAttribute(
       'aria-label',
-      `${action} task list - ${completedCount} of ${totalCount} completed`
+      t('ui.taskListOfCompletedFormat', { p0: action, p1: completedCount, p2: totalCount })
     );
     this.todoHeaderEl.setAttribute('aria-expanded', String(this.isTodoExpanded));
   }
diff --git a/src/features/inline-edit/ui/InlineEditModal.ts b/src/features/inline-edit/ui/InlineEditModal.ts
index ff9ae5c..8d9f890 100644
--- a/src/features/inline-edit/ui/InlineEditModal.ts
+++ b/src/features/inline-edit/ui/InlineEditModal.ts
@@ -4,6 +4,8 @@ import { Decoration, EditorView, WidgetType } from '@codemirror/view';
 import type { App, Component, Editor, MarkdownView } from 'obsidian';
 import { Notice } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import { getHiddenProviderCommandSet } from '../../../core/providers/commands/hiddenCommands';
 import { normalizeProviderCommandDiscoveryItems } from '../../../core/providers/commands/ProviderCommandDiscoveryResult';
 import { ProviderCommandDiscoveryStore } from '../../../core/providers/commands/ProviderCommandDiscoveryStore';
@@ -330,7 +332,7 @@ export class InlineEditModal {
     }
 
     if (!editorView) {
-      new Notice('Inline edit unavailable: could not access the active editor. Try reopening the note.');
+      new Notice(t('ui.inlineEditUnavailableCouldNotAccessTheActiveEditorTryReopeningTheNote'));
       return { decision: 'reject' };
     }
 
@@ -342,7 +344,7 @@ export class InlineEditModal {
         'inline-edit',
       );
     } catch {
-      new Notice(`Inline edit unavailable: failed to initialize the ${providerContext.providerId} provider.`);
+      new Notice(t('ui.inlineEditUnavailableFailedToInitializeTheProviderFormat', { p0: providerContext.providerId }));
       return { decision: 'reject' };
     }
 
@@ -416,7 +418,7 @@ export class InlineEditSession {
     this.resolvedProviderId = providerId;
     this.mentionDataProvider = new VaultMentionDataProvider(this.app, {
       onFileLoadError: () => {
-        new Notice('Failed to load vault files. Vault @-mentions may be unavailable.');
+        new Notice(t('ui.failedToLoadVaultFilesVaultMentionsMayBeUnavailable'));
       },
     });
     this.mentionDataProvider.initializeInBackground();
@@ -537,7 +539,7 @@ export class InlineEditSession {
       cls: 'claudian-inline-input',
       attr: {
         type: 'text',
-        placeholder: this.mode === 'cursor' ? 'Insert instructions...' : 'Edit instructions...',
+        placeholder: this.mode === 'cursor' ? t('ui.insertInstructions') : t('ui.editInstructions'),
         spellcheck: 'false',
       },
     });
@@ -591,9 +593,9 @@ export class InlineEditSession {
 
     const actionsEl = previewEl.createDiv({ cls: 'claudian-inline-preview-actions' });
     actionsEl.setAttribute('role', 'toolbar');
-    actionsEl.setAttribute('aria-label', 'Inline edit actions');
-    actionsEl.appendChild(this.createPreviewActionButton('Reject', 'reject', () => this.reject()));
-    actionsEl.appendChild(this.createPreviewActionButton('Accept', 'accept', () => this.accept()));
+    actionsEl.setAttribute('aria-label', t('ui.inlineEditActions'));
+    actionsEl.appendChild(this.createPreviewActionButton(t('ui.reject'), 'reject', () => this.reject()));
+    actionsEl.appendChild(this.createPreviewActionButton(t('ui.accept'), 'accept', () => this.accept()));
 
     void this.renderMarkdownDiffPreview(bodyEl, diffOps);
     return previewEl;
@@ -609,8 +611,8 @@ export class InlineEditSession {
       text: label,
       attr: {
         type: 'button',
-        'aria-label': `${label} inline edit`,
-        title: variant === 'accept' ? 'Accept (enter)' : 'Reject (esc)',
+        'aria-label': t('ui.inlineEditFormat', { p0: label }),
+        title: variant === 'accept' ? t('ui.acceptEnter') : t('ui.rejectEsc'),
       },
     });
     button.addEventListener('click', (event: MouseEvent) => {
@@ -708,7 +710,7 @@ export class InlineEditSession {
       }
     } catch (error) {
       if (this.isGenerationActive(generation)) {
-        this.handleError(error instanceof Error ? error.message : 'Error - try again');
+        this.handleError(error instanceof Error ? error.message : t('ui.errorTryAgain'));
       }
       return;
     } finally {
@@ -737,17 +739,17 @@ export class InlineEditSession {
         this.isConversing = true;
         this.inputEl.disabled = false;
         this.inputEl.value = '';
-        this.inputEl.placeholder = 'Reply to continue...';
+        this.inputEl.placeholder = t('ui.replyToContinue');
         this.inputEl.focus();
       } else {
-        this.handleError('No response from agent');
+        this.handleError(t('ui.noResponseFromAgent'));
       }
     } else {
       if (result.resetRequired) {
         this.isConversing = false;
         this.inlineEditService.resetConversation();
       }
-      this.handleError(result.error || 'Error - try again');
+      this.handleError(result.error || t('ui.errorTryAgain'));
     }
   }
 
@@ -945,7 +947,7 @@ export class InlineEditSession {
     if (this.settled) {
       return;
     }
-    new Notice('Inline edit was not applied because the source document or selection changed.');
+    new Notice(t('ui.inlineEditWasNotAppliedBecauseTheSourceDocumentOrSelectionChanged'));
     this.settled = true;
     this.cleanup();
     this.focusEditor();
@@ -994,7 +996,7 @@ export class InlineEditSession {
       const vaultPath = getVaultPath(this.app);
       return normalizePathForVaultUtil(rawPath, vaultPath);
     } catch {
-      new Notice('Failed to attach file: invalid path');
+      new Notice(t('ui.failedToAttachFileInvalidPath'));
       return null;
     }
   }
diff --git a/src/features/settings/ClaudianSettings.ts b/src/features/settings/ClaudianSettings.ts
index 48a12c0..6181eb2 100644
--- a/src/features/settings/ClaudianSettings.ts
+++ b/src/features/settings/ClaudianSettings.ts
@@ -223,7 +223,7 @@ export class ClaudianSettingTab extends PluginSettingTab {
         providerContent.empty();
         const renderer = ProviderWorkspaceRegistry.getSettingsTabRenderer(providerId);
         if (!renderer) {
-          providerContent.createDiv({ text: 'Provider settings are unavailable.' });
+          providerContent.createDiv({ text: t('ui.providerSettingsAreUnavailable') });
           return;
         }
         renderer.render(providerContent, {
@@ -247,7 +247,7 @@ export class ClaudianSettingTab extends PluginSettingTab {
         if (renderGeneration !== this.renderGeneration) return;
         renderedProviderIds.delete(providerId);
         providerContent.empty();
-        const message = error instanceof Error ? error.message : 'Unknown error';
+        const message = error instanceof Error ? error.message : t('ui.unknownError');
         providerContent.createDiv({
           cls: 'claudian-setting-validation claudian-setting-validation-error',
           text: `Could not load provider settings: ${message}`,
@@ -686,8 +686,8 @@ export class ClaudianSettingTab extends PluginSettingTab {
       plugin: this.plugin.providerHost,
       scope: 'shared',
       heading: t('settings.environment'),
-      name: 'Shared environment',
-      desc: 'Provider-neutral runtime variables shared across all providers. Use this for PATH, proxy, cert, and temp variables.',
+      name: t('ui.sharedEnvironment'),
+      desc: t('ui.providerneutralRuntimeVariablesSharedAcrossAllProvidersUseThisForPATHProxyCertAndTempVariables'),
       placeholder: 'PATH=/opt/homebrew/bin:/usr/local/bin\nHTTPS_PROXY=http://proxy.example.com:8080\nSSL_CERT_FILE=/path/to/cert.pem',
     });
 
@@ -710,14 +710,14 @@ export class ClaudianSettingTab extends PluginSettingTab {
               const reconciled = await this.plugin.warmExecutionPool.reconcileLimit();
               if (!reconciled) {
                 new Notice(
-                  'The new concurrent running session limit will apply as busy sessions become idle.',
+                  t('ui.theNewConcurrentRunningSessionLimitWillApplyAsBusySessionsBecomeIdle'),
                 );
               }
             } catch (error) {
               new Notice(
                 error instanceof Error
                   ? error.message
-                  : 'Failed to release excess warm agent processes.',
+                  : t('ui.failedToReleaseExcessWarmAgentProcesses'),
               );
             }
           });
@@ -916,7 +916,7 @@ export class ClaudianSettingTab extends PluginSettingTab {
         value: currentAlias,
       });
       aliasInputEl.setAttribute('aria-label', `Alias for ${modelId}`);
-      aliasInputEl.title = 'Custom label shown in the model selector. Leave empty to use the default.';
+      aliasInputEl.title = t('ui.customLabelShownInTheModelSelectorLeaveEmptyToUseTheDefault');
 
       const inputEl = inputWrapper.createEl('input', {
         type: 'text',
diff --git a/src/features/settings/keyboardNavigation.ts b/src/features/settings/keyboardNavigation.ts
index b294c73..60e5443 100644
--- a/src/features/settings/keyboardNavigation.ts
+++ b/src/features/settings/keyboardNavigation.ts
@@ -1,4 +1,5 @@
 import type { KeyboardNavigationSettings } from '@/core/types/settings';
+import { t } from '@/i18n/i18n';
 
 const NAV_ACTIONS = ['scrollUp', 'scrollDown', 'focusInput'] as const;
 type NavAction = (typeof NAV_ACTIONS)[number];
@@ -24,7 +25,7 @@ export const parseNavMappings = (
 
     const parts = line.split(/\s+/);
     if (parts.length !== 3 || parts[0] !== 'map') {
-      return { error: 'Each line must follow "map <key> <action>"' };
+      return { error: t('ui.eachLineMustFollowMapKeyAction') };
     }
 
     const key = parts[1];
@@ -40,7 +41,7 @@ export const parseNavMappings = (
 
     const normalizedKey = key.toLowerCase();
     if (usedKeys.has(normalizedKey)) {
-      return { error: 'Navigation keys must be unique' };
+      return { error: t('ui.navigationKeysMustBeUnique') };
     }
 
     if (parsed[action]) {
diff --git a/src/i18n/locales/en.json b/src/i18n/locales/en.json
index 1fa2c1b..b964a29 100644
--- a/src/i18n/locales/en.json
+++ b/src/i18n/locales/en.json
@@ -886,5 +886,567 @@
       "header": "Codex Subagents",
       "noAgents": "No Codex subagents in vault. Click + to create one."
     }
+  },
+  "ui": {
+    "resizeConversationSessions": "Resize conversation sessions",
+    "newConversation": "New conversation",
+    "chatHistory": "Chat history",
+    "failedToCreateTab": "Failed to create tab",
+    "failedToCreateConversation": "Failed to create conversation",
+    "failedToStartANewConversation": "Failed to start a new conversation",
+    "newConversationInputIsUnavailable": "New conversation input is unavailable",
+    "failedToStartTheApprovedPlanInANewConversation": "Failed to start the approved plan in a new conversation",
+    "failedToSwitchTab": "Failed to switch tab",
+    "failedToCloseTab": "Failed to close tab",
+    "archived": "Archived",
+    "searchArchivedSessions": "Search archived sessions",
+    "searchSessions": "Search sessions",
+    "search": "Search",
+    "archive": "Archive",
+    "expandAllGroups": "Expand all groups",
+    "collapseAllGroups": "Collapse all groups",
+    "sessionOptions": "Session options",
+    "chatTabsAreUnavailable": "Chat tabs are unavailable",
+    "linkedContentIsNoLongerAvailable": "Linked content is no longer available",
+    "failedToCreateAProvisionalChatTab": "Failed to create a provisional chat tab",
+    "runningSessionsCannotBeArchived": "Running sessions cannot be archived",
+    "failedToCloseTheSessionBeforeArchiving": "Failed to close the session before archiving",
+    "organizeSessions": "Organize sessions",
+    "inOneList": "In one list",
+    "byLinkedContent": "By linked content",
+    "sortSessionsBy": "Sort sessions by",
+    "lastActivity": "Last activity",
+    "created": "Created",
+    "failedToUpdateSessionOrganization": "Failed to update session organization",
+    "failedToUpdateSessionSorting": "Failed to update session sorting",
+    "failedToCloseTheProvisionalSessionPreview": "Failed to close the provisional session preview",
+    "failedToChangePermissionMode": "Failed to change permission mode.",
+    "failedToInitializeAgentService": "Failed to initialize agent service",
+    "agentServiceNotAvailable": "Agent service not available",
+    "agentExecutionNotAvailable": "Agent execution not available",
+    "conversationChangedWhileRewinding": "Conversation changed while rewinding.",
+    "unknownError": "Unknown error",
+    "failedToSave": "Failed to save",
+    "pinned": "Pinned",
+    "noMatchingSessions": "No matching sessions",
+    "noConversations": "No conversations",
+    "missing": "Missing",
+    "failedToStartAChatForThisLinkedContent": "Failed to start a chat for this Linked content",
+    "unpinLinkedContent": "Unpin Linked content",
+    "pinLinkedContent": "Pin Linked content",
+    "failedToUnpinLinkedContent": "Failed to unpin Linked content",
+    "failedToPinLinkedContent": "Failed to pin Linked content",
+    "archiveAllSessions": "Archive all sessions",
+    "failedToArchiveLinkedContentSessions": "Failed to archive Linked content sessions",
+    "failedToLoadConversation": "Failed to load conversation",
+    "generatingTitle": "Generating title...",
+    "regenerateTitle": "Regenerate title",
+    "failedToRegenerateResponse": "Failed to regenerate response",
+    "openInNewTab": "Open in new tab",
+    "failedToDeleteConversation": "Failed to delete conversation",
+    "assignToThisDevice": "Assign to this device",
+    "failedToAssignSessionToThisDevice": "Failed to assign session to this device",
+    "unpin": "Unpin",
+    "pin": "Pin",
+    "failedToUnpinSession": "Failed to unpin session",
+    "failedToPinSession": "Failed to pin session",
+    "cannotArchiveARunningSession": "Cannot archive a running session",
+    "failedToArchiveSession": "Failed to archive session",
+    "restore": "Restore",
+    "failedToRestoreSession": "Failed to restore session",
+    "rename": "Rename",
+    "needsYourInput": "Needs your input",
+    "stoppedWithAnError": "Stopped with an error",
+    "lastActive": "Last active",
+    "runningInAnotherPane": "Running in another pane",
+    "openInAnotherPane": "Open in another pane",
+    "currentSession": "Current session",
+    "currentTab": "current tab",
+    "openInBackgroundTab": "Open in background tab",
+    "switchToOpenSession": "Switch to open session",
+    "failedToRenameConversation": "Failed to rename conversation",
+    "happySunday": "Happy Sunday",
+    "sundaySession": "Sunday session?",
+    "welcomeToTheWeekend": "Welcome to the weekend",
+    "happyMonday": "Happy Monday",
+    "backAtIt": "Back at it",
+    "backAtIt2": "Back at it!",
+    "happyTuesday": "Happy Tuesday",
+    "happyWednesday": "Happy Wednesday",
+    "happyThursday": "Happy Thursday",
+    "happyFriday": "Happy Friday",
+    "thatFridayFeeling": "That Friday feeling",
+    "happySaturday": "Happy Saturday",
+    "happySaturday2": "Happy Saturday!",
+    "goodMorning": "Good morning",
+    "coffeeAndClaudianTime": "Coffee and Claudian time?",
+    "goodAfternoon": "Good afternoon",
+    "heyThere": "Hey there",
+    "howsItGoing": "How's it going",
+    "goodEvening": "Good evening",
+    "evening": "Evening",
+    "howWasYourDay": "How was your day",
+    "helloNightOwl": "Hello, night owl",
+    "hiHowAreYou": "Hi, how are you?",
+    "welcomeBack": "Welcome back",
+    "whatsNew": "What's new",
+    "youAreAbsolutelyRight": "You are absolutely right!",
+    "deny": "Deny",
+    "allowOnce": "Allow once",
+    "alwaysAllow": "Always allow",
+    "compacting": "Compacting...",
+    "failedToInitializeAgentExecutionPleaseTryAgain": "Failed to initialize agent execution. Please try again.",
+    "agentExecutionIsNotAvailablePleaseReloadThePlugin": "Agent execution is not available. Please reload the plugin.",
+    "theProviderSessionNoLongerExistsItsClaudianRecordWasRemovedSendAgainToStartANewSession": "The provider session no longer exists. Its Claudian record was removed; send again to start a new session.",
+    "theProviderSessionNoLongerExistsClaudianPreservedTheRecoverableHistorySendAgainToRebuildTheSession": "The provider session no longer exists. Claudian preserved the recoverable history; send again to rebuild the session.",
+    "theProviderSessionNoLongerExistsClaudianPreservedItsRecordBecauseTheRemainingHistoryCouldNotBeVerified": "The provider session no longer exists. Claudian preserved its record because the remaining history could not be verified.",
+    "theProviderSessionNoLongerExistsSendAgainToStartANewSession": "The provider session no longer exists. Send again to start a new session.",
+    "messageWasNotSentPleaseTryAgain": "Message was not sent. Please try again.",
+    "steering": "⌙ Steering: ",
+    "queued": "⌙ Queued: ",
+    "steering2": "Steering...",
+    "steerNow": "Steer Now",
+    "editQueuedMessage": "Edit queued message",
+    "discardQueuedMessage": "Discard queued message",
+    "failedToSteerTheQueuedMessageItIsStillAvailable": "Failed to steer the queued message. It is still available.",
+    "steerDeliveryCouldNotBeConfirmedTheMessageWasNotRequeuedToAvoidSendingItTwice": "Steer delivery could not be confirmed. The message was not requeued to avoid sending it twice.",
+    "failedToInitializeInstructionRefinementPleaseTryAgain": "Failed to initialize instruction refinement. Please try again.",
+    "instructionAddedToCustomSystemPrompt": "Instruction added to custom system prompt",
+    "failedToProcessResponse": "Failed to process response",
+    "failedToRefineInstruction": "Failed to refine instruction",
+    "noInstructionReceived": "No instruction received",
+    "allowThisAction": "Allow this action?",
+    "permissionRequired": "Permission required",
+    "externalContextSelectorNotAvailable": "External context selector not available.",
+    "forkIsNotSupportedByThisProvider": "Fork is not supported by this provider.",
+    "forkNotAvailable": "Fork not available.",
+    "fastModeIsNotAvailableForThisModel": "Fast mode is not available for this model.",
+    "failedToToggleFastMode": "Failed to toggle fast mode.",
+    "instructionModeIsNotAvailable": "Instruction mode is not available.",
+    "noConversationsToResume": "No conversations to resume",
+    "blocked": "Blocked",
+    "notice": "Notice",
+    "done": "DONE",
+    "taskCancelled": "Task cancelled",
+    "conversationCompacted": "Conversation compacted",
+    "rewindIsNotSupportedByThisProvider": "Rewind is not supported by this provider.",
+    "finishOrStopASessionBeforeStartingAnother": "Finish or stop a session before starting another.",
+    "claudianChatIsNotReady": "Claudian chat is not ready.",
+    "failedToAddFileToClaudian": "Failed to add file to Claudian.",
+    "addToClaudian": "Add to Claudian",
+    "none": "None",
+    "noLinkedContent": "No Linked content",
+    "linkedContent": "Linked content:",
+    "linkedContentChoices": "Linked content choices",
+    "noMatchingFilesOrFolders": "No matching files or folders",
+    "memoryUsed": "Memory used",
+    "noChanges": "No changes",
+    "enterToSelectTabArrowKeysToNavigateEscToCancel": "Enter to select · Tab/Arrow keys to navigate · Esc to cancel",
+    "enterToSelectArrowKeysToNavigateEscToCancel": "Enter to select · Arrow keys to navigate · Esc to cancel",
+    "question": "Question",
+    "option": "Option",
+    "submit": "Submit",
+    "enterSecret": "Enter secret.",
+    "typeSomething": "Type something.",
+    "reviewYourAnswers": "Review your answers",
+    "notAnswered": "Not answered",
+    "readyToSubmitYourAnswers": "Ready to submit your answers?",
+    "submitAnswers": "Submit answers",
+    "arrowKeysToNavigateEnterToSelectEscToCancel": "Arrow keys to navigate · Enter to select · Esc to cancel",
+    "planComplete": "Plan complete",
+    "requestedPermissions": "Requested permissions:",
+    "approveNewSession": "Approve (new session)",
+    "approveCurrentSession": "Approve (current session)",
+    "enterFeedbackToContinuePlanning": "Enter feedback to continue planning...",
+    "abandon": "Abandon",
+    "arrowKeysToNavigateEnterToSelectShiftTabToAbandon": "Arrow keys to navigate · Enter to select · Shift+Tab to abandon",
+    "unknownError2": "unknown error",
+    "implement": "Implement",
+    "enterFeedbackToRevisePlan": "Enter feedback to revise plan...",
+    "interrupted": "Interrupted",
+    "whatShouldClaudianDoInstead": "· What should Claudian do instead?",
+    "subagentTask": "Subagent task",
+    "failedToRenderMessageContent": "Failed to render message content.",
+    "copyMessage": "Copy message",
+    "copied": "Copied!",
+    "noPromptProvided": "No prompt provided",
+    "result": "Result",
+    "statusRunning": "Status: running",
+    "prompt": "Prompt",
+    "initializing": "Initializing",
+    "orphaned": "Orphaned",
+    "runningInBackground": "Running in background",
+    "completed": "Completed",
+    "conversationEndedBeforeTaskCompleted": "Conversation ended before task completed",
+    "backgroundTask": "Background task",
+    "extendedThinkingClickToExpand": "Extended thinking - click to expand",
+    "thinking0s": "Thinking 0s...",
+    "thought": "Thought",
+    "tasks": "Tasks",
+    "enteringPlanMode": "Entering plan mode",
+    "findInPage": "Find in page",
+    "openPage": "Open page",
+    "urlUnavailable": "URL unavailable",
+    "searchWeb": "Search web",
+    "noResult": "No result",
+    "noMatchesFound": "No matches found",
+    "fileDeleted": "File deleted",
+    "noTextualDiffAvailable": "No textual diff available",
+    "statusCompleted": "Status: completed",
+    "statusInProgress": "Status: in progress",
+    "tasksUpdated": "Tasks updated",
+    "waitingForAnswer": "Waiting for answer...",
+    "noOptionsRecorded": "No options recorded",
+    "writing": "Writing...",
+    "statusError": "Status: error",
+    "clickToCollapse": "click to collapse",
+    "clickToExpand": "click to expand",
+    "outputExceededMaximumBufferSize1MB": "Output exceeded maximum buffer size (1MB)",
+    "taskFailedToStart": "Task failed to start",
+    "backgroundTaskFailed": "Background task failed.",
+    "backgroundTaskCompleted": "Background task completed.",
+    "unlinked": "Unlinked",
+    "needsYourInput2": "needs your input",
+    "stoppedWithAnErrorReadyToReview": "stopped with an error, ready to review",
+    "finishedReadyToReview": "finished, ready to review",
+    "backgroundTaskStateCouldNotBeSavedBeforeClosingTheTab": "Background task state could not be saved before closing the tab.",
+    "newChat": "New Chat",
+    "loadingConversation": "Loading conversation…",
+    "fork": "Fork: ",
+    "failedToInitializeChatExecution": "Failed to initialize chat execution",
+    "askToMakeChangesMentionFilesRunCommands": "Ask to make changes, @mention files, run /commands",
+    "providerExecutionFailed": "Provider execution failed.",
+    "cannotSwitchProviderOnABoundSessionStartANewConversationInstead": "Cannot switch provider on a bound session. Start a new conversation instead.",
+    "showLess": "Show less",
+    "dropImageHere": "Drop image here",
+    "imageAttachmentsAreNotSupportedByThisProvider": "Image attachments are not supported by this provider.",
+    "unsupportedImageType": "Unsupported image type.",
+    "failedToAttachImage": "Failed to attach image.",
+    "image": "Image",
+    "closeImagePreview": "Close image preview",
+    "unknown": "Unknown",
+    "failedToChangeModel": "Failed to change model",
+    "failedToChangeMode": "Failed to change mode",
+    "effort": "Effort:",
+    "thinking": "Thinking:",
+    "failedToChangeEffortLevel": "Failed to change effort level",
+    "failedToChangeThinkingBudget": "Failed to change thinking budget",
+    "failedToChangePermissionMode2": "Failed to change permission mode",
+    "plan": "PLAN",
+    "failedToChangeServiceTier": "Failed to change service tier",
+    "noPathProvidedUsageAdddirAbsolutepath": "No path provided. Usage: /add-dir /absolute/path",
+    "pathMustBeAbsoluteUsageAdddirAbsolutepath": "Path must be absolute. Usage: /add-dir /absolute/path",
+    "thisFolderIsAlreadyAddedAsAnExternalContext": "This folder is already added as an external context.",
+    "selectExternalContext": "Select External Context",
+    "unableToOpenFolderPicker": "Unable to open folder picker.",
+    "externalContexts": "External contexts",
+    "clickFolderIconToAdd": "Click folder icon to add",
+    "persistentClickToMakeSessiononly": "Persistent (click to make session-only)",
+    "sessiononlyClickToPersist": "Session-only (click to persist)",
+    "removePath": "Remove path",
+    "addExternalContextsClick": "Add external contexts (click)",
+    "contextUsage": "Context usage",
+    "approachingLimitRunCompactToContinue": " (Approaching limit, run `/compact` to continue)",
+    "saveInCustomSystemPrompt": "Save in custom system prompt",
+    "scrollToTop": "Scroll to top",
+    "previousMessage": "Previous message",
+    "conversationDirectory": "Conversation directory",
+    "nextMessage": "Next message",
+    "scrollToBottom": "Scroll to bottom",
+    "noUserPromptsInThisConversation": "No user prompts in this conversation",
+    "collapse": "Collapse",
+    "expand": "Expand",
+    "providerSettingsAreUnavailable": "Provider settings are unavailable.",
+    "sharedEnvironment": "Shared environment",
+    "providerneutralRuntimeVariablesSharedAcrossAllProvidersUseThisForPATHProxyCertAndTempVariables": "Provider-neutral runtime variables shared across all providers. Use this for PATH, proxy, cert, and temp variables.",
+    "theNewConcurrentRunningSessionLimitWillApplyAsBusySessionsBecomeIdle": "The new concurrent running session limit will apply as busy sessions become idle.",
+    "failedToReleaseExcessWarmAgentProcesses": "Failed to release excess warm agent processes.",
+    "customLabelShownInTheModelSelectorLeaveEmptyToUseTheDefault": "Custom label shown in the model selector. Leave empty to use the default.",
+    "eachLineMustFollowMapKeyAction": "Each line must follow \"map <key> <action>\"",
+    "navigationKeysMustBeUnique": "Navigation keys must be unique",
+    "inlineEditUnavailableCouldNotAccessTheActiveEditorTryReopeningTheNote": "Inline edit unavailable: could not access the active editor. Try reopening the note.",
+    "failedToLoadVaultFilesVaultMentionsMayBeUnavailable": "Failed to load vault files. Vault @-mentions may be unavailable.",
+    "insertInstructions": "Insert instructions...",
+    "editInstructions": "Edit instructions...",
+    "inlineEditActions": "Inline edit actions",
+    "reject": "Reject",
+    "accept": "Accept",
+    "acceptEnter": "Accept (enter)",
+    "rejectEsc": "Reject (esc)",
+    "errorTryAgain": "Error - try again",
+    "replyToContinue": "Reply to continue...",
+    "noResponseFromAgent": "No response from agent",
+    "inlineEditWasNotAppliedBecauseTheSourceDocumentOrSelectionChanged": "Inline edit was not applied because the source document or selection changed.",
+    "failedToAttachFileInvalidPath": "Failed to attach file: invalid path",
+    "resumeConversation": "Resume conversation",
+    "loading": "Loading…",
+    "noMatches": "No matches",
+    "couldNotLoadSuggestions": "Could not load suggestions",
+    "typeAgainToRetry": "Type again to retry",
+    "agents": "Agents",
+    "loadingProviderCommands": "Loading provider commands…",
+    "tryProviderCommandDiscoveryAgain": "Try provider command discovery again",
+    "addCustomInstruction": "Add custom instruction",
+    "yourInput": "Your input:",
+    "processingYourInstruction": "Processing your instruction...",
+    "yourResponse": "Your response:",
+    "provideMoreDetails": "Provide more details...",
+    "refinedSnippet": "Refined snippet:",
+    "submitResponse": "Submit response",
+    "editInstruction": "Edit instruction",
+    "acceptInstruction": "Accept instruction",
+    "unexpectedStorageError": "Unexpected storage error",
+    "insert": "Insert",
+    "failedToInsertSnippet": "Failed to insert snippet",
+    "failedToDeleteSnippet": "Failed to delete snippet",
+    "chooseWhichModelsAreAvailableInTheChatSelectorDragToReorderThemTheProviderUsesTheFirstCurrentlyUsableModelAsItsDefaultSelectAtLeastOneModelToUseThisProvider": "Choose which models are available in the chat selector. Drag to reorder them; the provider uses the first currently usable model as its default. Select at least one model to use this provider.",
+    "visibleModels": "Visible models",
+    "browseModels": "Browse models",
+    "filterByModelProviderOrID": "Filter by model, provider, or ID...",
+    "discover": "Discover",
+    "visible": "Visible: ",
+    "loadingModels": "Loading models...",
+    "noModelsDiscoveredYet": "No models discovered yet",
+    "loading2": "Loading...",
+    "dragOrUseArrowKeysToReorder": "Drag or use arrow keys to reorder",
+    "default": "Default",
+    "aliasOptional": "Alias (optional)",
+    "noModelsMatchYourFilter": "No models match your filter.",
+    "unavailable": "Unavailable",
+    "safe": "Safe",
+    "yolo": "YOLO",
+    "completionFlavor0": "Baked",
+    "completionFlavor1": "Cooked",
+    "completionFlavor2": "Crunched",
+    "completionFlavor3": "Brewed",
+    "completionFlavor4": "Crafted",
+    "completionFlavor5": "Forged",
+    "completionFlavor6": "Conjured",
+    "completionFlavor7": "Whipped up",
+    "completionFlavor8": "Stirred",
+    "completionFlavor9": "Simmered",
+    "completionFlavor10": "Toasted",
+    "completionFlavor11": "Sautéed",
+    "completionFlavor12": "Finagled",
+    "completionFlavor13": "Marinated",
+    "completionFlavor14": "Distilled",
+    "completionFlavor15": "Fermented",
+    "completionFlavor16": "Percolated",
+    "completionFlavor17": "Steeped",
+    "completionFlavor18": "Roasted",
+    "completionFlavor19": "Cured",
+    "completionFlavor20": "Smoked",
+    "completionFlavor21": "Cogitated",
+    "thinkingFlavor0": "Thinking...",
+    "thinkingFlavor1": "Pondering...",
+    "thinkingFlavor2": "Processing...",
+    "thinkingFlavor3": "Analyzing...",
+    "thinkingFlavor4": "Considering...",
+    "thinkingFlavor5": "Working on it...",
+    "thinkingFlavor6": "Vibing...",
+    "thinkingFlavor7": "One moment...",
+    "thinkingFlavor8": "On it...",
+    "thinkingFlavor9": "Ruminating...",
+    "thinkingFlavor10": "Contemplating...",
+    "thinkingFlavor11": "Reflecting...",
+    "thinkingFlavor12": "Mulling it over...",
+    "thinkingFlavor13": "Let me think...",
+    "thinkingFlavor14": "Hmm...",
+    "thinkingFlavor15": "Cogitating...",
+    "thinkingFlavor16": "Deliberating...",
+    "thinkingFlavor17": "Weighing options...",
+    "thinkingFlavor18": "Gathering thoughts...",
+    "thinkingFlavor19": "Brewing ideas...",
+    "thinkingFlavor20": "Connecting dots...",
+    "thinkingFlavor21": "Assembling thoughts...",
+    "thinkingFlavor22": "Spinning up neurons...",
+    "thinkingFlavor23": "Loading brilliance...",
+    "thinkingFlavor24": "Consulting the oracle...",
+    "thinkingFlavor25": "Summoning knowledge...",
+    "thinkingFlavor26": "Crunching thoughts...",
+    "thinkingFlavor27": "Dusting off neurons...",
+    "thinkingFlavor28": "Wrangling ideas...",
+    "thinkingFlavor29": "Herding thoughts...",
+    "thinkingFlavor30": "Juggling concepts...",
+    "thinkingFlavor31": "Untangling this...",
+    "thinkingFlavor32": "Piecing it together...",
+    "thinkingFlavor33": "Sipping coffee...",
+    "thinkingFlavor34": "Warming up...",
+    "thinkingFlavor35": "Getting cozy with this...",
+    "thinkingFlavor36": "Settling in...",
+    "thinkingFlavor37": "Making tea...",
+    "thinkingFlavor38": "Grabbing a snack...",
+    "thinkingFlavor39": "Parsing...",
+    "thinkingFlavor40": "Compiling thoughts...",
+    "thinkingFlavor41": "Running inference...",
+    "thinkingFlavor42": "Querying the void...",
+    "thinkingFlavor43": "Defragmenting brain...",
+    "thinkingFlavor44": "Allocating memory...",
+    "thinkingFlavor45": "Optimizing...",
+    "thinkingFlavor46": "Indexing...",
+    "thinkingFlavor47": "Syncing neurons...",
+    "thinkingFlavor48": "Breathing...",
+    "thinkingFlavor49": "Finding clarity...",
+    "thinkingFlavor50": "Channeling focus...",
+    "thinkingFlavor51": "Centering...",
+    "thinkingFlavor52": "Aligning chakras...",
+    "thinkingFlavor53": "Meditating on this...",
+    "thinkingFlavor54": "Asking the stars...",
+    "thinkingFlavor55": "Reading tea leaves...",
+    "thinkingFlavor56": "Shaking the magic 8-ball...",
+    "thinkingFlavor57": "Consulting ancient scrolls...",
+    "thinkingFlavor58": "Decoding the matrix...",
+    "thinkingFlavor59": "Communing with the ether...",
+    "thinkingFlavor60": "Peering into the abyss...",
+    "thinkingFlavor61": "Channeling the cosmos...",
+    "thinkingFlavor62": "Diving in...",
+    "thinkingFlavor63": "Rolling up sleeves...",
+    "thinkingFlavor64": "Getting to work...",
+    "thinkingFlavor65": "Tackling this...",
+    "thinkingFlavor66": "On the case...",
+    "thinkingFlavor67": "Investigating...",
+    "thinkingFlavor68": "Exploring...",
+    "thinkingFlavor69": "Digging deeper...",
+    "thinkingFlavor70": "Bear with me...",
+    "thinkingFlavor71": "Hang tight...",
+    "thinkingFlavor72": "Just a sec...",
+    "thinkingFlavor73": "Working my magic...",
+    "thinkingFlavor74": "Almost there...",
+    "thinkingFlavor75": "Give me a moment...",
+    "completedIn": "* {word} for {duration}",
+    "thinkingSeconds": "Thinking {seconds}s...",
+    "thoughtSeconds": "Thought for {seconds}s",
+    "helloName": "Hi {name}, how are you?",
+    "nameReturns": "{name} returns!",
+    "loadMoreRemainingFormat": "Load more ({p0} remaining)",
+    "newChatForFormat": "New chat for {p0}",
+    "runningInFormat": "Running in {p0}",
+    "currentTabFormat": "Current tab {p0}",
+    "openInFormat": "Open in {p0}",
+    "tabFormat": "tab {p0}",
+    "approvalDetailsFormat": "{p0} approval details",
+    "unexpectedApprovalSelectionFormat": "Unexpected approval selection: \"{p0}\"",
+    "isNotSupportedByThisProviderFormat": "/{p0} is not supported by this provider.",
+    "addedExternalContextFormat": "Added external context: {p0}",
+    "unknownCommandFormat": "Unknown command: {p0}",
+    "failedToOpenConversationFormat": "Failed to open conversation: {p0}",
+    "escToInterruptFormat": " (esc to interrupt · {p0})",
+    "theConcurrentRunningSessionLimitIsCurrentlyInUseFormat": "The concurrent running session limit ({p0}) is currently in use. ",
+    "missingContentFormat": "{p0} · Missing content",
+    "linkedContentMissingContentFormat": "Linked content: {p0}. Missing content",
+    "linkedContentFormat": "Linked content: {p0}",
+    "linkedContentIsMissingFormat": "Linked content is missing: {p0}",
+    "failedToOpenLinkedContentFormat": "Failed to open Linked content: {p0}",
+    "memoryUsedFormat": "Memory used, {p0} {p1}",
+    "moreLinesFormat": "... {p0} more lines",
+    "couldNotReadPlanFileApproveNewSessionWillNotIncludePlanDetailsFormat": "Could not read plan file: {p0}. \"Approve (new session)\" will not include plan details.",
+    "forFormat": "* {p0} for {p1}",
+    "previewFormat": "Preview {p0}",
+    "subagentTaskStatusClickToExpandFormat": "Subagent task: {p0} - Status: {p1} - click to expand",
+    "backgroundTaskClickToExpandFormat": "Background task: {p0} - {p1} - click to expand",
+    "backgroundTaskInitializingClickToExpandFormat": "Background task: {p0} - Initializing - click to expand",
+    "tasksFormat": "Tasks {p0}/{p1}",
+    "tasksFormat2": "Tasks ({p0}/{p1})",
+    "openFormat": "Open {p0}",
+    "findFormat": "Find \"{p0}\"",
+    "inFormat": " in {p0}",
+    "altQueryFormat": "Alt query: {p0}",
+    "moreQueriesFormat": "... {p0} more queries",
+    "moreCharactersFormat": "... {p0} more characters",
+    "commandTimedOutAfterSFormat": "Command timed out after {p0}s",
+    "failedToSwitchToTabAndRestoreThePreviousTabFormat": "Failed to switch to tab {p0} and restore the previous tab",
+    "failedToCloseTabCleanlyFormat": "Failed to close tab {p0} cleanly",
+    "commandFailedFormat": "Command failed: {p0}",
+    "showMoreContextItemsFormat": "Show {p0} more context items",
+    "removeFormat": "Remove {p0}",
+    "imageExceedsLimitFormat": "Image exceeds {p0} limit.",
+    "imageFormat": "Image {p0}",
+    "imageAttachmentFormat": "Image attachment: {p0}",
+    "fileNotFoundFormat": "{p0} (File not found)",
+    "permissionDeniedFormat": "{p0} (Permission denied)",
+    "imagePreviewFormat": "Image preview: {p0}",
+    "fastModeFormat": "Fast mode: {p0}",
+    "removedInvalidExternalContextPathsFormat": "Removed {p0} invalid external context path(s): {p1}",
+    "cannotPersistDirectoryNoLongerExistsFormat": "Cannot persist \"{p0}\" - directory no longer exists",
+    "cannotAddItsInsideExistingPathFormat": "Cannot add \"{p0}\" - it's inside existing path \"{p1}\"",
+    "cannotAddItContainsExistingPathFormat": "Cannot add \"{p0}\" - it contains existing path \"{p1}\"",
+    "externalContextClickToAddMoreFormat": "{p0} external context{p1} (click to add more)",
+    "taskListOfCompletedFormat": "{p0} task list - {p1} of {p2} completed",
+    "inlineEditUnavailableFailedToInitializeTheProviderFormat": "Inline edit unavailable: failed to initialize the {p0} provider.",
+    "inlineEditFormat": "{p0} inline edit",
+    "aliasForFormat": "Alias for {p0}",
+    "contextWindowForFormat": "Context window for {p0}",
+    "deleteEnvironmentSnippetFormat": "Delete environment snippet \"{p0}\"?",
+    "environmentSnippetSavedFormat": "Environment snippet \"{p0}\" saved",
+    "environmentSnippetUpdatedFormat": "Environment snippet \"{p0}\" updated",
+    "environmentSnippetDeletedFormat": "Environment snippet \"{p0}\" deleted",
+    "reviewEnvironmentOwnershipForFormat": "Review environment ownership for: {p0}",
+    "ofDiscoveredFormat": " of {p0} discovered | {p1} {p2}",
+    "ofDiscoveredFormat2": " of {p0} discovered",
+    "selectedFormat": "Selected ({p0})",
+    "clearAllSelectedModelsFormat": "Clear all selected {p0} models",
+    "reorderDragOrUseTheUpAndDownArrowKeysFormat": "Reorder {p0}; drag or use the Up and Down Arrow keys",
+    "allProvidersFormat": "All providers ({p0})",
+    "configuredModelNotCurrentlyReportedByFormat": "Configured model not currently reported by {p0}",
+    "openChatView": "Open chat view",
+    "inlineEdit": "Inline edit",
+    "inlineEditorUnavailable": "Inline edit unavailable: could not access the active Markdown view.",
+    "inserted": "Inserted",
+    "editApplied": "Edit applied",
+    "new": "New",
+    "replaceConversation": "Replace current conversation",
+    "closeCurrentTab": "Close current tab",
+    "copyStartupDiagnostics": "Copy startup diagnostics",
+    "startupDiagnosticsCopied": "Startup diagnostics copied to clipboard.",
+    "startupDiagnosticsFailed": "Failed to copy startup diagnostics.",
+    "linkedPathsFailed": "Failed to update linked content paths",
+    "linkedPinsFailed": "Failed to update pinned linked content",
+    "obsoleteConfigFailed": "Failed to remove obsolete Claude configuration",
+    "defaultModel": "Default model",
+    "defaultModelDescription": "Used when a new conversation needs a Claude fallback model.",
+    "learnMore": "Learn more",
+    "safeDefault": "default",
+    "safeAcceptEdits": "acceptEdits",
+    "safeAuto": "auto",
+    "effortlow": "Low",
+    "effortmedium": "Medium",
+    "efforthigh": "High",
+    "effortxhigh": "xHigh",
+    "effortmax": "Max",
+    "commandNew": "Start a new conversation",
+    "commandAddDirectory": "Add external context directory",
+    "commandResume": "Resume a previous conversation",
+    "commandFork": "Fork entire conversation to new session",
+    "commandFast": "Toggle fast mode",
+    "commandInstruction": "Save a reusable custom instruction",
+    "provider": "provider",
+    "providers": "providers",
+    "source": "source",
+    "sources": "sources",
+    "openClaudian": "Open Claudian",
+    "actionUnknown": "(unknown)",
+    "statusBlocked": "Status: blocked",
+    "tabActive": "active",
+    "tabWorking": "working",
+    "tabIdle": "idle",
+    "modelHaiku": "Fast and efficient",
+    "modelSonnet": "Balanced performance",
+    "modelOpus": "Most capable",
+    "modelFable": "Anthropic's most capable model — premium pricing above Opus",
+    "claudeCommandsUnavailable": "Claude command catalog is unavailable.",
+    "claudeCommandsEmpty": "No commands or skills configured. Click + to create one.",
+    "claudePluginsHeader": "Claude Code Plugins",
+    "claudePluginsEmpty": "No Claude code plugins found. Enable plugins via the Claude CLI.",
+    "claudeProjectPlugins": "Project plugins",
+    "claudeUserPlugins": "User plugins",
+    "pluginDisable": "Disable",
+    "pluginEnable": "Enable",
+    "claudeEnvironmentDescription": "Claude-owned runtime variables only. Use this for ANTHROPIC_* and Claude-specific toggles.",
+    "actionRun": "Run command: {pattern}",
+    "actionRead": "Read file: {pattern}",
+    "actionWrite": "Write to file: {pattern}",
+    "actionEdit": "Edit file: {pattern}",
+    "actionGlob": "Search files matching: {pattern}",
+    "actionGrep": "Search content matching: {pattern}"
   }
 }
diff --git a/src/i18n/locales/es.json b/src/i18n/locales/es.json
index 47f648a..d15e2e7 100644
--- a/src/i18n/locales/es.json
+++ b/src/i18n/locales/es.json
@@ -781,9 +781,9 @@
       }
     },
     "agentSkills": {
-      "sectionTitle": "Skills", "fixedRoot": "Managed in .agents/skills", "sharedExpectation": "Manage shared skills in .agents/skills/ for compatible providers.", "noSkills": "No shared skills configured. Click + to create one.", "skillBadge": "skill", "diagnosticsTitle": "Skills needing attention", "loadFailed": "Could not load shared skills.", "savedRefreshFailed": "Saved, but provider refresh failed.", "validationFailed": "Invalid skill: {message}", "collisionFailed": "A skill named \"{name}\" already exists.", "saveFailed": "Failed to save shared skill: {message}", "deleteFailed": "Failed to move shared skill to trash: {message}", "staleConflict": "This skill changed elsewhere; refresh before saving.", "created": "Shared skill \"{name}\" created.", "updated": "Shared skill \"{name}\" updated.", "deleted": "Shared skill \"{name}\" moved to trash.",
-      "modal": { "titleEdit": "Edit Shared Skill", "titleAdd": "Add Shared Skill", "name": "Skill name", "nameDesc": "Lowercase letters and numbers separated by single hyphens", "namePlaceholder": "code-reviewer", "description": "Description", "descriptionDesc": "Required portable description used by providers to select the skill", "descriptionPlaceholder": "Reviews code for correctness and maintainability", "instructions": "Instructions", "instructionsDesc": "Required SKILL.md instructions", "instructionsPlaceholder": "Review the supplied code and report actionable findings..." },
-      "delete": { "title": "Delete Shared Skill", "description": "The entire skill folder, including scripts, references, and assets, will be moved to trash.", "confirm": "Move to trash" }
+      "sectionTitle": "Habilidades", "fixedRoot": "Gestionadas en .agents/skills", "sharedExpectation": "Gestiona habilidades compartidas en .agents/skills/ para los proveedores compatibles.", "noSkills": "No hay habilidades compartidas. Pulsa + para crear una.", "skillBadge": "habilidad", "diagnosticsTitle": "Habilidades que necesitan atención", "loadFailed": "No se pudieron cargar las habilidades compartidas.", "savedRefreshFailed": "Se guardó, pero falló la actualización del proveedor.", "validationFailed": "Habilidad no válida: {message}", "collisionFailed": "Ya existe una habilidad llamada \"{name}\".", "saveFailed": "No se pudo guardar la habilidad compartida: {message}", "deleteFailed": "No se pudo enviar la habilidad compartida a la papelera: {message}", "staleConflict": "Esta habilidad cambió en otro lugar; actualiza antes de guardar.", "created": "Habilidad compartida \"{name}\" creada.", "updated": "Habilidad compartida \"{name}\" actualizada.", "deleted": "Habilidad compartida \"{name}\" enviada a la papelera.",
+      "modal": { "titleEdit": "Editar habilidad compartida", "titleAdd": "Añadir habilidad compartida", "name": "Nombre de la habilidad", "nameDesc": "Letras minúsculas y números separados por guiones simples", "namePlaceholder": "code-reviewer", "description": "Descripción", "descriptionDesc": "Descripción obligatoria que permite al proveedor seleccionar la habilidad", "descriptionPlaceholder": "Revisa la corrección y la facilidad de mantenimiento del código", "instructions": "Instrucciones", "instructionsDesc": "Instrucciones obligatorias de SKILL.md", "instructionsPlaceholder": "Revisa el código proporcionado e informa de problemas concretos..." },
+      "delete": { "title": "Eliminar habilidad compartida", "description": "Se enviará a la papelera toda la carpeta de la habilidad, incluidos scripts, referencias y recursos.", "confirm": "Enviar a la papelera" }
     },
     "codexSkills": {
       "modal": {
@@ -855,5 +855,567 @@
       "header": "Subagentes de Codex",
       "noAgents": "No hay subagentes Codex en el vault. Haz clic en + para crear uno."
     }
+  },
+  "ui": {
+    "resizeConversationSessions": "Cambiar el tamaño de la lista de conversaciones",
+    "newConversation": "Nueva conversación",
+    "chatHistory": "Historial de conversaciones",
+    "failedToCreateTab": "No se pudo crear la pestaña",
+    "failedToCreateConversation": "No se pudo crear la conversación",
+    "failedToStartANewConversation": "No se pudo iniciar una nueva conversación",
+    "newConversationInputIsUnavailable": "La entrada de la nueva conversación no está disponible",
+    "failedToStartTheApprovedPlanInANewConversation": "No se pudo iniciar el plan aprobado en una nueva conversación",
+    "failedToSwitchTab": "No se pudo cambiar de pestaña",
+    "failedToCloseTab": "No se pudo cerrar la pestaña",
+    "archived": "Archivadas",
+    "searchArchivedSessions": "Buscar sesiones archivadas",
+    "searchSessions": "Buscar sesiones",
+    "search": "Buscar",
+    "archive": "Archivar",
+    "expandAllGroups": "Expandir todos los grupos",
+    "collapseAllGroups": "Contraer todos los grupos",
+    "sessionOptions": "Opciones de sesión",
+    "chatTabsAreUnavailable": "Las pestañas de conversación no están disponibles",
+    "linkedContentIsNoLongerAvailable": "El contenido vinculado ya no está disponible",
+    "failedToCreateAProvisionalChatTab": "No se pudo crear una pestaña de vista previa",
+    "runningSessionsCannotBeArchived": "No se pueden archivar sesiones en ejecución",
+    "failedToCloseTheSessionBeforeArchiving": "No se pudo cerrar la sesión antes de archivarla",
+    "organizeSessions": "Organizar sesiones",
+    "inOneList": "En una sola lista",
+    "byLinkedContent": "Por contenido vinculado",
+    "sortSessionsBy": "Ordenar sesiones por",
+    "lastActivity": "Última actividad",
+    "created": "Fecha de creación",
+    "failedToUpdateSessionOrganization": "No se pudo cambiar la organización de las sesiones",
+    "failedToUpdateSessionSorting": "No se pudo cambiar el orden de las sesiones",
+    "failedToCloseTheProvisionalSessionPreview": "No se pudo cerrar la vista previa de la sesión",
+    "failedToChangePermissionMode": "No se pudo cambiar el modo de permisos.",
+    "failedToInitializeAgentService": "No se pudo iniciar el servicio del agente",
+    "agentServiceNotAvailable": "El servicio del agente no está disponible",
+    "agentExecutionNotAvailable": "La ejecución del agente no está disponible",
+    "conversationChangedWhileRewinding": "La conversación cambió durante la restauración.",
+    "unknownError": "error desconocido",
+    "failedToSave": "No se pudo guardar",
+    "pinned": "Fijadas",
+    "noMatchingSessions": "No hay sesiones que coincidan",
+    "noConversations": "No hay conversaciones",
+    "missing": "No encontrado",
+    "failedToStartAChatForThisLinkedContent": "No se pudo iniciar una conversación para este contenido vinculado",
+    "unpinLinkedContent": "Desfijar contenido vinculado",
+    "pinLinkedContent": "Fijar contenido vinculado",
+    "failedToUnpinLinkedContent": "No se pudo desfijar el contenido vinculado",
+    "failedToPinLinkedContent": "No se pudo fijar el contenido vinculado",
+    "archiveAllSessions": "Archivar todas las sesiones",
+    "failedToArchiveLinkedContentSessions": "No se pudieron archivar las sesiones del contenido vinculado",
+    "failedToLoadConversation": "No se pudo cargar la conversación",
+    "generatingTitle": "Generando título...",
+    "regenerateTitle": "Volver a generar el título",
+    "failedToRegenerateResponse": "No se pudo volver a generar la respuesta",
+    "openInNewTab": "Abrir en una pestaña nueva",
+    "failedToDeleteConversation": "No se pudo eliminar la conversación",
+    "assignToThisDevice": "Asignar a este dispositivo",
+    "failedToAssignSessionToThisDevice": "No se pudo asignar la sesión a este dispositivo",
+    "unpin": "Desfijar",
+    "pin": "Fijar",
+    "failedToUnpinSession": "No se pudo desfijar la sesión",
+    "failedToPinSession": "No se pudo fijar la sesión",
+    "cannotArchiveARunningSession": "No se puede archivar una sesión en ejecución",
+    "failedToArchiveSession": "No se pudo archivar la sesión",
+    "restore": "Restaurar",
+    "failedToRestoreSession": "No se pudo restaurar la sesión",
+    "rename": "Cambiar nombre",
+    "needsYourInput": "Necesita tu respuesta",
+    "stoppedWithAnError": "Se detuvo por un error",
+    "lastActive": "Última actividad",
+    "runningInAnotherPane": "En ejecución en otro panel",
+    "openInAnotherPane": "Abierta en otro panel",
+    "currentSession": "Sesión actual",
+    "currentTab": "pestaña actual",
+    "openInBackgroundTab": "Abrir en segundo plano",
+    "switchToOpenSession": "Ir a la sesión abierta",
+    "failedToRenameConversation": "No se pudo cambiar el nombre de la conversación",
+    "happySunday": "Feliz domingo",
+    "sundaySession": "¿Una sesión de domingo?",
+    "welcomeToTheWeekend": "Buen fin de semana",
+    "happyMonday": "Feliz lunes",
+    "backAtIt": "De vuelta al trabajo",
+    "backAtIt2": "¡De vuelta al trabajo!",
+    "happyTuesday": "Feliz martes",
+    "happyWednesday": "Feliz miércoles",
+    "happyThursday": "Feliz jueves",
+    "happyFriday": "Feliz viernes",
+    "thatFridayFeeling": "Ya es viernes",
+    "happySaturday": "Feliz sábado",
+    "happySaturday2": "¡Feliz sábado!",
+    "goodMorning": "Buenos días",
+    "coffeeAndClaudianTime": "¿Un café y un rato con Claudian?",
+    "goodAfternoon": "Buenas tardes",
+    "heyThere": "Hola",
+    "howsItGoing": "¿Qué tal",
+    "goodEvening": "Buenas noches",
+    "evening": "Buenas noches",
+    "howWasYourDay": "¿Cómo fue tu día",
+    "helloNightOwl": "Hola, alma nocturna",
+    "hiHowAreYou": "Hola, ¿cómo estás?",
+    "welcomeBack": "Qué bueno verte de nuevo",
+    "whatsNew": "¿Qué hay de nuevo",
+    "youAreAbsolutelyRight": "¡Tienes toda la razón!",
+    "deny": "Denegar",
+    "allowOnce": "Permitir una vez",
+    "alwaysAllow": "Permitir siempre",
+    "compacting": "Compactando...",
+    "failedToInitializeAgentExecutionPleaseTryAgain": "No se pudo iniciar el agente. Inténtalo de nuevo.",
+    "agentExecutionIsNotAvailablePleaseReloadThePlugin": "El agente no está disponible. Recarga el complemento.",
+    "theProviderSessionNoLongerExistsItsClaudianRecordWasRemovedSendAgainToStartANewSession": "La sesión del proveedor ya no existe. Se eliminó su registro de Claudian; vuelve a enviar el mensaje para iniciar otra sesión.",
+    "theProviderSessionNoLongerExistsClaudianPreservedTheRecoverableHistorySendAgainToRebuildTheSession": "La sesión del proveedor ya no existe. Claudian conservó el historial recuperable; vuelve a enviar el mensaje para reconstruirla.",
+    "theProviderSessionNoLongerExistsClaudianPreservedItsRecordBecauseTheRemainingHistoryCouldNotBeVerified": "La sesión del proveedor ya no existe. Claudian conservó su registro porque no pudo verificar el historial restante.",
+    "theProviderSessionNoLongerExistsSendAgainToStartANewSession": "La sesión del proveedor ya no existe. Vuelve a enviar el mensaje para iniciar otra sesión.",
+    "messageWasNotSentPleaseTryAgain": "No se envió el mensaje. Inténtalo de nuevo.",
+    "steering": "⌙ Redirigiendo: ",
+    "queued": "⌙ En cola: ",
+    "steering2": "Redirigiendo...",
+    "steerNow": "Redirigir ahora",
+    "editQueuedMessage": "Editar mensaje en cola",
+    "discardQueuedMessage": "Descartar mensaje en cola",
+    "failedToSteerTheQueuedMessageItIsStillAvailable": "No se pudo redirigir el mensaje en cola. Sigue disponible.",
+    "steerDeliveryCouldNotBeConfirmedTheMessageWasNotRequeuedToAvoidSendingItTwice": "No se pudo confirmar la entrega. El mensaje no volvió a la cola para evitar enviarlo dos veces.",
+    "failedToInitializeInstructionRefinementPleaseTryAgain": "No se pudo iniciar la mejora de instrucciones. Inténtalo de nuevo.",
+    "instructionAddedToCustomSystemPrompt": "Instrucción añadida a las instrucciones personalizadas del sistema",
+    "failedToProcessResponse": "No se pudo procesar la respuesta",
+    "failedToRefineInstruction": "No se pudo mejorar la instrucción",
+    "noInstructionReceived": "No se recibió ninguna instrucción",
+    "allowThisAction": "¿Permitir esta acción?",
+    "permissionRequired": "Se necesita permiso",
+    "externalContextSelectorNotAvailable": "El selector de contexto externo no está disponible.",
+    "forkIsNotSupportedByThisProvider": "Este proveedor no permite ramificar conversaciones.",
+    "forkNotAvailable": "No se puede ramificar esta conversación.",
+    "fastModeIsNotAvailableForThisModel": "El modo rápido no está disponible para este modelo.",
+    "failedToToggleFastMode": "No se pudo cambiar el modo rápido.",
+    "instructionModeIsNotAvailable": "El modo de instrucciones no está disponible.",
+    "noConversationsToResume": "No hay conversaciones para reanudar",
+    "blocked": "Bloqueado",
+    "notice": "Aviso",
+    "done": "HECHO",
+    "taskCancelled": "Tarea cancelada",
+    "conversationCompacted": "Conversación compactada",
+    "rewindIsNotSupportedByThisProvider": "Este proveedor no permite retroceder la conversación.",
+    "finishOrStopASessionBeforeStartingAnother": "Termina o detén una sesión antes de iniciar otra.",
+    "claudianChatIsNotReady": "El chat de Claudian aún no está listo.",
+    "failedToAddFileToClaudian": "No se pudo añadir el archivo a Claudian.",
+    "addToClaudian": "Añadir a Claudian",
+    "none": "Ninguno",
+    "noLinkedContent": "Sin contenido vinculado",
+    "linkedContent": "Contenido vinculado:",
+    "linkedContentChoices": "Opciones de contenido vinculado",
+    "noMatchingFilesOrFolders": "No hay archivos ni carpetas que coincidan",
+    "memoryUsed": "Memoria utilizada",
+    "noChanges": "Sin cambios",
+    "enterToSelectTabArrowKeysToNavigateEscToCancel": "Intro para elegir · Tab o flechas para navegar · Esc para cancelar",
+    "enterToSelectArrowKeysToNavigateEscToCancel": "Intro para elegir · Flechas para navegar · Esc para cancelar",
+    "question": "Pregunta",
+    "option": "Opción",
+    "submit": "Enviar",
+    "enterSecret": "Introduce el dato secreto.",
+    "typeSomething": "Escribe algo.",
+    "reviewYourAnswers": "Revisa tus respuestas",
+    "notAnswered": "Sin responder",
+    "readyToSubmitYourAnswers": "¿Enviar tus respuestas?",
+    "submitAnswers": "Enviar respuestas",
+    "arrowKeysToNavigateEnterToSelectEscToCancel": "Flechas para navegar · Intro para elegir · Esc para cancelar",
+    "planComplete": "Plan terminado",
+    "requestedPermissions": "Permisos solicitados:",
+    "approveNewSession": "Aprobar en una sesión nueva",
+    "approveCurrentSession": "Aprobar en la sesión actual",
+    "enterFeedbackToContinuePlanning": "Escribe tus comentarios para seguir planificando...",
+    "abandon": "Abandonar",
+    "arrowKeysToNavigateEnterToSelectShiftTabToAbandon": "Flechas para navegar · Intro para elegir · Mayús+Tab para abandonar",
+    "unknownError2": " error desconocido",
+    "implement": "Implementar",
+    "enterFeedbackToRevisePlan": "Escribe tus comentarios para revisar el plan...",
+    "interrupted": "Interrumpido",
+    "whatShouldClaudianDoInstead": "· ¿Qué debería hacer Claudian ahora?",
+    "subagentTask": "Tarea de subagente",
+    "failedToRenderMessageContent": "No se pudo mostrar el contenido del mensaje.",
+    "copyMessage": "Copiar mensaje",
+    "copied": "¡Copiado!",
+    "noPromptProvided": "No se proporcionaron instrucciones",
+    "result": "Resultado",
+    "statusRunning": "Estado: en ejecución",
+    "prompt": "Instrucciones",
+    "initializing": "Iniciando",
+    "orphaned": "Sin sesión activa",
+    "runningInBackground": "En ejecución en segundo plano",
+    "completed": "Completado",
+    "conversationEndedBeforeTaskCompleted": "La conversación terminó antes que la tarea",
+    "backgroundTask": "Tarea en segundo plano",
+    "extendedThinkingClickToExpand": "Razonamiento ampliado, pulsa para expandir",
+    "thinking0s": "Pensando 0 s...",
+    "thought": "Razonamiento",
+    "tasks": "Tareas",
+    "enteringPlanMode": "Entrando en modo de planificación",
+    "findInPage": "Buscar en la página",
+    "openPage": "Abrir página",
+    "urlUnavailable": "URL no disponible",
+    "searchWeb": "Buscar en la web",
+    "noResult": "Sin resultados",
+    "noMatchesFound": "No se encontraron coincidencias",
+    "fileDeleted": "Archivo eliminado",
+    "noTextualDiffAvailable": "No hay diferencias de texto disponibles",
+    "statusCompleted": "Estado: completado",
+    "statusInProgress": "Estado: en curso",
+    "tasksUpdated": "Tareas actualizadas",
+    "waitingForAnswer": "Esperando respuesta...",
+    "noOptionsRecorded": "No hay opciones registradas",
+    "writing": "Escribiendo...",
+    "statusError": "Estado: error",
+    "clickToCollapse": "pulsa para contraer",
+    "clickToExpand": "pulsa para expandir",
+    "outputExceededMaximumBufferSize1MB": "La salida superó el límite de 1 MB",
+    "taskFailedToStart": "No se pudo iniciar la tarea",
+    "backgroundTaskFailed": "La tarea en segundo plano falló.",
+    "backgroundTaskCompleted": "La tarea en segundo plano terminó.",
+    "unlinked": "Sin vínculo",
+    "needsYourInput2": "necesita tu respuesta",
+    "stoppedWithAnErrorReadyToReview": "se detuvo por un error, lista para revisar",
+    "finishedReadyToReview": "terminada, lista para revisar",
+    "backgroundTaskStateCouldNotBeSavedBeforeClosingTheTab": "No se pudo guardar el estado de la tarea en segundo plano antes de cerrar la pestaña.",
+    "newChat": "Nueva conversación",
+    "loadingConversation": "Cargando conversación…",
+    "fork": "Rama: ",
+    "failedToInitializeChatExecution": "No se pudo iniciar la conversación con el agente",
+    "askToMakeChangesMentionFilesRunCommands": "Pide cambios, menciona archivos con @ o ejecuta /comandos",
+    "providerExecutionFailed": "La ejecución del proveedor falló.",
+    "cannotSwitchProviderOnABoundSessionStartANewConversationInstead": "No se puede cambiar el proveedor de una sesión vinculada. Inicia una conversación nueva.",
+    "showLess": "Mostrar menos",
+    "dropImageHere": "Suelta la imagen aquí",
+    "imageAttachmentsAreNotSupportedByThisProvider": "Este proveedor no admite imágenes adjuntas.",
+    "unsupportedImageType": "Tipo de imagen no compatible.",
+    "failedToAttachImage": "No se pudo adjuntar la imagen.",
+    "image": "Imagen",
+    "closeImagePreview": "Cerrar vista previa de la imagen",
+    "unknown": "Desconocido",
+    "failedToChangeModel": "No se pudo cambiar el modelo",
+    "failedToChangeMode": "No se pudo cambiar el modo",
+    "effort": "Esfuerzo:",
+    "thinking": "Razonamiento:",
+    "failedToChangeEffortLevel": "No se pudo cambiar el nivel de esfuerzo",
+    "failedToChangeThinkingBudget": "No se pudo cambiar el límite de razonamiento",
+    "failedToChangePermissionMode2": "No se pudo cambiar el modo de permisos",
+    "plan": "PLAN",
+    "failedToChangeServiceTier": "No se pudo cambiar el nivel de servicio",
+    "noPathProvidedUsageAdddirAbsolutepath": "Falta la ruta. Uso: /add-dir /ruta/absoluta",
+    "pathMustBeAbsoluteUsageAdddirAbsolutepath": "La ruta debe ser absoluta. Uso: /add-dir /ruta/absoluta",
+    "thisFolderIsAlreadyAddedAsAnExternalContext": "Esta carpeta ya está añadida como contexto externo.",
+    "selectExternalContext": "Seleccionar contexto externo",
+    "unableToOpenFolderPicker": "No se pudo abrir el selector de carpetas.",
+    "externalContexts": "Contextos externos",
+    "clickFolderIconToAdd": "Pulsa el icono de carpeta para añadir",
+    "persistentClickToMakeSessiononly": "Persistente, pulsa para usar solo en esta sesión",
+    "sessiononlyClickToPersist": "Solo esta sesión, pulsa para conservar",
+    "removePath": "Quitar ruta",
+    "addExternalContextsClick": "Añadir contextos externos, pulsa aquí",
+    "contextUsage": "Uso del contexto",
+    "approachingLimitRunCompactToContinue": " (Cerca del límite; ejecuta `/compact` para continuar)",
+    "saveInCustomSystemPrompt": "Guardar en las instrucciones personalizadas del sistema",
+    "scrollToTop": "Ir al principio",
+    "previousMessage": "Mensaje anterior",
+    "conversationDirectory": "Índice de la conversación",
+    "nextMessage": "Mensaje siguiente",
+    "scrollToBottom": "Ir al final",
+    "noUserPromptsInThisConversation": "No hay mensajes del usuario en esta conversación",
+    "collapse": "Contraer",
+    "expand": "Expandir",
+    "providerSettingsAreUnavailable": "Los ajustes del proveedor no están disponibles.",
+    "sharedEnvironment": "Entorno compartido",
+    "providerneutralRuntimeVariablesSharedAcrossAllProvidersUseThisForPATHProxyCertAndTempVariables": "Variables de ejecución compartidas por todos los proveedores. Úsalas para PATH, proxy, certificados y archivos temporales.",
+    "theNewConcurrentRunningSessionLimitWillApplyAsBusySessionsBecomeIdle": "El nuevo límite de sesiones simultáneas se aplicará cuando terminen las sesiones ocupadas.",
+    "failedToReleaseExcessWarmAgentProcesses": "No se pudieron liberar los procesos de agente sobrantes.",
+    "customLabelShownInTheModelSelectorLeaveEmptyToUseTheDefault": "Nombre mostrado en el selector de modelos. Déjalo vacío para usar el predeterminado.",
+    "eachLineMustFollowMapKeyAction": "Cada línea debe seguir el formato \"map <tecla> <acción>\"",
+    "navigationKeysMustBeUnique": "Las teclas de navegación no se pueden repetir",
+    "inlineEditUnavailableCouldNotAccessTheActiveEditorTryReopeningTheNote": "Edición no disponible: no se pudo acceder al editor activo. Intenta volver a abrir la nota.",
+    "failedToLoadVaultFilesVaultMentionsMayBeUnavailable": "No se pudieron cargar los archivos de la bóveda. Las menciones con @ pueden no estar disponibles.",
+    "insertInstructions": "Instrucciones para insertar...",
+    "editInstructions": "Instrucciones para editar...",
+    "inlineEditActions": "Acciones de edición",
+    "reject": "Rechazar",
+    "accept": "Aceptar",
+    "acceptEnter": "Aceptar con Intro",
+    "rejectEsc": "Rechazar con Esc",
+    "errorTryAgain": "Error, inténtalo de nuevo",
+    "replyToContinue": "Responde para continuar...",
+    "noResponseFromAgent": "El agente no respondió",
+    "inlineEditWasNotAppliedBecauseTheSourceDocumentOrSelectionChanged": "No se aplicó la edición porque cambió el documento o la selección original.",
+    "failedToAttachFileInvalidPath": "No se pudo adjuntar el archivo: ruta no válida",
+    "resumeConversation": "Reanudar conversación",
+    "loading": "Cargando…",
+    "noMatches": "Sin coincidencias",
+    "couldNotLoadSuggestions": "No se pudieron cargar las sugerencias",
+    "typeAgainToRetry": "Vuelve a escribir para reintentar",
+    "agents": "Agentes",
+    "loadingProviderCommands": "Cargando comandos del proveedor…",
+    "tryProviderCommandDiscoveryAgain": "Volver a buscar comandos del proveedor",
+    "addCustomInstruction": "Añadir instrucción personalizada",
+    "yourInput": "Tu entrada:",
+    "processingYourInstruction": "Procesando tu instrucción...",
+    "yourResponse": "Tu respuesta:",
+    "provideMoreDetails": "Añade más detalles...",
+    "refinedSnippet": "Instrucción mejorada:",
+    "submitResponse": "Enviar respuesta",
+    "editInstruction": "Editar instrucción",
+    "acceptInstruction": "Aceptar instrucción",
+    "unexpectedStorageError": "Error de almacenamiento inesperado",
+    "insert": "Insertar",
+    "failedToInsertSnippet": "No se pudo insertar el fragmento",
+    "failedToDeleteSnippet": "No se pudo eliminar el fragmento",
+    "chooseWhichModelsAreAvailableInTheChatSelectorDragToReorderThemTheProviderUsesTheFirstCurrentlyUsableModelAsItsDefaultSelectAtLeastOneModelToUseThisProvider": "Elige los modelos disponibles en el chat. Arrastra para ordenarlos; el proveedor usa como predeterminado el primer modelo disponible. Selecciona al menos uno para usar este proveedor.",
+    "visibleModels": "Modelos visibles",
+    "browseModels": "Explorar modelos",
+    "filterByModelProviderOrID": "Filtrar por modelo, proveedor o ID...",
+    "discover": "Descubrir",
+    "visible": "Visibles: ",
+    "loadingModels": "Cargando modelos...",
+    "noModelsDiscoveredYet": "Aún no se han encontrado modelos",
+    "loading2": "Cargando...",
+    "dragOrUseArrowKeysToReorder": "Arrastra o usa las flechas para cambiar el orden",
+    "default": "Predeterminado",
+    "aliasOptional": "Alias opcional",
+    "noModelsMatchYourFilter": "Ningún modelo coincide con el filtro.",
+    "unavailable": "No disponible",
+    "safe": "Seguro",
+    "yolo": "YOLO · sin confirmación",
+    "completionFlavor0": "Horneado",
+    "completionFlavor1": "Cocinado",
+    "completionFlavor2": "Procesado",
+    "completionFlavor3": "Preparado",
+    "completionFlavor4": "Elaborado",
+    "completionFlavor5": "Forjado",
+    "completionFlavor6": "Conjurado",
+    "completionFlavor7": "Preparado",
+    "completionFlavor8": "Mezclado",
+    "completionFlavor9": "Cocinado a fuego lento",
+    "completionFlavor10": "Tostado",
+    "completionFlavor11": "Salteado",
+    "completionFlavor12": "Resuelto",
+    "completionFlavor13": "Marinado",
+    "completionFlavor14": "Destilado",
+    "completionFlavor15": "Fermentado",
+    "completionFlavor16": "Filtrado",
+    "completionFlavor17": "Reposado",
+    "completionFlavor18": "Asado",
+    "completionFlavor19": "Curado",
+    "completionFlavor20": "Ahumado",
+    "completionFlavor21": "Razonado",
+    "thinkingFlavor0": "Pensando...",
+    "thinkingFlavor1": "Reflexionando...",
+    "thinkingFlavor2": "Procesando...",
+    "thinkingFlavor3": "Analizando...",
+    "thinkingFlavor4": "Considerando...",
+    "thinkingFlavor5": "Trabajando en ello...",
+    "thinkingFlavor6": "Siguiendo el ritmo...",
+    "thinkingFlavor7": "Un momento...",
+    "thinkingFlavor8": "En ello...",
+    "thinkingFlavor9": "Dándole vueltas...",
+    "thinkingFlavor10": "Contemplando...",
+    "thinkingFlavor11": "Reflexionando...",
+    "thinkingFlavor12": "Pensándolo...",
+    "thinkingFlavor13": "Déjame pensar...",
+    "thinkingFlavor14": "Mmm...",
+    "thinkingFlavor15": "Razonando...",
+    "thinkingFlavor16": "Deliberando...",
+    "thinkingFlavor17": "Sopesando opciones...",
+    "thinkingFlavor18": "Ordenando ideas...",
+    "thinkingFlavor19": "Preparando ideas...",
+    "thinkingFlavor20": "Conectando ideas...",
+    "thinkingFlavor21": "Reuniendo ideas...",
+    "thinkingFlavor22": "Activando neuronas...",
+    "thinkingFlavor23": "Cargando inspiración...",
+    "thinkingFlavor24": "Consultando el oráculo...",
+    "thinkingFlavor25": "Reuniendo conocimientos...",
+    "thinkingFlavor26": "Procesando ideas...",
+    "thinkingFlavor27": "Despertando neuronas...",
+    "thinkingFlavor28": "Domando ideas...",
+    "thinkingFlavor29": "Agrupando pensamientos...",
+    "thinkingFlavor30": "Combinando conceptos...",
+    "thinkingFlavor31": "Desenredando esto...",
+    "thinkingFlavor32": "Uniendo las piezas...",
+    "thinkingFlavor33": "Tomando café...",
+    "thinkingFlavor34": "Entrando en calor...",
+    "thinkingFlavor35": "Familiarizándome con esto...",
+    "thinkingFlavor36": "Acomodándome...",
+    "thinkingFlavor37": "Preparando té...",
+    "thinkingFlavor38": "Buscando algo para picar...",
+    "thinkingFlavor39": "Interpretando...",
+    "thinkingFlavor40": "Compilando ideas...",
+    "thinkingFlavor41": "Razonando...",
+    "thinkingFlavor42": "Consultando el vacío...",
+    "thinkingFlavor43": "Ordenando la mente...",
+    "thinkingFlavor44": "Reservando memoria...",
+    "thinkingFlavor45": "Optimizando...",
+    "thinkingFlavor46": "Indexando...",
+    "thinkingFlavor47": "Sincronizando neuronas...",
+    "thinkingFlavor48": "Respirando...",
+    "thinkingFlavor49": "Buscando claridad...",
+    "thinkingFlavor50": "Concentrándome...",
+    "thinkingFlavor51": "Centrando la atención...",
+    "thinkingFlavor52": "Alineando los chakras...",
+    "thinkingFlavor53": "Meditando sobre esto...",
+    "thinkingFlavor54": "Preguntando a las estrellas...",
+    "thinkingFlavor55": "Leyendo las hojas de té...",
+    "thinkingFlavor56": "Agitando la bola mágica...",
+    "thinkingFlavor57": "Consultando pergaminos antiguos...",
+    "thinkingFlavor58": "Descifrando la matriz...",
+    "thinkingFlavor59": "Conectando con el éter...",
+    "thinkingFlavor60": "Mirando al abismo...",
+    "thinkingFlavor61": "Consultando al cosmos...",
+    "thinkingFlavor62": "Entrando en materia...",
+    "thinkingFlavor63": "Manos a la obra...",
+    "thinkingFlavor64": "Empezando a trabajar...",
+    "thinkingFlavor65": "Abordando esto...",
+    "thinkingFlavor66": "Siguiendo la pista...",
+    "thinkingFlavor67": "Investigando...",
+    "thinkingFlavor68": "Explorando...",
+    "thinkingFlavor69": "Profundizando...",
+    "thinkingFlavor70": "Dame un momento...",
+    "thinkingFlavor71": "Un poco de paciencia...",
+    "thinkingFlavor72": "Un segundo...",
+    "thinkingFlavor73": "Haciendo magia...",
+    "thinkingFlavor74": "Ya casi...",
+    "thinkingFlavor75": "Dame un momento...",
+    "completedIn": "* {word} durante {duration}",
+    "thinkingSeconds": "Pensando {seconds} s...",
+    "thoughtSeconds": "Razonó durante {seconds} s",
+    "helloName": "Hola {name}, ¿cómo estás?",
+    "nameReturns": "¡{name} ha vuelto!",
+    "loadMoreRemainingFormat": "Cargar más, quedan {p0}",
+    "newChatForFormat": "Nueva conversación para {p0}",
+    "runningInFormat": "En ejecución en {p0}",
+    "currentTabFormat": "Pestaña actual {p0}",
+    "openInFormat": "Abierta en {p0}",
+    "tabFormat": "pestaña {p0}",
+    "approvalDetailsFormat": "Detalles del permiso para {p0}",
+    "unexpectedApprovalSelectionFormat": "Selección de permiso inesperada: \"{p0}\"",
+    "isNotSupportedByThisProviderFormat": "Este proveedor no admite /{p0}.",
+    "addedExternalContextFormat": "Contexto externo añadido: {p0}",
+    "unknownCommandFormat": "Comando desconocido: {p0}",
+    "failedToOpenConversationFormat": "No se pudo abrir la conversación: {p0}",
+    "escToInterruptFormat": " (Esc para interrumpir · {p0})",
+    "theConcurrentRunningSessionLimitIsCurrentlyInUseFormat": "El límite de {p0} sesiones simultáneas está ocupado. ",
+    "missingContentFormat": "{p0} · Contenido no encontrado",
+    "linkedContentMissingContentFormat": "Contenido vinculado: {p0}. Contenido no encontrado",
+    "linkedContentFormat": "Contenido vinculado: {p0}",
+    "linkedContentIsMissingFormat": "No se encuentra el contenido vinculado: {p0}",
+    "failedToOpenLinkedContentFormat": "No se pudo abrir el contenido vinculado: {p0}",
+    "memoryUsedFormat": "Memoria utilizada, {p0} {p1}",
+    "moreLinesFormat": "... {p0} líneas más",
+    "couldNotReadPlanFileApproveNewSessionWillNotIncludePlanDetailsFormat": "No se pudo leer el archivo del plan: {p0}. La aprobación en una sesión nueva no incluirá los detalles del plan.",
+    "forFormat": "* {p0} durante {p1}",
+    "previewFormat": "Vista previa de {p0}",
+    "subagentTaskStatusClickToExpandFormat": "Tarea de subagente: {p0}. Estado: {p1}. Pulsa para expandir",
+    "backgroundTaskClickToExpandFormat": "Tarea en segundo plano: {p0}. {p1}. Pulsa para expandir",
+    "backgroundTaskInitializingClickToExpandFormat": "Tarea en segundo plano: {p0}. Iniciando. Pulsa para expandir",
+    "tasksFormat": "Tareas {p0}/{p1}",
+    "tasksFormat2": "Tareas ({p0}/{p1})",
+    "openFormat": "Abrir {p0}",
+    "findFormat": "Buscar \"{p0}\"",
+    "inFormat": " en {p0}",
+    "altQueryFormat": "Consulta alternativa: {p0}",
+    "moreQueriesFormat": "... {p0} consultas más",
+    "moreCharactersFormat": "... {p0} caracteres más",
+    "commandTimedOutAfterSFormat": "El comando agotó el tiempo de espera de {p0} s",
+    "failedToSwitchToTabAndRestoreThePreviousTabFormat": "No se pudo cambiar a la pestaña {p0} ni restaurar la anterior",
+    "failedToCloseTabCleanlyFormat": "No se pudo cerrar correctamente la pestaña {p0}",
+    "commandFailedFormat": "El comando falló: {p0}",
+    "showMoreContextItemsFormat": "Mostrar {p0} elementos de contexto más",
+    "removeFormat": "Quitar {p0}",
+    "imageExceedsLimitFormat": "La imagen supera el límite de {p0}.",
+    "imageFormat": "Imagen {p0}",
+    "imageAttachmentFormat": "Imagen adjunta: {p0}",
+    "fileNotFoundFormat": "{p0} (Archivo no encontrado)",
+    "permissionDeniedFormat": "{p0} (Permiso denegado)",
+    "imagePreviewFormat": "Vista previa de imagen: {p0}",
+    "fastModeFormat": "Modo rápido: {p0}",
+    "removedInvalidExternalContextPathsFormat": "Se quitaron {p0} rutas de contexto externo no válidas: {p1}",
+    "cannotPersistDirectoryNoLongerExistsFormat": "No se puede conservar \"{p0}\": la carpeta ya no existe",
+    "cannotAddItsInsideExistingPathFormat": "No se puede añadir \"{p0}\": está dentro de la ruta existente \"{p1}\"",
+    "cannotAddItContainsExistingPathFormat": "No se puede añadir \"{p0}\": contiene la ruta existente \"{p1}\"",
+    "externalContextClickToAddMoreFormat": "Contextos externos: {p0}{p1}. Pulsa para añadir más",
+    "taskListOfCompletedFormat": "{p0} lista de tareas, {p1} de {p2} completadas",
+    "inlineEditUnavailableFailedToInitializeTheProviderFormat": "Edición no disponible: no se pudo iniciar el proveedor {p0}.",
+    "inlineEditFormat": "{p0} edición",
+    "aliasForFormat": "Alias de {p0}",
+    "contextWindowForFormat": "Ventana de contexto de {p0}",
+    "deleteEnvironmentSnippetFormat": "¿Eliminar el fragmento de entorno \"{p0}\"?",
+    "environmentSnippetSavedFormat": "Fragmento de entorno \"{p0}\" guardado",
+    "environmentSnippetUpdatedFormat": "Fragmento de entorno \"{p0}\" actualizado",
+    "environmentSnippetDeletedFormat": "Fragmento de entorno \"{p0}\" eliminado",
+    "reviewEnvironmentOwnershipForFormat": "Revisa a qué entorno corresponden: {p0}",
+    "ofDiscoveredFormat": " de {p0} encontrados | {p1} {p2}",
+    "ofDiscoveredFormat2": " de {p0} encontrados",
+    "selectedFormat": "Seleccionados ({p0})",
+    "clearAllSelectedModelsFormat": "Quitar todos los modelos seleccionados de {p0}",
+    "reorderDragOrUseTheUpAndDownArrowKeysFormat": "Cambiar el orden de {p0}; arrastra o usa las flechas arriba y abajo",
+    "allProvidersFormat": "Todos los proveedores ({p0})",
+    "configuredModelNotCurrentlyReportedByFormat": "Modelo configurado que {p0} no ofrece actualmente",
+    "openChatView": "Abrir chat",
+    "inlineEdit": "Editar en la nota",
+    "inlineEditorUnavailable": "Edición no disponible: no se pudo acceder a la nota Markdown activa.",
+    "inserted": "Insertado",
+    "editApplied": "Edición aplicada",
+    "new": "Nueva",
+    "replaceConversation": "Reemplazar la conversación actual",
+    "closeCurrentTab": "Cerrar la pestaña actual",
+    "copyStartupDiagnostics": "Copiar diagnóstico de inicio",
+    "startupDiagnosticsCopied": "Diagnóstico de inicio copiado al portapapeles.",
+    "startupDiagnosticsFailed": "No se pudo copiar el diagnóstico de inicio.",
+    "linkedPathsFailed": "No se pudieron actualizar las rutas del contenido vinculado",
+    "linkedPinsFailed": "No se pudo actualizar el contenido vinculado fijado",
+    "obsoleteConfigFailed": "No se pudo eliminar la configuración obsoleta de Claude",
+    "defaultModel": "Modelo predeterminado",
+    "defaultModelDescription": "Se usa cuando una conversación nueva necesita un modelo alternativo de Claude.",
+    "learnMore": "Más información",
+    "safeDefault": "Predeterminado",
+    "safeAcceptEdits": "Aceptar ediciones",
+    "safeAuto": "Automático",
+    "effortlow": "Bajo",
+    "effortmedium": "Medio",
+    "efforthigh": "Alto",
+    "effortxhigh": "Muy alto",
+    "effortmax": "Máximo",
+    "commandNew": "Iniciar una conversación nueva",
+    "commandAddDirectory": "Añadir una carpeta de contexto externo",
+    "commandResume": "Reanudar una conversación anterior",
+    "commandFork": "Ramificar toda la conversación en una sesión nueva",
+    "commandFast": "Cambiar el modo rápido",
+    "commandInstruction": "Guardar una instrucción personalizada reutilizable",
+    "provider": "proveedor",
+    "providers": "proveedores",
+    "source": "fuente",
+    "sources": "fuentes",
+    "openClaudian": "Abrir Claudian",
+    "actionUnknown": "(desconocido)",
+    "statusBlocked": "Estado: bloqueado",
+    "tabActive": "activa",
+    "tabWorking": "trabajando",
+    "tabIdle": "inactiva",
+    "modelHaiku": "Rápido y eficiente",
+    "modelSonnet": "Rendimiento equilibrado",
+    "modelOpus": "El más capaz",
+    "modelFable": "El modelo más capaz de Anthropic, con un precio superior a Opus",
+    "claudeCommandsUnavailable": "El catálogo de comandos de Claude no está disponible.",
+    "claudeCommandsEmpty": "No hay comandos ni habilidades configurados. Pulsa + para crear uno.",
+    "claudePluginsHeader": "Complementos de Claude Code",
+    "claudePluginsEmpty": "No se encontraron complementos de Claude Code. Actívalos desde la CLI de Claude.",
+    "claudeProjectPlugins": "Complementos del proyecto",
+    "claudeUserPlugins": "Complementos del usuario",
+    "pluginDisable": "Desactivar",
+    "pluginEnable": "Activar",
+    "claudeEnvironmentDescription": "Solo variables del entorno de ejecución de Claude. Usa este campo para ANTHROPIC_* y opciones específicas de Claude.",
+    "actionRun": "Ejecutar comando: {pattern}",
+    "actionRead": "Leer archivo: {pattern}",
+    "actionWrite": "Escribir en archivo: {pattern}",
+    "actionEdit": "Editar archivo: {pattern}",
+    "actionGlob": "Buscar archivos que coincidan: {pattern}",
+    "actionGrep": "Buscar contenido que coincida: {pattern}"
   }
 }
diff --git a/src/main.ts b/src/main.ts
index 2d550cf..e9a66bb 100644
--- a/src/main.ts
+++ b/src/main.ts
@@ -265,12 +265,12 @@ export default class ClaudianPlugin extends Plugin {
       registerFileMenu(this);
       this.registerEvent(this.app.vault.on('rename', (file, oldPath) => {
         void this.handleLinkedContentRename(file, oldPath).catch(() => {
-          new Notice('Failed to update linked content paths');
+          new Notice(t('ui.linkedPathsFailed'));
         });
       }));
       this.registerEvent(this.app.vault.on('delete', (file) => {
         void this.handlePinnedLinkedContentDeleted(file).catch(() => {
-          new Notice('Failed to update pinned linked content');
+          new Notice(t('ui.linkedPinsFailed'));
         });
       }));
       this.registerEvent(this.app.vault.on('create', (file) => {
@@ -280,13 +280,13 @@ export default class ClaudianPlugin extends Plugin {
         this.notifyConversationViewsChanged();
       }));
 
-      this.addRibbonIcon('bot', 'Open Claudian', () => {
+      this.addRibbonIcon('bot', t('ui.openClaudian'), () => {
         void this.activateView();
       });
 
       this.addCommand({
         id: 'open-view',
-        name: 'Open chat view',
+        name: t('ui.openChatView'),
         callback: () => {
           void this.activateView();
         },
@@ -334,13 +334,13 @@ export default class ClaudianPlugin extends Plugin {
 
       this.addCommand({
         id: 'inline-edit',
-        name: 'Inline edit',
+        name: t('ui.inlineEdit'),
         editorCallback: async (editor: Editor, ctx) => {
           const view = ctx instanceof MarkdownView
             ? ctx
             : this.app.workspace.getActiveViewOfType(MarkdownView);
           if (!view) {
-            new Notice('Inline edit unavailable: could not access the active Markdown view.');
+            new Notice(t('ui.inlineEditorUnavailable'));
             return;
           }
 
@@ -373,14 +373,14 @@ export default class ClaudianPlugin extends Plugin {
           const result = await modal.openAndWait();
 
           if (result.decision === 'accept' && result.editedText !== undefined) {
-            new Notice(editContext.mode === 'cursor' ? 'Inserted' : 'Edit applied');
+            new Notice(editContext.mode === 'cursor' ? t('ui.inserted') : t('ui.editApplied'));
           }
         },
       });
 
       this.addCommand({
         id: 'new-tab',
-        name: 'New',
+        name: t('ui.new'),
         checkCallback: (checking: boolean) => {
           if (!this.canCreateNewTab()) return false;
 
@@ -393,7 +393,7 @@ export default class ClaudianPlugin extends Plugin {
 
       this.addCommand({
         id: 'new-session',
-        name: 'Replace current conversation',
+        name: t('ui.replaceConversation'),
         checkCallback: (checking: boolean) => {
           const view = this.getView();
           if (!view) return false;
@@ -416,7 +416,7 @@ export default class ClaudianPlugin extends Plugin {
 
       this.addCommand({
         id: 'close-current-tab',
-        name: 'Close current tab',
+        name: t('ui.closeCurrentTab'),
         checkCallback: (checking: boolean) => {
           const view = this.getView();
           if (!view) return false;
@@ -437,10 +437,10 @@ export default class ClaudianPlugin extends Plugin {
 
       this.addCommand({
         id: 'copy-startup-diagnostics',
-        name: 'Copy startup diagnostics',
+        name: t('ui.copyStartupDiagnostics'),
         callback: async () => {
           const copied = await StartupProfiler.copyToClipboard();
-          new Notice(copied ? 'Startup diagnostics copied to clipboard.' : 'Failed to copy startup diagnostics.');
+          new Notice(copied ? t('ui.startupDiagnosticsCopied') : t('ui.startupDiagnosticsFailed'));
         },
       });
 
@@ -1194,7 +1194,7 @@ export default class ClaudianPlugin extends Plugin {
     try {
       await deleteLegacyMcpConfig(sharedStorage.getAdapter());
     } catch {
-      new Notice('Failed to remove obsolete Claude configuration');
+      new Notice(t('ui.obsoleteConfigFailed'));
     }
     const { claudian } = await sharedStorage.initialize();
     this.settings = {
diff --git a/src/providers/claude/modelTiers.ts b/src/providers/claude/modelTiers.ts
index 3169aa8..a54b1b9 100644
--- a/src/providers/claude/modelTiers.ts
+++ b/src/providers/claude/modelTiers.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 interface ClaudeModelVersion {
   major: number;
   minor: number;
@@ -8,7 +10,7 @@ export const CLAUDE_MODEL_TIER_DEFINITIONS = [
     id: 'haiku',
     label: 'Haiku',
     agentLabel: 'Haiku',
-    description: 'Fast and efficient',
+    get description() { return t('ui.modelHaiku'); },
     environmentKey: 'ANTHROPIC_DEFAULT_HAIKU_MODEL',
     environmentPriority: 4,
     agentOrder: 3,
@@ -23,7 +25,7 @@ export const CLAUDE_MODEL_TIER_DEFINITIONS = [
     id: 'sonnet',
     label: 'Sonnet',
     agentLabel: 'Sonnet',
-    description: 'Balanced performance',
+    get description() { return t('ui.modelSonnet'); },
     environmentKey: 'ANTHROPIC_DEFAULT_SONNET_MODEL',
     environmentPriority: 3,
     agentOrder: 1,
@@ -38,7 +40,7 @@ export const CLAUDE_MODEL_TIER_DEFINITIONS = [
     id: 'opus',
     label: 'Opus',
     agentLabel: 'Opus',
-    description: 'Most capable',
+    get description() { return t('ui.modelOpus'); },
     environmentKey: 'ANTHROPIC_DEFAULT_OPUS_MODEL',
     environmentPriority: 2,
     agentOrder: 2,
@@ -53,7 +55,7 @@ export const CLAUDE_MODEL_TIER_DEFINITIONS = [
     id: 'fable',
     label: 'Fable ($$$)',
     agentLabel: 'Fable',
-    description: "Anthropic's most capable model — premium pricing above Opus",
+    get description() { return t('ui.modelFable'); },
     environmentKey: 'ANTHROPIC_DEFAULT_FABLE_MODEL',
     environmentPriority: 1,
     agentOrder: 4,
diff --git a/src/providers/claude/types/models.ts b/src/providers/claude/types/models.ts
index 8c941d4..1893e41 100644
--- a/src/providers/claude/types/models.ts
+++ b/src/providers/claude/types/models.ts
@@ -20,10 +20,10 @@ import {
 export type ClaudeModel = string;
 
 export const DEFAULT_CLAUDE_MODELS: { value: ClaudeModel; label: string; description: string }[] =
-  CLAUDE_MODEL_TIER_DEFINITIONS.map(({ id, label, description }) => ({
-    value: id,
-    label,
-    description,
+  CLAUDE_MODEL_TIER_DEFINITIONS.map(definition => ({
+    value: definition.id,
+    label: definition.label,
+    get description() { return definition.description; },
   }));
 
 /** Effort levels for adaptive thinking models. */
diff --git a/src/providers/claude/ui/ClaudeChatUIConfig.ts b/src/providers/claude/ui/ClaudeChatUIConfig.ts
index 51d22bc..7916b50 100644
--- a/src/providers/claude/ui/ClaudeChatUIConfig.ts
+++ b/src/providers/claude/ui/ClaudeChatUIConfig.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 import { DEFAULT_REASONING_VALUE } from '../../../core/providers/reasoning';
 import type {
   ProviderChatUIConfig,
@@ -27,9 +29,9 @@ import {
 
 const CLAUDE_PERMISSION_MODE_TOGGLE: ProviderPermissionModeToggleConfig = {
   inactiveValue: 'normal',
-  inactiveLabel: 'Safe',
+  get inactiveLabel() { return t('ui.safe'); },
   activeValue: 'yolo',
-  activeLabel: 'YOLO',
+  get activeLabel() { return t('ui.yolo'); },
   planValue: 'plan',
   planLabel: 'PLAN',
 };
@@ -63,7 +65,7 @@ export const claudeChatUIConfig: ProviderChatUIConfig = {
     const levels = supportsXHighEffort(runtimeModel)
       ? EFFORT_LEVELS
       : EFFORT_LEVELS.filter(e => e.value !== 'xhigh');
-    return levels.map(e => ({ value: e.value, label: e.label }));
+    return levels.map(e => ({ value: e.value, label: t(`ui.effort${e.value}`) }));
   },
 
   getDefaultReasoningValue(model: string, _settings: Record<string, unknown>): string {
diff --git a/src/providers/claude/ui/ClaudeSettingsTab.ts b/src/providers/claude/ui/ClaudeSettingsTab.ts
index b0ad2e1..1f9f0d9 100644
--- a/src/providers/claude/ui/ClaudeSettingsTab.ts
+++ b/src/providers/claude/ui/ClaudeSettingsTab.ts
@@ -146,8 +146,8 @@ export const claudeSettingsTabRenderer: ProviderSettingsTabRenderer = {
     new Setting(container).setName(t('settings.models')).setHeading();
 
     new Setting(container)
-      .setName('Default model')
-      .setDesc('Used when a new conversation needs a Claude fallback model.')
+      .setName(t('ui.defaultModel'))
+      .setDesc(t('ui.defaultModelDescription'))
       .addDropdown((dropdown) => {
         for (const option of getClaudeModelOptions(settingsBag)) {
           dropdown.addOption(option.value, option.label);
@@ -211,7 +211,7 @@ export const claudeSettingsTabRenderer: ProviderSettingsTabRenderer = {
       .setDesc(t('settings.claudeSafeMode.desc'))
       .addDropdown((dropdown) => {
         for (const mode of CLAUDE_SAFE_MODES) {
-          dropdown.addOption(mode, mode);
+          dropdown.addOption(mode, mode === 'default' ? t('ui.safeDefault') : mode === 'acceptEdits' ? t('ui.safeAcceptEdits') : t('ui.safeAuto'));
         }
         dropdown
           .setValue(claudeSettings.safeMode)
@@ -246,7 +246,7 @@ export const claudeSettingsTabRenderer: ProviderSettingsTabRenderer = {
     const descP = slashCommandsDesc.createEl('p', { cls: 'setting-item-description' });
     descP.appendText(t('settings.slashCommands.desc') + ' ');
     descP.createEl('a', {
-      text: 'Learn more',
+      text: t('ui.learnMore'),
       href: 'https://code.claude.com/docs/en/skills',
     });
 
@@ -320,7 +320,7 @@ export const claudeSettingsTabRenderer: ProviderSettingsTabRenderer = {
       scope: 'provider:claude',
       heading: t('settings.environment'),
       name: t('settings.customVariables.name'),
-      desc: 'Claude-owned runtime variables only. Use this for ANTHROPIC_* and Claude-specific toggles.',
+      desc: t('ui.claudeEnvironmentDescription'),
       placeholder: 'ANTHROPIC_API_KEY=your-key\nANTHROPIC_BASE_URL=https://api.example.com\nANTHROPIC_MODEL=custom-model\nCLAUDE_CODE_USE_BEDROCK=1',
       renderCustomContextLimits: (target) => context.renderCustomContextLimits(target, 'claude'),
     });
diff --git a/src/providers/claude/ui/PluginSettingsManager.ts b/src/providers/claude/ui/PluginSettingsManager.ts
index e1c3407..72e1c24 100644
--- a/src/providers/claude/ui/PluginSettingsManager.ts
+++ b/src/providers/claude/ui/PluginSettingsManager.ts
@@ -1,5 +1,7 @@
 import { Notice, setIcon } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import type {
   AppAgentManager,
   AppPluginManager,
@@ -31,11 +33,11 @@ export class PluginSettingsManager {
     this.containerEl.empty();
 
     const headerEl = this.containerEl.createDiv({ cls: 'claudian-plugin-header' });
-    headerEl.createSpan({ text: 'Claude Code Plugins', cls: 'claudian-plugin-label' });
+    headerEl.createSpan({ text: t('ui.claudePluginsHeader'), cls: 'claudian-plugin-label' });
 
     const refreshBtn = headerEl.createEl('button', {
       cls: 'claudian-settings-action-btn',
-      attr: { 'aria-label': 'Refresh' },
+      attr: { 'aria-label': t('common.refresh') },
     });
     setIcon(refreshBtn, 'refresh-cw');
     refreshBtn.addEventListener('click', () => {
@@ -46,7 +48,7 @@ export class PluginSettingsManager {
 
     if (plugins.length === 0) {
       const emptyEl = this.containerEl.createDiv({ cls: 'claudian-plugin-empty' });
-      emptyEl.setText('No Claude code plugins found. Enable plugins via the Claude CLI.');
+      emptyEl.setText(t('ui.claudePluginsEmpty'));
       return;
     }
 
@@ -57,7 +59,7 @@ export class PluginSettingsManager {
 
     if (projectPlugins.length > 0) {
       const sectionHeader = listEl.createDiv({ cls: 'claudian-plugin-section-header' });
-      sectionHeader.setText('Project plugins');
+      sectionHeader.setText(t('ui.claudeProjectPlugins'));
 
       for (const plugin of projectPlugins) {
         this.renderPluginItem(listEl, plugin);
@@ -66,7 +68,7 @@ export class PluginSettingsManager {
 
     if (userPlugins.length > 0) {
       const sectionHeader = listEl.createDiv({ cls: 'claudian-plugin-section-header' });
-      sectionHeader.setText('User plugins');
+      sectionHeader.setText(t('ui.claudeUserPlugins'));
 
       for (const plugin of userPlugins) {
         this.renderPluginItem(listEl, plugin);
@@ -98,7 +100,7 @@ export class PluginSettingsManager {
 
     const toggleBtn = actionsEl.createEl('button', {
       cls: 'claudian-plugin-action-btn',
-      attr: { 'aria-label': plugin.enabled ? 'Disable' : 'Enable' },
+      attr: { 'aria-label': plugin.enabled ? t('ui.pluginDisable') : t('ui.pluginEnable') },
     });
     setIcon(toggleBtn, plugin.enabled ? 'toggle-right' : 'toggle-left');
     toggleBtn.addEventListener('click', () => {
diff --git a/src/providers/claude/ui/SlashCommandSettings.ts b/src/providers/claude/ui/SlashCommandSettings.ts
index 53841f2..a238394 100644
--- a/src/providers/claude/ui/SlashCommandSettings.ts
+++ b/src/providers/claude/ui/SlashCommandSettings.ts
@@ -329,7 +329,7 @@ export class SlashCommandSettings {
   private renderUnavailable(): void {
     this.containerEl.empty();
     const emptyEl = this.containerEl.createDiv({ cls: 'claudian-sp-empty-state' });
-    emptyEl.setText('Claude command catalog is unavailable.');
+    emptyEl.setText(t('ui.claudeCommandsUnavailable'));
   }
 
   private render(): void {
@@ -342,14 +342,14 @@ export class SlashCommandSettings {
 
     const addBtn = actionsEl.createEl('button', {
       cls: 'claudian-settings-action-btn',
-      attr: { 'aria-label': 'Add' },
+      attr: { 'aria-label': t('common.add') },
     });
     setIcon(addBtn, 'plus');
     addBtn.addEventListener('click', () => this.openCommandModal(null));
 
     if (this.commands.length === 0) {
       const emptyEl = this.containerEl.createDiv({ cls: 'claudian-sp-empty-state' });
-      emptyEl.setText('No commands or skills configured. Click + to create one.');
+      emptyEl.setText(t('ui.claudeCommandsEmpty'));
       return;
     }
 
diff --git a/src/shared/components/CopyableCodeFence.ts b/src/shared/components/CopyableCodeFence.ts
index 41142fc..b732504 100644
--- a/src/shared/components/CopyableCodeFence.ts
+++ b/src/shared/components/CopyableCodeFence.ts
@@ -1,5 +1,7 @@
 import { setIcon } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 export interface CopyableCodeFenceOptions {
   readonly copiedLabel?: string;
   readonly copyLabel: string;
@@ -57,7 +59,7 @@ export function renderCopyableCodeFence(
     () => text,
     () => {
       copyButton.empty();
-      copyButton.setText(options.copiedLabel ?? 'Copied!');
+      copyButton.setText(options.copiedLabel ?? t('ui.copied'));
       copyButton.classList.add('copied');
     },
     renderIdle,
@@ -87,7 +89,7 @@ export function enhanceRenderedCodeFence(pre: HTMLPreElement): HTMLElement {
     bindCopyFeedback(
       label,
       () => code.textContent ?? '',
-      () => label.setText('Copied!'),
+      () => label.setText(t('ui.copied')),
       () => label.setText(language),
     );
   }
diff --git a/src/shared/components/ResumeSessionDropdown.ts b/src/shared/components/ResumeSessionDropdown.ts
index 04c5921..4f20b19 100644
--- a/src/shared/components/ResumeSessionDropdown.ts
+++ b/src/shared/components/ResumeSessionDropdown.ts
@@ -4,9 +4,10 @@
  * Dropup UI for selecting a previous conversation to resume.
  * Shown when the /resume built-in command is executed.
  */
-
 import { setIcon } from 'obsidian';
 
+import { getLocale, t } from '@/i18n/i18n';
+
 import type { ConversationMeta } from '../../core/types';
 
 export interface ResumeSessionDropdownCallbacks {
@@ -163,15 +164,15 @@ export class ResumeSessionDropdown {
     this.dropdownEl.empty();
 
     const header = this.dropdownEl.createDiv({ cls: 'claudian-resume-header' });
-    header.createSpan({ text: 'Resume conversation' });
+    header.createSpan({ text: t('ui.resumeConversation') });
 
     const list = this.dropdownEl.createDiv({ cls: 'claudian-resume-list' });
     list.setAttribute('id', this.listboxId);
     list.setAttribute('role', 'listbox');
-    list.setAttribute('aria-label', 'Resume conversation');
+    list.setAttribute('aria-label', t('ui.resumeConversation'));
 
     if (this.conversations.length === 0) {
-      list.createDiv({ cls: 'claudian-resume-empty', text: 'No conversations' });
+      list.createDiv({ cls: 'claudian-resume-empty', text: t('ui.noConversations') });
       this.inputEl.removeAttribute('aria-activedescendant');
       return;
     }
@@ -194,7 +195,7 @@ export class ResumeSessionDropdown {
       titleEl.setAttribute('title', conv.title);
       content.createDiv({
         cls: 'claudian-resume-item-date',
-        text: isCurrent ? 'Current session' : this.formatDate(conv.lastActivityAt),
+        text: isCurrent ? t('ui.currentSession') : this.formatDate(conv.lastActivityAt),
       });
 
       item.addEventListener('click', () => {
@@ -237,8 +238,8 @@ export class ResumeSessionDropdown {
     const now = new Date();
 
     if (date.toDateString() === now.toDateString()) {
-      return date.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit', hour12: false });
+      return date.toLocaleTimeString(getLocale(), { hour: '2-digit', minute: '2-digit', hour12: false });
     }
-    return date.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
+    return date.toLocaleDateString(getLocale(), { month: 'short', day: 'numeric' });
   }
 }
diff --git a/src/shared/composer-dropdown/ComposerDropdownController.ts b/src/shared/composer-dropdown/ComposerDropdownController.ts
index a27c747..68e7643 100644
--- a/src/shared/composer-dropdown/ComposerDropdownController.ts
+++ b/src/shared/composer-dropdown/ComposerDropdownController.ts
@@ -1,3 +1,5 @@
+import { t } from '@/i18n/i18n';
+
 import { ComposerDropdownView } from './ComposerDropdownView';
 import type {
   ComposerDropdownFolderItem,
@@ -182,7 +184,7 @@ export class ComposerDropdownController {
     }
 
     const generation = this.invalidateActiveLoad();
-    this.items = [{ id: 'loading', kind: 'status', label: 'Loading…', state: 'loading' }];
+    this.items = [{ id: 'loading', kind: 'status', label: t('ui.loading'), state: 'loading' }];
     this.selectedIndex = -1;
     this.view.render(this.items, this.selectedIndex);
 
@@ -218,7 +220,7 @@ export class ComposerDropdownController {
       if (this.destroyed || controller.signal.aborted || generation !== this.generation) return;
       this.items = items.length > 0
         ? items
-        : [{ id: 'empty', kind: 'status', label: 'No matches', state: 'empty' }];
+        : [{ id: 'empty', kind: 'status', label: t('ui.noMatches'), state: 'empty' }];
       this.selectedIndex = this.findSelectable(0, 1, true);
       this.view.render(this.items, this.selectedIndex);
     } catch {
@@ -226,8 +228,8 @@ export class ComposerDropdownController {
       this.items = [{
         id: 'error',
         kind: 'status',
-        label: 'Could not load suggestions',
-        detail: 'Type again to retry',
+        label: t('ui.couldNotLoadSuggestions'),
+        detail: t('ui.typeAgainToRetry'),
         state: 'error',
       }];
       this.selectedIndex = -1;
diff --git a/src/shared/composer-dropdown/MentionSource.ts b/src/shared/composer-dropdown/MentionSource.ts
index bca3a07..5adad96 100644
--- a/src/shared/composer-dropdown/MentionSource.ts
+++ b/src/shared/composer-dropdown/MentionSource.ts
@@ -1,6 +1,7 @@
 import type { TFile } from 'obsidian';
 
 import type { AgentMentionProvider } from '@/core/providers/types';
+import { t } from '@/i18n/i18n';
 import { buildExternalContextDisplayEntries } from '@/utils/externalContext';
 import { externalContextScanner } from '@/utils/externalContextScanner';
 
@@ -183,7 +184,7 @@ export class MentionSource implements ComposerDropdownSource {
       id: 'agents',
       inputPrefix: 'Agents/',
       kind: 'folder',
-      label: 'Agents',
+      label: t('ui.agents'),
       load: query => (this.agentService?.searchAgents(query) ?? []).map(agent => ({
         className: 'is-agent',
         detail: agent.description,
diff --git a/src/shared/composer-dropdown/SlashCommandSource.ts b/src/shared/composer-dropdown/SlashCommandSource.ts
index 2045e1f..db9e9d0 100644
--- a/src/shared/composer-dropdown/SlashCommandSource.ts
+++ b/src/shared/composer-dropdown/SlashCommandSource.ts
@@ -4,6 +4,7 @@ import type { ProviderCommandDiscoverySource } from '@/core/providers/commands/P
 import type { ProviderCommandEntry } from '@/core/providers/commands/ProviderCommandEntry';
 import type { ProviderId } from '@/core/providers/types';
 import type { SlashCommand } from '@/core/types';
+import { t } from '@/i18n/i18n';
 import { normalizeArgumentHint } from '@/utils/slashCommand';
 
 import type {
@@ -92,7 +93,7 @@ export class SlashCommandSource implements ComposerDropdownSource {
     if (result && result.status !== 'ready' && result.status !== 'empty') {
       const label = result.status === 'requires-session' || result.status === 'error'
         ? result.message
-        : 'Loading provider commands…';
+        : t('ui.loadingProviderCommands');
       const statusItems: ComposerDropdownItem[] = [
         ...items,
         {
@@ -104,11 +105,11 @@ export class SlashCommandSource implements ComposerDropdownSource {
       ];
       if (result.status === 'error' && result.retryable) {
         statusItems.push({
-          detail: 'Try provider command discovery again',
+          detail: t('ui.tryProviderCommandDiscoveryAgain'),
           icon: 'refresh-cw',
           id: 'provider-retry',
           kind: 'value',
-          label: 'Retry',
+          label: t('collab.panel.retry'),
           replacement: '',
           value: { kind: 'retry' } satisfies SlashValue,
         });
diff --git a/src/shared/modals/InstructionConfirmModal.ts b/src/shared/modals/InstructionConfirmModal.ts
index 03a0958..72b1d2f 100644
--- a/src/shared/modals/InstructionConfirmModal.ts
+++ b/src/shared/modals/InstructionConfirmModal.ts
@@ -6,10 +6,11 @@
  * - Clarification (agent asks question)
  * - Confirmation (final instruction review)
  */
-
 import type { App } from 'obsidian';
 import { Modal, TextAreaComponent } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 export type InstructionDecision = 'accept' | 'reject';
 
 type ModalState = 'loading' | 'clarification' | 'confirmation';
@@ -59,12 +60,12 @@ export class InstructionModal extends Modal {
   onOpen() {
     const { contentEl } = this;
     contentEl.addClass('claudian-instruction-modal');
-    this.setTitle('Add custom instruction');
+    this.setTitle(t('ui.addCustomInstruction'));
 
     // User input section (always visible)
     const inputSection = contentEl.createDiv({ cls: 'claudian-instruction-section' });
     const inputLabel = inputSection.createDiv({ cls: 'claudian-instruction-label' });
-    inputLabel.setText('Your input:');
+    inputLabel.setText(t('ui.yourInput'));
     const inputText = inputSection.createDiv({ cls: 'claudian-instruction-original' });
     inputText.setText(this.rawInstruction);
 
@@ -74,7 +75,7 @@ export class InstructionModal extends Modal {
     // Loading state
     this.loadingEl = this.contentSectionEl.createDiv({ cls: 'claudian-instruction-loading' });
     this.loadingEl.createDiv({ cls: 'claudian-instruction-spinner' });
-    this.loadingEl.createSpan({ text: 'Processing your instruction...' });
+    this.loadingEl.createSpan({ text: t('ui.processingYourInstruction') });
 
     // Clarification state (hidden initially)
     this.clarificationEl = this.contentSectionEl.createDiv({ cls: 'claudian-instruction-clarification-section' });
@@ -83,12 +84,12 @@ export class InstructionModal extends Modal {
 
     const responseSection = this.clarificationEl.createDiv({ cls: 'claudian-instruction-section' });
     const responseLabel = responseSection.createDiv({ cls: 'claudian-instruction-label' });
-    responseLabel.setText('Your response:');
+    responseLabel.setText(t('ui.yourResponse'));
 
     this.responseTextarea = new TextAreaComponent(responseSection);
     this.responseTextarea.inputEl.addClass('claudian-instruction-response-textarea');
     this.responseTextarea.inputEl.rows = 3;
-    this.responseTextarea.inputEl.placeholder = 'Provide more details...';
+    this.responseTextarea.inputEl.placeholder = t('ui.provideMoreDetails');
 
     this.responseTextarea.inputEl.addEventListener('keydown', (e) => {
       // Check !e.isComposing for IME support (Chinese, Japanese, Korean, etc.)
@@ -105,7 +106,7 @@ export class InstructionModal extends Modal {
     // Refined instruction display/edit
     const refinedSection = this.confirmationEl.createDiv({ cls: 'claudian-instruction-section' });
     const refinedLabel = refinedSection.createDiv({ cls: 'claudian-instruction-label' });
-    refinedLabel.setText('Refined snippet:');
+    refinedLabel.setText(t('ui.refinedSnippet'));
 
     this.refinedDisplayEl = refinedSection.createDiv({ cls: 'claudian-instruction-refined' });
     this.editContainerEl = refinedSection.createDiv({ cls: 'claudian-instruction-edit-container' });
@@ -184,33 +185,33 @@ export class InstructionModal extends Modal {
     this.buttonsEl.empty();
 
     const cancelBtn = this.buttonsEl.createEl('button', {
-      text: 'Cancel',
+      text: t('common.cancel'),
       cls: 'claudian-instruction-btn claudian-instruction-reject-btn',
-      attr: { 'aria-label': 'Cancel' }
+      attr: { 'aria-label': t('common.cancel') }
     });
     cancelBtn.addEventListener('click', () => this.handleReject());
 
     if (this.state === 'clarification') {
       const submitBtn = this.buttonsEl.createEl('button', {
-        text: 'Submit',
+        text: t('ui.submit'),
         cls: 'claudian-instruction-btn claudian-instruction-accept-btn',
-        attr: { 'aria-label': 'Submit response' }
+        attr: { 'aria-label': t('ui.submitResponse') }
       });
       submitBtn.addEventListener('click', () => {
         void this.submitClarification();
       });
     } else if (this.state === 'confirmation') {
       this.editBtnEl = this.buttonsEl.createEl('button', {
-        text: 'Edit',
+        text: t('common.edit'),
         cls: 'claudian-instruction-btn claudian-instruction-edit-btn',
-        attr: { 'aria-label': 'Edit instruction' }
+        attr: { 'aria-label': t('ui.editInstruction') }
       });
       this.editBtnEl.addEventListener('click', () => this.toggleEdit());
 
       const acceptBtn = this.buttonsEl.createEl('button', {
-        text: 'Accept',
+        text: t('ui.accept'),
         cls: 'claudian-instruction-btn claudian-instruction-accept-btn',
-        attr: { 'aria-label': 'Accept instruction' }
+        attr: { 'aria-label': t('ui.acceptInstruction') }
       });
       acceptBtn.addEventListener('click', () => this.handleAccept());
       acceptBtn.focus();
@@ -238,7 +239,7 @@ export class InstructionModal extends Modal {
     if (this.isEditing) {
       this.refinedDisplayEl?.addClass('claudian-hidden');
       this.editContainerEl?.removeClass('claudian-hidden');
-      if (this.editBtnEl) this.editBtnEl.setText('Preview');
+      if (this.editBtnEl) this.editBtnEl.setText(t('common.preview'));
       this.editTextarea?.inputEl.focus();
     } else {
       const edited = this.editTextarea?.getValue() || this.refinedInstruction;
@@ -248,7 +249,7 @@ export class InstructionModal extends Modal {
         this.refinedDisplayEl.removeClass('claudian-hidden');
       }
       this.editContainerEl?.addClass('claudian-hidden');
-      if (this.editBtnEl) this.editBtnEl.setText('Edit');
+      if (this.editBtnEl) this.editBtnEl.setText(t('common.edit'));
     }
   }
 
diff --git a/src/shared/settings/AgentSkillSettings.ts b/src/shared/settings/AgentSkillSettings.ts
index 5a490fb..e41fe6a 100644
--- a/src/shared/settings/AgentSkillSettings.ts
+++ b/src/shared/settings/AgentSkillSettings.ts
@@ -32,7 +32,7 @@ function errorMessage(error: unknown): string {
   ) {
     return error.message;
   }
-  return 'Unexpected storage error';
+  return t('ui.unexpectedStorageError');
 }
 
 function showSaveError(error: unknown, skillName: string): void {
diff --git a/src/shared/settings/EnvSnippetManager.ts b/src/shared/settings/EnvSnippetManager.ts
index 0d534f1..d743156 100644
--- a/src/shared/settings/EnvSnippetManager.ts
+++ b/src/shared/settings/EnvSnippetManager.ts
@@ -138,8 +138,8 @@ export class EnvSnippetModal extends Modal {
           cls: 'claudian-snippet-alias-input',
         });
         aliasInput.value = existingAliases[modelId] ?? '';
-        aliasInput.setAttribute('aria-label', `Alias for ${modelId}`);
-        aliasInput.title = 'Custom label shown in the model selector. Leave empty to use the default.';
+        aliasInput.setAttribute('aria-label', t('ui.aliasForFormat', { p0: modelId }));
+        aliasInput.title = t('ui.customLabelShownInTheModelSelectorLeaveEmptyToUseTheDefault');
         modelAliasInputs.set(modelId, aliasInput);
 
         const input = row.createEl('input', {
@@ -148,7 +148,7 @@ export class EnvSnippetModal extends Modal {
           cls: 'claudian-snippet-limit-input',
         });
         input.value = existingLimits[modelId] ? formatContextLimit(existingLimits[modelId]) : '';
-        input.setAttribute('aria-label', `Context window for ${modelId}`);
+        input.setAttribute('aria-label', t('ui.contextWindowForFormat', { p0: modelId }));
         contextLimitInputs.set(modelId, input);
       }
     };
@@ -272,7 +272,7 @@ export class EnvSnippetManager {
 
       const restoreBtn = actionsEl.createEl('button', {
         cls: 'claudian-settings-action-btn',
-        attr: { 'aria-label': 'Insert' },
+        attr: { 'aria-label': t('ui.insert') },
       });
       setIcon(restoreBtn, 'clipboard-paste');
       restoreBtn.addEventListener('click', () => {
@@ -280,14 +280,14 @@ export class EnvSnippetManager {
         try {
           await this.insertSnippet(snippet);
         } catch {
-          new Notice('Failed to insert snippet');
+          new Notice(t('ui.failedToInsertSnippet'));
         }
         })();
       });
 
       const editBtn = actionsEl.createEl('button', {
         cls: 'claudian-settings-action-btn',
-        attr: { 'aria-label': 'Edit' },
+        attr: { 'aria-label': t('common.edit') },
       });
       setIcon(editBtn, 'pencil');
       editBtn.addEventListener('click', () => {
@@ -296,17 +296,17 @@ export class EnvSnippetManager {
 
       const deleteBtn = actionsEl.createEl('button', {
         cls: 'claudian-settings-action-btn claudian-settings-delete-btn',
-        attr: { 'aria-label': 'Delete' },
+        attr: { 'aria-label': t('common.delete') },
       });
       setIcon(deleteBtn, 'trash-2');
       deleteBtn.addEventListener('click', () => {
         void (async (): Promise<void> => {
         try {
-          if (await confirmDelete(this.plugin.app, `Delete environment snippet "${snippet.name}"?`)) {
+          if (await confirmDelete(this.plugin.app, t('ui.deleteEnvironmentSnippetFormat', { p0: snippet.name }))) {
             await this.deleteSnippet(snippet);
           }
         } catch {
-          new Notice('Failed to delete snippet');
+          new Notice(t('ui.failedToDeleteSnippet'));
         }
         })();
       });
@@ -325,7 +325,7 @@ export class EnvSnippetManager {
             settings.envSnippets.push(snippet);
           });
           this.render();
-          new Notice(`Environment snippet "${snippet.name}" saved`);
+          new Notice(t('ui.environmentSnippetSavedFormat', { p0: snippet.name }));
         })();
       }
     );
@@ -400,7 +400,7 @@ export class EnvSnippetManager {
               }
             });
             this.render();
-            new Notice(`Environment snippet "${updatedSnippet.name}" updated`);
+            new Notice(t('ui.environmentSnippetUpdatedFormat', { p0: updatedSnippet.name }));
           }
         })();
       }
@@ -413,7 +413,7 @@ export class EnvSnippetManager {
       settings.envSnippets = settings.envSnippets.filter(s => s.id !== snippet.id);
     });
     this.render();
-    new Notice(`Environment snippet "${snippet.name}" deleted`);
+    new Notice(t('ui.environmentSnippetDeletedFormat', { p0: snippet.name }));
   }
 
   public refresh() {
diff --git a/src/shared/settings/EnvironmentSettingsSection.ts b/src/shared/settings/EnvironmentSettingsSection.ts
index e6fde85..29e4847 100644
--- a/src/shared/settings/EnvironmentSettingsSection.ts
+++ b/src/shared/settings/EnvironmentSettingsSection.ts
@@ -1,5 +1,7 @@
 import { Setting } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 import { getEnvironmentReviewKeysForScope } from '../../core/providers/providerEnvironment';
 import type { ProviderHost } from '../../core/providers/ProviderHost';
 import type { EnvironmentScope } from '../../core/types/settings';
@@ -47,7 +49,7 @@ export function renderEnvironmentSettingsSection(
       return;
     }
 
-    reviewEl.setText(`Review environment ownership for: ${reviewKeys.join(', ')}`);
+    reviewEl.setText(t('ui.reviewEnvironmentOwnershipForFormat', { p0: reviewKeys.join(', ') }));
     reviewEl.toggleClass('claudian-hidden', false);
   };
 
diff --git a/src/shared/settings/ProviderModelPicker.ts b/src/shared/settings/ProviderModelPicker.ts
index b8b062a..8655242 100644
--- a/src/shared/settings/ProviderModelPicker.ts
+++ b/src/shared/settings/ProviderModelPicker.ts
@@ -1,7 +1,9 @@
 import { Setting } from 'obsidian';
 
+import { t } from '@/i18n/i18n';
+
 const ALL_PROVIDERS_KEY = 'all';
-const VISIBLE_MODELS_DESCRIPTION = 'Choose which models are available in the chat selector. Drag to reorder them; the provider uses the first currently usable model as its default. Select at least one model to use this provider.';
+const VISIBLE_MODELS_DESCRIPTION = () => t('ui.chooseWhichModelsAreAvailableInTheChatSelectorDragToReorderThemTheProviderUsesTheFirstCurrentlyUsableModelAsItsDefaultSelectAtLeastOneModelToUseThisProvider');
 
 export function reorderProviderModelIds(
   selectedIds: readonly string[],
@@ -67,8 +69,8 @@ export function renderProviderModelPicker(
   options: ProviderModelPickerOptions,
 ): ProviderModelPickerController {
   const visibleModelsSetting = new Setting(options.container)
-    .setName('Visible models')
-    .setDesc(VISIBLE_MODELS_DESCRIPTION);
+    .setName(t('ui.visibleModels'))
+    .setDesc(VISIBLE_MODELS_DESCRIPTION());
   visibleModelsSetting.settingEl.addClass('claudian-provider-model-picker-setting');
 
   const pickerEl = options.container.createDiv({
@@ -94,7 +96,7 @@ export function renderProviderModelPicker(
   });
   catalogSummaryEl.createSpan({
     cls: 'claudian-provider-model-picker-catalog-title',
-    text: 'Browse models',
+    text: t('ui.browseModels'),
   });
   const catalogSummaryCountEl = catalogSummaryEl.createSpan({
     cls: 'claudian-provider-model-picker-catalog-count',
@@ -105,7 +107,7 @@ export function renderProviderModelPicker(
     cls: 'claudian-provider-model-picker-search',
     type: 'search',
   });
-  searchInput.placeholder = options.searchPlaceholder ?? 'Filter by model, provider, or ID...';
+  searchInput.placeholder = options.searchPlaceholder ?? t('ui.filterByModelProviderOrID');
   searchInput.addEventListener('input', () => {
     searchQuery = searchInput.value.trim().toLowerCase();
     renderList();
@@ -121,7 +123,7 @@ export function renderProviderModelPicker(
 
   const catalogActionEl = controlsEl.createEl('button', {
     cls: 'claudian-provider-model-picker-action',
-    text: 'Discover',
+    text: t('ui.discover'),
   });
   catalogActionEl.setAttribute('type', 'button');
   catalogActionEl.addEventListener('click', () => {
@@ -137,31 +139,31 @@ export function renderProviderModelPicker(
       state.models.map(model => model.providerKey).filter((key): key is string => Boolean(key)),
     ).size;
 
-    summaryEl.createSpan({ text: 'Visible: ' });
+    summaryEl.createSpan({ text: t('ui.visible') });
     summaryEl.createSpan({
       cls: 'claudian-provider-model-picker-summary-value',
       text: String(state.selectedIds.length),
     });
     summaryEl.createSpan({
       text: providerCount > 0
-        ? ` of ${state.discoveredCount} discovered | ${providerCount} ${providerCount === 1 ? 'provider' : 'providers'}`
-        : ` of ${state.discoveredCount} discovered`,
+        ? t('ui.ofDiscoveredFormat', { p0: state.discoveredCount, p1: providerCount, p2: providerCount === 1 ? t('ui.provider') : t('ui.providers') })
+        : t('ui.ofDiscoveredFormat2', { p0: state.discoveredCount }),
     });
 
     catalogSummaryCountEl.setText(
       loadingCatalog
-        ? 'Loading models...'
+        ? t('ui.loadingModels')
         : state.discoveredCount > 0
         ? `${state.discoveredCount} available`
-        : 'No models discovered yet',
+        : t('ui.noModelsDiscoveredYet'),
     );
     catalogActionEl.disabled = loadingCatalog;
     catalogActionEl.setText(
       loadingCatalog
-        ? 'Loading...'
+        ? t('ui.loading2')
         : state.discoveredCount > 0
-        ? 'Refresh'
-        : 'Discover',
+        ? t('common.refresh')
+        : t('ui.discover'),
     );
   };
 
@@ -199,14 +201,14 @@ export function renderProviderModelPicker(
     const headerEl = selectedEl.createDiv({ cls: 'claudian-provider-model-picker-selected-header' });
     headerEl.createSpan({
       cls: 'claudian-provider-model-picker-selected-label',
-      text: `Selected (${state.selectedIds.length})`,
+      text: t('ui.selectedFormat', { p0: state.selectedIds.length }),
     });
     const clearAllButton = headerEl.createEl('button', {
       cls: 'claudian-provider-model-picker-selected-clear',
-      text: 'Clear all',
+      text: t('common.clearAll'),
     });
     clearAllButton.setAttribute('type', 'button');
-    clearAllButton.setAttribute('aria-label', `Clear all selected ${options.providerName} models`);
+    clearAllButton.setAttribute('aria-label', t('ui.clearAllSelectedModelsFormat', { p0: options.providerName }));
     clearAllButton.addEventListener('click', () => {
       void persistSelectedIds([]);
     });
@@ -259,9 +261,9 @@ export function renderProviderModelPicker(
       dragHandle.setAttribute('type', 'button');
       dragHandle.setAttribute(
         'aria-label',
-        `Reorder ${defaultLabel}; drag or use the Up and Down Arrow keys`,
+        t('ui.reorderDragOrUseTheUpAndDownArrowKeysFormat', { p0: defaultLabel }),
       );
-      dragHandle.setAttribute('title', 'Drag or use arrow keys to reorder');
+      dragHandle.setAttribute('title', t('ui.dragOrUseArrowKeysToReorder'));
       dragHandle.draggable = state.selectedIds.length > 1;
       dragHandle.addEventListener('dragstart', (event) => {
         draggedModelId = modelId;
@@ -314,7 +316,7 @@ export function renderProviderModelPicker(
       if (modelId === defaultModelId) {
         titleEl.createSpan({
           cls: 'claudian-provider-model-picker-selected-default',
-          text: 'Default',
+          text: t('ui.default'),
         });
       }
       if (model.isAvailable === false && model.unavailableMessage) {
@@ -334,7 +336,7 @@ export function renderProviderModelPicker(
       });
       aliasFieldEl.createSpan({
         cls: 'claudian-provider-model-picker-selected-alias-label',
-        text: 'Alias (optional)',
+        text: t('ui.aliasOptional'),
       });
       const aliasInput = aliasFieldEl.createEl('input', {
         cls: 'claudian-provider-model-picker-selected-alias',
@@ -342,8 +344,8 @@ export function renderProviderModelPicker(
       });
       aliasInput.placeholder = defaultLabel;
       aliasInput.value = state.aliases[model.id] ?? '';
-      aliasInput.setAttribute('aria-label', `Alias for ${defaultLabel}`);
-      aliasInput.title = 'Custom label shown in the model selector. Leave empty to use the default.';
+      aliasInput.setAttribute('aria-label', t('ui.aliasForFormat', { p0: defaultLabel }));
+      aliasInput.title = t('ui.customLabelShownInTheModelSelectorLeaveEmptyToUseTheDefault');
       aliasInput.addEventListener('blur', () => {
         void persistAlias(model.id, aliasInput.value);
       });
@@ -363,7 +365,7 @@ export function renderProviderModelPicker(
         text: '×',
       });
       removeButton.setAttribute('type', 'button');
-      removeButton.setAttribute('aria-label', `Remove ${defaultLabel}`);
+      removeButton.setAttribute('aria-label', t('ui.removeFormat', { p0: defaultLabel }));
       removeButton.addEventListener('click', () => {
         void persistSelectedIds(options.getState().selectedIds.filter(id => id !== model.id));
       });
@@ -388,7 +390,7 @@ export function renderProviderModelPicker(
     providerSelectEl.toggleClass('claudian-hidden', providers.size === 0);
     providerSelectEl.empty();
     providerSelectEl.createEl('option', {
-      text: `All providers (${models.length})`,
+      text: t('ui.allProvidersFormat', { p0: models.length }),
       value: ALL_PROVIDERS_KEY,
     });
     for (const [key, { count, label }] of Array.from(providers.entries())
@@ -437,7 +439,7 @@ export function renderProviderModelPicker(
           ? options.failedCatalogText
           : state.models.length === 0
           ? options.emptyCatalogText
-          : 'No models match your filter.',
+          : t('ui.noModelsMatchYourFilter'),
       });
       return;
     }
@@ -474,7 +476,7 @@ export function renderProviderModelPicker(
         text: model.name,
       });
       const badgeLabel = model.isAvailable === false
-        ? 'Unavailable'
+        ? t('ui.unavailable')
         : model.catalogBadge ?? model.providerLabel;
       if (badgeLabel) {
         const badgeEl = headerEl.createSpan({
@@ -483,7 +485,7 @@ export function renderProviderModelPicker(
         });
         if (model.isAvailable === false) {
           badgeEl.classList.add('claudian-provider-model-picker-row-badge--unavailable');
-          badgeEl.title = model.unavailableTitle ?? `Configured model not currently reported by ${options.providerName}`;
+          badgeEl.title = model.unavailableTitle ?? t('ui.configuredModelNotCurrentlyReportedByFormat', { p0: options.providerName });
         }
       }
       textEl.createDiv({
diff --git a/tests/unit/i18n/locales.test.ts b/tests/unit/i18n/locales.test.ts
index ddeafc3..557d275 100644
--- a/tests/unit/i18n/locales.test.ts
+++ b/tests/unit/i18n/locales.test.ts
@@ -1,3 +1,4 @@
+import { setLocale, t } from '@/i18n/i18n';
 import * as de from '@/i18n/locales/de.json';
 import * as en from '@/i18n/locales/en.json';
 import * as es from '@/i18n/locales/es.json';
@@ -8,6 +9,7 @@ import * as pt from '@/i18n/locales/pt.json';
 import * as ru from '@/i18n/locales/ru.json';
 import * as zhCN from '@/i18n/locales/zh-CN.json';
 import * as zhTW from '@/i18n/locales/zh-TW.json';
+import type { TranslationKey } from '@/i18n/types';
 
 interface TranslationTree {
   [key: string]: string | TranslationTree;
@@ -136,11 +138,24 @@ describe('locale files', () => {
 
     for (const [locale, translations] of Object.entries(locales)) {
       const localeKeys = Object.keys(flattenTranslations(translations as unknown as TranslationTree)).sort();
-      expect(localeKeys).toEqual(englishKeys);
+      expect(localeKeys).toEqual(locale === 'es' ? englishKeys : englishKeys.filter(key => !key.startsWith('ui.') && !key.startsWith('default.ui.')));
       expect(locale).toBeTruthy();
     }
   });
 
+  it('uses the existing English fallback for the Spanish patch in other locales', () => {
+    try {
+      for (const locale of Object.keys(locales).filter(locale => locale !== 'es')) {
+        setLocale(locale as Parameters<typeof setLocale>[0]);
+        for (const key of Object.keys(en.ui)) {
+          expect(t(`ui.${key}` as TranslationKey)).toBe(english[`ui.${key}`]);
+        }
+      }
+    } finally {
+      setLocale('en');
+    }
+  });
+
   it('localizes the recent bang bash and subagent additions', () => {
     for (const translations of Object.values(locales)) {
       const locale = flattenTranslations(translations as unknown as TranslationTree);
diff --git a/tests/unit/providers/claude/ui/ClaudeSettingsTab.test.ts b/tests/unit/providers/claude/ui/ClaudeSettingsTab.test.ts
index 2ada020..69009c1 100644
--- a/tests/unit/providers/claude/ui/ClaudeSettingsTab.test.ts
+++ b/tests/unit/providers/claude/ui/ClaudeSettingsTab.test.ts
@@ -648,7 +648,7 @@ describe('ClaudeSettingsTab', () => {
 
     claudeSettingsTabRenderer.render(createContainer(), context);
 
-    const setting = findSetting('Default model');
+    const setting = findSetting('ui.defaultModel');
     const dropdown = setting.dropdownComponents[0];
     expect(dropdown.value).toBe('claude-code/claude-opus-4-6');
     expect(dropdown.options).toEqual(expect.arrayContaining([
@@ -673,7 +673,7 @@ describe('ClaudeSettingsTab', () => {
 
     claudeSettingsTabRenderer.render(createContainer(), context);
 
-    const dropdown = findSetting('Default model').dropdownComponents[0];
+    const dropdown = findSetting('ui.defaultModel').dropdownComponents[0];
     expect(dropdown.value).toBe('claude-code/claude-opus-enterprise');
 
     await dropdown.onChangeCallback?.('claude-code/claude-sonnet-enterprise');
@@ -738,9 +738,9 @@ describe('ClaudeSettingsTab', () => {
     const safeModeDropdown = safeModeSetting.dropdownComponents[0];
 
     expect(safeModeDropdown.options).toEqual([
-      { value: 'acceptEdits', label: 'acceptEdits' },
-      { value: 'auto', label: 'auto' },
-      { value: 'default', label: 'default' },
+      { value: 'acceptEdits', label: 'ui.safeAcceptEdits' },
+      { value: 'auto', label: 'ui.safeAuto' },
+      { value: 'default', label: 'ui.safeDefault' },
     ]);
 
     await safeModeDropdown.onChangeCallback?.('auto');
